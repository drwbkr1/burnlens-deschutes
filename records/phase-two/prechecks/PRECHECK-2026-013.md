# PRECHECK-2026-013 - Cross-Event Five-State Transfer Entry Gate

**Issue / branch / base:** #367 / `codex/p2o4-t04-cross-event-label-transfer` / `2bdec053022aa02b7fd322ece37013fd8a3a56bc`

## Cycle-start evidence

The synchronized BurnLens 0.11 workflow reproduced `CROSS-EVENT-SOURCE-FITNESS-2026-001` JSON, HTML, and PNG byte for byte from the registered four-archive Sentinel package. The predecessor again accepts McKay and accepts Tepee only with SCL and registration exclusions binding. It creates no labels, dataset, split, model, baseline, accuracy result, or application.

The highest-leverage evidence-visible weakness is therefore whether the already implemented five-state proposal can transfer to both independent event groups without erasing uncertainty or laundering an analyst reference into truth. More event discovery, source acquisition, threshold search, model work, or interface breadth would not answer that question.

## Frozen inputs and source precedence

- Predecessor report: `CROSS-EVENT-SOURCE-FITNESS-2026-001`; run `BL-2026-07-16-cross-event-source-fitness-r006`; JSON SHA-256 `c9aa90894150e081a6df26b8ee16c502bd532b67d7ecd493e038a366a480b34f`.
- Four exact registered Sentinel archives: `SOURCE-2026-012`; provider byte and native-pixel fitness gates remain binding.
- Two exact public MTBS clips: `SOURCE-2026-013`; `TERMS-2026-008`; provider bytes remain ignored.
- Design protocol: `burn-scar-label-protocol-v0.1.0`.
- Implemented state schema: `burn-scar-five-state-schema-v0.1.0`.

Sentinel native pixels and SCL control spectral/quality evidence; the frozen event geometry controls boundary context; source-fitness windows control registration eligibility; MTBS classes provide only analyst-interpreted reference support or ambiguity. No source may silently override a stronger restriction.

## Allowed action

Produce one five-state proposal raster and one binary candidate-target raster for each frozen event, plus machine-readable, semantic, and rendered comparison evidence. Recompute the complete decision independently in a separately invoked QA module that does not import the proposal classifier. Use the owner-authorized burn-scar binary-mask fallback only as an event-relative stability screen when the inherited fixed near-zero background rule does not transfer; retain a fixed cap, lowest-tail selection, spatial-coherence gate, and explicit diagnostics.

## Prohibited action

No event or source expansion, threshold optimization against desired counts, label fabrication for an absent state, training/validation/test split, accepted dataset, model, baseline, performance/generalization metric, operational application, field-validation claim, or official/emergency/endorsement implication. MTBS or SCL alone may never create a burned candidate. Pixels outside an event boundary may never become background from source absence alone.

## Entry gates passed

- Issue #367 freezes the bounded outcome, sources, five-state behavior, independent-QA requirement, and render gates.
- The issue's protocol transcription was corrected from `v0.2.0` to the repository's actual frozen `v0.1.0` before source freeze.
- Branch begins at synchronized `main`; predecessor replay is byte-identical.
- Current official MTBS and Sentinel processing/product sources were freshly reviewed.
- `TERMS-2026-007` and `TERMS-2026-008` resolve the bounded source and derived-evidence actions.
- The two exact MTBS clips pass byte, grid, class-domain, and bounded OneDrive-link verification; all four Sentinel archives retain strict link and provider-integrity gates.
- 136 tests, compilation, dependency health, diff checks, and raw-provider-byte exclusion pass before the real final run.

Decision: `PASS_CROSS_EVENT_LABEL_TRANSFER_ENTRY_GATE`.

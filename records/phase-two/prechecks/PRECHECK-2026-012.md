# PRECHECK-2026-012 - Cross-Event Source-Fitness Entry Gate

**Issue / branch / base:** #361 / `codex/p2o4-t03-cross-event-source-fitness` / `2523207ad10ca88d822bd34d96bb482feded6072`; pre-tag trace remediation #363 / `codex/p2o4-t03-label-trace-remediation` / analytical merge `6a6da910849daefa918ed56af6631b2ec44bc211`

## Cycle-start evidence

The synchronized BurnLens 0.10 workflow reproduced `CROSS-EVENT-FITNESS-2026-001` JSON, HTML, and PNG byte for byte. It again selected only Tepee and McKay and stated that availability is not pixel fitness. Dataset, split, baseline, model, and application versions remain null.

The highest-leverage weakness is therefore authenticated delivery and real native-pixel fitness for the four already-frozen products. More catalogue breadth, events, thresholds, tiles, labels, or model work would not answer that question.

## Source-precedence gate

Current official OData and STAC records agree on UUID and SAFE identity but expose route-specific archive sizes/checksums. `SOURCE_PRECEDENCE-2026-005` assigns OData metadata and the documented OData download route authority over acquired archive bytes while preserving STAC Product assets as discovery provenance. No provider byte may be requested if any scene identity or OData contract field drifts.

## Allowed action

Acquire exactly four authorized archives, validate and register them atomically below ignored storage, independently reverify them, inspect native event pixels, measure pair-local registration, render bounded derived evidence, and accept/remediate/reject each pair.

## Prohibited action

No fifth product, event expansion, mosaic, boundary shrink, native archive commit/redistribution, label, dataset, partition, patch set, baseline, model, application, deployment, performance/generalization claim, or official/operational/emergency/field/endorsement implication.

## Pre-data gates passed

- issue #361 freezes exact outcome, products, non-goals, and quality gates;
- branch begins at synchronized `main`;
- v0.10 predecessor artifacts reproduce exactly;
- current official token/OData/Sentinel/terms sources reviewed;
- `TERMS-2026-007` resolves bounded internal acquisition;
- `ACCESS-2026-010` records explicit credential authorization and secret boundary;
- source commit `9693f0f5d1476740e06d4a69620072e70cd2002d` passes 118 tests, compilation, dependency health, focused live metadata refresh, diff, raw-exclusion, and secret-assignment checks.

Decision: `PASS_EXACT_CROSS_EVENT_ACQUISITION_ENTRY_GATE`.

## Gate fulfillment

The allowed action completed under acquisition run `BL-2026-07-16-cross-event-optical-intake-r005`. Analytical source-fitness run `r005` produced the accepted measurements, but fresh merged-main semantic verification correctly withheld the tag because its `label_schema_version` named the earlier design protocol while the manifest named the implemented five-state schema. Issue #363 adds a fail-closed schema-input gate and explicit protocol/schema fields. Remediated run `BL-2026-07-16-cross-event-source-fitness-r006`, source `cf1d9101e2760bf7d779b6fae68e605bb8809c1c`, preserves every event, pixel, window, metric, decision, lineage field, and input hash while aligning the trace. All four exact archives remain registered and reverified. McKay passes; Tepee is accepted only with its SCL and window exclusions binding. The prohibited label/dataset/split/baseline/model/application actions remain absent. PR #364 merged at `01c3aa4abeb89e3f15771571276a25d33e44d390`; a fresh merged-main clone passes all 129 tests and release gates; verified tag object `eca7ba5362518684f2a1e25d5abdbc1707e24a61` remotely peels to that merge.

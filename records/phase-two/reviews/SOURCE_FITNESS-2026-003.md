# SOURCE_FITNESS-2026-003 - Exact Cross-Event Native-Pixel Fitness

**Issue:** #361

**Decision:** `ACCEPT_CROSS_EVENT_SOURCE_FITNESS_WITH_EXCLUSIONS`

## Evidence reviewed

Acquisition run `BL-2026-07-16-cross-event-optical-intake-r005` registers all four OData-governed archives. Remediated evidence run `BL-2026-07-16-cross-event-source-fitness-r006`, generated by source `cf1d9101e2760bf7d779b6fae68e605bb8809c1c`, re-verifies the registered package, opens exact native TCI/B04/B8A/B12/SCL members, counts quality only at pixel centers inside each full MTBS boundary, measures independent-gradient registration, and renders JSON/HTML/PNG. It preserves every scientific result, lineage field, and input hash from `r005` while exposing both the governing label protocol and implemented five-state schema.

| Event | Eligible | Review-needed | Excluded | Registration |
|---|---:|---:|---:|---|
| Tepee | 86.5217% | 8.3441% | 5.1341% | W-01 excluded; W-02 review-needed; W-03 pass; p95 `0.0976` px |
| McKay | 100.0000% | 0% | 0% | 3/3 pass; p95 `0.1008` px |

The Tepee exclusions are binding downstream evidence states, not defects to hide. Registration windows may use surrounding native context for correlation, but only full-boundary pixels enter quality evidence. No shared correlation mask, reprojection, resampling, upsampling, mosaic, or boundary shrink is used.

## Storage and integrity exception

All four provider archives are single-linked and match exact provider/local hashes plus ZIP/SAFE/CRC checks. OneDrive created a second link only for `.burnlens-registration.json`. The cross-event verifier accepts that metadata-only exception after one-read SHA-256 and complete registration-content comparison; default package verification still rejects multiply linked manifests. The report makes the exception visible and records manifest SHA-256 `7827232e4986d8209fefd4ebbe160d3c9432bcd18562fad0587afcf6d79ed26c`.

## Rendered QA

- original PNG reviewed at 1,800 by 1,450 pixels; both full-boundary pre/post pairs, event dispositions, manifest exception, attribution, warning, and trace are legible;
- semantic HTML reviewed in the in-app browser: one H1, exact event/product/window rows, Tepee exclusions, McKay passes, attribution, null analytical versions, source commit, evidence run, acquisition run, and manifest exception are visible;
- JSON/HTML/PNG are deterministic for fixed registered bytes and run inputs;
- visual decision matches the machine decision: `ACCEPT_CROSS_EVENT_SOURCE_FITNESS_WITH_EXCLUSIONS`.

## Narrow acceptance

Accept these exact sources only for cross-event five-state proposal and QA work. No SCL class, reflectance change, MTBS boundary, or registration result is label truth. No dataset, split, untouched test set, baseline, model, accuracy, generalization, application, deployment, independent human annotation, field validation, official status, or operational capability exists.

# ACCESS-2026-008 - Bounded NOAA-21 Observation Screen Delivery

**Access date:** 2026-07-14 UTC

**Issue / branch:** #333 / `codex/p2o2-t04-observation-geometry`

**Authorization:** `ACCESS-2026-006` and the owner's current explicit permission to use the machine-bound NASA account

**Run:** `BL-2026-07-14-observation-geometry-r002`

## Result

`AUTHENTICATED_REGISTERED_EXACT_OBSERVATION_SCREEN_PACKAGE`.

BurnLens used the owner-authorized, machine-bound NASA Earthdata credential at runtime to acquire only the 23 official CMR `VJ214IMG.002` inventory granules in the frozen AOI/time window plus the exact `VJ203MODLL.021` companion for the selected materially improved observation. CDSE authentication was not needed for this checkpoint.

The final package `darlene3-vj214img-observation-screen-v0.2.0` contains exactly 24 assets and 83,723,055 bytes. It passed exact identity, size, native-container, pair-token, local-multihash, all-or-none atomic-promotion, and registered-package re-verification gates. Contract SHA-256 is `af396fcbf6fb32860c4f76111ab74ac0f3d2c810ab2c1aba19903337a757ad3c`.

## Secret and transport boundary

- The protected credential was read only by the local wrapper under the authorized Windows user.
- Plaintext existed only in the child process environment and was removed in `finally`; no secret, cookie, token, signed URL, provider response body, or credential-store detail enters repository evidence.
- Earthdata authentication is restricted to the exact login host; redirects are HTTPS-only and allowlisted; cross-host authorization is stripped.
- Stable provider routes are retained, while transient signed queries are excluded from state and evidence.
- Raw provider assets and the local registration manifest remain under an ignored `downloads/` package. Zero provider raw bytes are committed.

## Reliability observation

One final promotion attempt failed closed with `ASSET_MULTILINK_NOT_ALLOWED` because OneDrive temporarily exposed an asset through an additional hard link. After OneDrive released the link, the unchanged package passed. No intake rule was bypassed or weakened.

## Non-implications

Successful delivery and registration prove bounded access and raw-integrity behavior. They do not prove label fitness, background truth, dataset readiness, a fire perimeter, a model result, operational value, or endorsement.


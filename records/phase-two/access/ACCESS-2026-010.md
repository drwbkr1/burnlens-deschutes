# ACCESS-2026-010 - Exact Cross-Event Sentinel Package Authorization

**Access date:** 2026-07-15 UTC

**Issue / branch:** #361 / `codex/p2o4-t03-cross-event-source-fitness`

**Authorization:** The owner's standing authorization for the machine-bound DPAPI-protected CDSE account, including the reported quota reset

## Pre-access state

`AUTHORIZED_EXACT_CROSS_EVENT_PACKAGE_NOT_YET_ACQUIRED`.

Public OData metadata was refreshed without a credential at `2026-07-15T23:35:00Z`. All four exact products are online and match the frozen UUID, SAFE name, platform, tile, relative orbit, processing baseline, acquisition time, and product type. `SOURCE-2026-012` freezes the route-specific OData archive sizes and checksums and discloses the separate STAC zipper packaging values.

## Secret and transport boundary

- Load only the CDSE username/password through the existing DPAPI-backed wrapper under the authorized Windows user.
- Pass plaintext only through short-lived child-process environment values, pop them immediately in Python, and remove them in PowerShell `finally`.
- Restrict token and product requests to HTTPS CDSE hosts; never log or retain credentials, tokens, authorization headers, cookies, query-bearing delivery URLs, or protected-store details.
- Download only the four issue-frozen UUID routes, sequentially, with exact resume, size, container, checksum, path, CRC, and link-count gates.
- Write provider bytes and private run state only under ignored `downloads/` paths; commit zero provider bytes.

## Non-implications

Authorization and successful delivery prove neither local pixel fitness nor label, dataset, split, baseline, model, application, accuracy, generalization, official, operational, emergency-ready, field-validated, or endorsed status.

## Fulfillment

Authenticated run `BL-2026-07-15-cross-event-optical-intake-r001` began at `2026-07-15T23:45:00Z`. The first Tepee archive response ended at `1,026,064,938` of `1,129,978,013` expected bytes, so the client returned `DOWNLOAD_SIZE_MISMATCH`. It created no raw registration and retained only the ignored resumable partial plus a bounded private failure record. Credential environment values were removed; the state record contains no credential, token, authorization header, cookie, protected-store detail, or provider payload.

OneDrive subsequently reported two hard links for the partial. The client was strengthened and regression-tested to return `PART_FILE_MULTILINK_NOT_ALLOWED` before any request whenever an existing partial is multiply linked. No external staging link was deleted or bypassed. A unique `r002` resume may begin only after the partial reports one link; all four exact archives and real source-fitness evidence therefore remain pending.

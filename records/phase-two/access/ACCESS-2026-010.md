# ACCESS-2026-010 - Exact Cross-Event Sentinel Package Authorization

**Access date:** 2026-07-15 UTC

**Issue / branch:** #361 / `codex/p2o4-t03-cross-event-source-fitness`

**Authorization:** The owner's standing authorization for the machine-bound DPAPI-protected CDSE account, including the reported quota reset

## Pre-access state

`AUTHORIZED_EXACT_CROSS_EVENT_PACKAGE_NOT_YET_ACQUIRED`.

Public OData metadata was refreshed without a credential at `2026-07-15T23:35:00Z`. All four exact products are online and match the frozen UUID, SAFE name, platform, tile, relative orbit, processing baseline, acquisition time, and product type. `SOURCE-2026-012` freezes the route-specific OData archive sizes and checksums and discloses the separate STAC zipper packaging values.

## Secret and transport boundary

- Load only the CDSE username/password through the existing DPAPI-backed wrapper under the authorized Windows user.
- Pass plaintext only through short-lived child-process environment values, pop them immediately in Python, and remove them in PowerShell `finally`.
- Restrict token and product requests to HTTPS CDSE hosts; never log or retain credentials, tokens, authorization headers, cookies, query-bearing delivery URLs, or protected-store details.
- Download only the four issue-frozen UUID routes, sequentially, with exact resume, size, container, checksum, path, CRC, and link-count gates.
- Write provider bytes and private run state only under ignored `downloads/` paths; commit zero provider bytes.

## Non-implications

Authorization and successful delivery prove neither local pixel fitness nor label, dataset, split, baseline, model, application, accuracy, generalization, official, operational, emergency-ready, field-validated, or endorsed status.

## Fulfillment

Authenticated run `BL-2026-07-15-cross-event-optical-intake-r001` began at `2026-07-15T23:45:00Z`. The first Tepee archive response ended at `1,026,064,938` of `1,129,978,013` expected bytes, so the client returned `DOWNLOAD_SIZE_MISMATCH`. It created no raw registration and retained only the ignored resumable partial plus a bounded private failure record. Credential environment values were removed; the state record contains no credential, token, authorization header, cookie, protected-store detail, or provider payload.

OneDrive subsequently reported two hard links for the partial. The client was strengthened and regression-tested to return `PART_FILE_MULTILINK_NOT_ALLOWED` before any request whenever an existing partial is multiply linked. No external staging link was deleted or bypassed. The aliased inode was preserved under ignored held evidence and a byte-identical, single-link repo-local copy was prepared for the next uniquely identified attempt.

Run `BL-2026-07-16-cross-event-optical-intake-r002` made no provider request because the fresh public metadata read returned a bounded `JSON_REQUEST_FAILED / URLError`; public access subsequently returned HTTP 200. Run `BL-2026-07-16-cross-event-optical-intake-r003` restarted the first object after the live OData route ignored its range header, reached `853,540,864` bytes, then stopped making progress while OneDrive staged another hard link. The wrapper was terminated, the ignored partial was preserved, and private state records `TRANSFER_STALLED_NO_PROGRESS`. Neither run created a raw registration or retained secret material.

Run `BL-2026-07-16-cross-event-optical-intake-r004` reused both accepted Tepee archives and completed all `1,132,353,399` McKay-pre bytes. McKay post reached `57,264,698` bytes on its first attempt; OneDrive then added a hard link before retry, so the client returned `PART_FILE_MULTILINK_NOT_ALLOWED` before a second request. No registration occurred.

The next client revision combines Microsoft’s two documented exclusions and stages incomplete cross-event objects as `~$<SAFE>.zip.tmp`. A live `~$...tmp` probe remained unchanged and single-linked for 30 seconds with OneDrive running; the earlier large `.zip.tmp` without the prefix had still been staged. Transient full-object retries remain bounded at five fresh-token attempts because the current OData route returned HTTP 200 with the complete object length and no `Content-Range` for a live range diagnostic. Final size, MD5, BLAKE3, ZIP/SAFE, CRC, path, link-count, atomic-promotion, and post-promotion gates are unchanged.

Run `BL-2026-07-16-cross-event-optical-intake-r005` completed all four exact archives and atomically registered `burnlens-cross-event-optical-package-v0.1.0`. The package contains 4,551,170,756 ignored local provider bytes. Tepee pre/post and McKay pre/post match their OData size, MD5, BLAKE3, ZIP/SAFE root, manifest and CRC contracts plus local SHA-256 `3d99aec012745da9231bec45c294a2914b5d6eb77a27dbbe4a720554ae0af609`, `9ff4bf957bfdc8d282ee5799a73d607d12e8c0f0c8499f7ad2e52fada3854366`, `32f5529cc254d74dffca4fc80774b0401dab96aa7dcdfd55d2b0440a7cefa8e7`, and `a735ecc6da284dbeb864210928dfc2997a9e2cbf50722b699a5fa6625d874555` respectively. The quarantine is absent after promotion; no credential, token, cookie, header, protected-store detail, or secret-bearing response is retained.

After registration, OneDrive repeatedly added an external staging alias to the small `.burnlens-registration.json` metadata file and once restored the exact Tepee-pre inode under its historical `.zip.part` name. BurnLens reverified the unchanged 1,129,978,013-byte SHA-256 and restored only the registered repo-local filename. All four provider archives remain single-linked. The generic verifier rejects multiply linked manifests; this exact cross-event package uses a visible metadata-only exception after one-read manifest SHA-256 `7827232e4986d8209fefd4ebbe160d3c9432bcd18562fad0587afcf6d79ed26c` and complete header, contract, acquisition-run, and asset comparison. External OneDrive aliases were not deleted or modified.

Final access state: `REGISTERED_EXACT_CROSS_EVENT_PACKAGE`.

# ACCESS-2026-009 - Exact Sentinel-2 optical-pair authorization

**Access date:** 2026-07-15 UTC

**Issue / branch:** #343 / `codex/p2o2-t06-optical-pair-protocol`

**Authorization:** The owner's standing authorization for the machine-bound DPAPI-protected CDSE account, including the reported quota reset

## Pre-access state

`AUTHORIZED_EXACT_PAIR_NOT_YET_ACQUIRED`.

Public official CDSE metadata was refreshed without a credential at `2026-07-15T16:00:59Z`. Both exact products are online and match the frozen UUID, SAFE name, byte size, provider MD5/BLAKE3, platform, tile, relative orbit, processing baseline, acquisition time, and tile cloud metadata recorded in `SOURCE-2026-009` and `SOURCE-2026-010`.

## Secret and transport boundary

- Load only the CDSE username/password through the existing local DPAPI-backed wrapper under the authorized Windows user.
- Pass plaintext only through short-lived child-process environment variables, pop them immediately in Python, and remove them in PowerShell `finally`.
- Restrict token and product requests to HTTPS CDSE hosts; do not log tokens, authorization headers, cookies, query-bearing delivery URLs, credential values, or protected-store details.
- Download only the two issue-frozen `$value` routes, sequentially, with exact resume/size/container limits.
- Write private run state and provider bytes only below ignored `downloads/` paths.

## Non-implications

Authorization and successful delivery would prove neither legal conclusions beyond `TERMS-2026-004` nor pixel quality, burn-scar presence, label fitness, dataset readiness, model readiness, operational value, field validation, or endorsement.

This record must receive a fulfillment update after the real run. Stop on metadata drift, failed authentication, untrusted redirects, incomplete delivery, failed checksums, unsafe archive structure, secret exposure, or any no-go boundary.

## Failed run preserved

Run `BL-2026-07-15-optical-pair-intake-r001` stopped before token issuance or provider-byte delivery with `PUBLIC_METADATA_DRIFT`. The code requested expanded attributes but omitted `Attributes` from its explicit OData `$select`, so all protected identity fields normalized as missing. The credential wrapper still removed its short-lived environment values. BurnLens did not weaken the contract or reuse the run ID; issue #343 corrects the projection and retries as `r002`.

Run `BL-2026-07-15-optical-pair-intake-r002` refreshed and accepted the exact public metadata, downloaded and fully validated the 1,126,185,170-byte pre-event archive, then received an early end to the post-event response at 352,502,902 of 1,128,620,461 bytes. It failed closed with `DOWNLOAD_SIZE_MISMATCH`, preserved the exact `.part` for range-resume, created no raw registration, and removed the short-lived credential environment. The complete pre archive and incomplete post archive remain private below ignored quarantine paths.

Run `BL-2026-07-15-optical-pair-intake-r003` refreshed the exact metadata and requested a short-lived token, then stopped before a product request because the complete pre archive temporarily had an additional OneDrive upload-staging hard link. Independent inspection returned `ASSET_MULTILINK_NOT_ALLOWED`. BurnLens preserved the guard, made no file mutation, created no raw registration, and removed the credential environment. A new run ID may resume only after independent inspection observes a single link and revalidates the complete pre archive.

Run `BL-2026-07-15-optical-pair-intake-r004` started only after the pre archive returned to one link and independently passed exact size, 95-member SAFE/root/manifest/CRC checks, provider MD5/BLAKE3, and local SHA-256 `90574a05ac4e4454964744452037a9ae215597a4e69dfab0211b88108f8f1321`. CDSE answered the retained post range request with a full response, so the bounded client safely replaced the partial rather than appending incompatible bytes. The complete 1,128,620,461-byte post archive then passed the same structure and provider checks; independent local SHA-256 is `374f34f27901156e10c92fcb890c5e9c77460698135583c6d477b7f334a3aa78`.

Between final download validation and atomic promotion, OneDrive created an upload-staging hard link for the post archive. Promotion rejected the pair and created no raw registration. The generic transaction `ValueError` was not normalized by the CLI, so this run produced a traceback and no private state JSON; no secret value or delivery URL appeared, and the wrapper removed both credential environment values. Commit `2ffe7789654d264773d9b7a955f50e7403333207` converts both ordinary promotion rejection and unexpected local transaction failures into bounded, secret-free run-state records with regression coverage. The complete unchanged quarantine remains retryable after the post link count returns to one.

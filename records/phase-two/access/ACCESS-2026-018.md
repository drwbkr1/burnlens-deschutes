# ACCESS-2026-018 - Petes Lake entry access gate

**Unit / issue:** `P2O4-T33-U01` / #521

**Checked:** 2026-07-21T15:52:47Z through 2026-07-21T15:55Z

**Access run:** `BL-2026-07-21-petes-lake-cdse-access-precheck-r003`

Public HTTPS MTBS, Burn Severity Portal, CDSE STAC, and CDSE OData metadata routes returned the exact U01 identities. A separate token-only probe loaded the existing owner-authorized machine-local DPAPI-protected CDSE credential, requested one current access token through the established provider client, printed only a boolean pass, and cleared credential environment values in `finally`.

The probe retained no username, password, token, authorization header, cookie, account detail, signed redirect, or protected-store path. It requested zero provider product/archive bytes and added no paid service or secret.

U02 may use only the two UUID-specific HTTPS OData `$value` routes bound by `SOURCE-2026-028`. Each archive is a separate fail-closed transaction into an ignored repository-local quarantine and no-overwrite raw package. Provider size, MD5, BLAKE3, local SHA-256, ZIP/SAFE root, manifest, CRC, path, link-count, atomic-promotion, and post-promotion checks remain mandatory. A failed first archive blocks the second transaction until the failure is retained and dispositioned; neither archive authorizes downstream pixel use until both exact custody gates pass.

The exact pre/post quarantine, raw-package, run-state, and aggregate paths are bound in `PETES-LAKE-ENTRY-GATE-2026-001.json`. All ignored destinations were absent, ignored, and untracked at U01 r003. Existing destinations must stop U02 without overwrite.

**Decision:** `PASS_EXISTING_AUTHENTICATED_ACCESS_FOR_EXACT_SEQUENTIAL_U02_TRANSACTIONS; ZERO_PROVIDER_PRODUCT_ARCHIVE_BYTES_REQUESTED_IN_U01`.

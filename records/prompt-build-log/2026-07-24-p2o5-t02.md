# P2O5-T02 - Six-event dataset sufficiency

Issue #552 starts from exact main `1c4df18d273bfa908737dca6831121db16af1d89`.

U01 reproduces the prior three-event insufficiency result and binds the current six-event v0.4 prototype evidence. U02 adds the deterministic BurnLens 0.50.0 evaluator. It classifies the event evidence from the exact promotion routes: Darlene uses NIFC context; McKay and Tepee use the MTBS-current regime; Green Ridge, Grandview, and Windigo use current BAER/MTBS/RAVG evidence.

U03 final run `BL-2026-07-24-six-event-dataset-sufficiency-r003` reconstructs 12 regions, 286 core pixels, 533 unknown-ring pixels, three never-tuned transfer events, and 90 prospective whole-event 2/2/2 assignments. All assignments fail source-program/regime replication because Darlene is the sole NIFC event. The project and independent audits both return `block`; training remains unauthorized.

R001 is retained as a failed source-commit binding. R002 is retained because its public JSON encoded the output directory and did not replay byte for byte. R003 fixes canonical bindings and no-overwrite behavior. Its candidate, audit, decision, JSON, HTML, and PNG outputs reproduce exactly.

The 12 focused evaluator/environment tests pass. Lock freshness, dependency health, compilation, and diff checks pass. The first full-suite run exposes three pre-existing historical-bundle test assumptions because current main's case study no longer matches the v0.48 frozen input. The regression is corrected to rebuild from the exact verified bundle roster, validate the preserved ZIP, and reject current mutable bytes. Final verification passes 595 tests, one expected skip, 58 warnings, and 86 subtests.

U04 records the block, critical August 6 schedule impact, contingency-only status, and one-event replacement path. Its first verifier attempt used an invalid PowerShell selector. Its second attempt exposed nondeterministic wheels without a fixed epoch. Final r003 uses `SOURCE_DATE_EPOCH=1784913185`; two 899,515-byte wheels repeat at SHA-256 `c3aa970694add791d17b78ee688108435bcac8921514071410dd87bf0ae84ca5`. A CPython 3.12.10 isolated install reports BurnLens 0.50.0, 13 compatible distributions, 89 commands, and a working sufficiency help route. The exact PNG renders correctly. The owner confirms the exact HTML renders correctly at desktop and narrow width. Release audit `RELEASE-AUDIT-2026-004` now verifies the candidate while preserving the scientific block.

No dataset, split, baseline, model, inference, deployment, external submission, or terminal fallback advances.

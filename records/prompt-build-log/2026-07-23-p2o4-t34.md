# P2O4-T34 - Official fallback source gate

**Checkpoint class:** milestone

**Issue / branch / base:** #532 / `codex/p2o4-t34-official-fallback-source-gate` / `ad298f244b0261a77ef0d6ff442da5a45738a7d9`

**Status:** active; U01 passed; U02 deferred; U03 deferred; U04 source checkpoint validated; production artifacts and HTML render gate pending

## Outcome

Compare the official Petes Lake McKenzie HUC8 `17090004` NWI route with exactly one frozen alternate event through fresh metadata, terms, rights, access, provenance, integrity-planning, prospective fitness, privacy, uncertainty, leakage, cost, and portfolio-value gates. Select at most one route for a separate future controlled-intake contract, or retain a reproducible defer/exclude/stop result. This checkpoint authorizes zero provider archive, raster, vector, tile, member, or pixel bytes.

## Entry evidence

The current Grandview intake tool was rerun from its exact locked response and receipt under BurnLens 0.45.0. Its real 1600 x 1180 PNG was inspected and the same ten-region/five-event dataset block remained visible. Two harness mistakes failed closed before the successful no-overwrite run; PRECHECK-2026-057 retains them and the expected current-version trace difference. Temporary validation derivatives were removed after inspection, while the locked evidence and tracked worktree remained unchanged.

Read-only entry verification found 168 ignored custody files / 16,999,841,856 bytes / two retained zero-byte failures, zero forbidden r004/U05R3 target paths, and zero active acquisition processes. P2O4-T33 remains `defer / complete / verified`; Petes is not event six; dataset, split, baseline, model, metric, GitHub Release, deployment, and deployed analytical application remain absent.

## Registered units

| Unit | Run | Status | Durable evidence | Next |
|---|---|---|---|---|
| `P2O4-T34-U01` | `BL-2026-07-23-official-fallback-entry-r001` | `pass` | `PRECHECK-2026-057`; exact two-candidate roster, request/rights boundaries, entry-state proof | U02 |
| `P2O4-T34-U02` | `BL-2026-07-23-mckenzie-huc8-metadata-r001` | `defer` | `SOURCE-2026-034`; `TERMS-2026-029`; validated blocked source-gate JSON | U03 |
| `P2O4-T34-U03` | `BL-2026-07-23-alternate-event-metadata-r001` | `defer` | `SOURCE-2026-035`; `TERMS-2026-030`; validated blocked source-gate JSON | U04 |
| `P2O4-T34-U04` | `BL-2026-07-23-official-fallback-comparison-r001` | active | `PRECHECK-2026-058`; offline deterministic report tool; production source/JSON/HTML/PNG pending | U05 |
| `P2O4-T34-U05` | `BL-2026-07-23-official-fallback-closeout-r001` | pending | `REGISTRY-2026-061`; `MANIFEST-2026-048`; decision/devlog/status handoff | milestone exit |

## Candidate decision

U01 freezes `candidate-petes-lake-mckenzie-hu8-17090004` and `candidate-milli-0843-cs-2017`. Milli is the eligible existing-reserve alternate because it has standard BAER/MTBS/RAVG identities and can reuse the established Sentinel/CDSE source regime without a new secret. Verified-scout rank-one GW Fire is not selected because its BAER identity is nonstandard, it predates Sentinel-2, and its observed Landsat asset route requires unestablished EROS authentication. Milli's known complete-boundary multi-tile risk is the first U03 decision and cannot be hidden or clipped.

## Boundaries

- Work only in `drwbkr1/burnlens-deschutes`; never read, use, change, or depend on `burnlens-site`.
- Official explanatory and bounded metadata bodies may be read only under PRECHECK-2026-057. Asset/archive routes are HEAD-only with redirects disabled.
- Do not authenticate, request a bundle, initialize or mutate custody, read prior Petes provider payloads, create r004, or acquire provider data.
- Do not create a candidate surface, owner response, label, sixth event, dataset, split, baseline, model, metric, deployment, official/field/operational claim, or public-sharing change.
- NWI remains context/exclusion evidence only. BAER, MTBS, RAVG, Sentinel, Landsat burned area, catalog cloud metadata, SCL, dNBR, and perimeters remain role-limited evidence rather than automatic truth.

## Research basis

Fresh primary-source assessment is required for every drift-prone identity, availability, terms, access, and limitation claim. Each source receives an independent eight-criterion gate record. Missing or contradictory evidence remains `unknown` or `fail`; no criterion passes from silence. Metadata readiness never becomes blanket authorization.

## U02 result

The exact McKenzie HUC8 asset remains live by no-redirect HEAD at 28,966,292 bytes with the same provider version identity observed by SOURCE-2026-033 and zero body bytes. Seven bounded official guidance/catalog pages pass their required identity, authority, access, limitation, citation, caution, and warning semantics. The live HUC table binds `17090004` to McKenzie.

U02 retains three failed attempts: one PowerShell HTTP-stack open failure with no response, one un-followed 301 from a search-index route before the canonical direct-200 page was frozen, and one bounded Data.gov CKAN API 404 after the public catalog page passed. None was retried or hidden. The first harness did not durably retain complete locator/time evidence for all three attempts; the missing fields remain an explicit trace failure and are not reconstructed. The authorized-family gate record is `OFFICIAL-FALLBACK-SOURCE-GATE-MCKENZIE-2026-001`; `provider_bytes_authorized` is `false`.

Identity, authority, metadata access, integrity planning, and metadata privacy/security pass. Rights, exact member/project provenance, and scientific fitness remain unknown. `TERMS-2026-029` therefore blocks selection, archive acquisition, retention, processing, derived publication, and raw redistribution. The gate record validates structurally as `blocked`; U02 is `defer`, not a permanent source rejection.

## U03 result

U03 began with rights and limitation evidence because issue #532 requires affirmative rights before route selection. Current semantics still distinguish the lawful Sentinel-data grant from separate CDSE portal content, preserve MTBS acknowledgement/no-warranty/revision terms, and retain analyst, scale, field-data, timing, boundary/perimeter, and program-specific limits. Exact future Milli BAER/MTBS/RAVG bundle notices remain unknown and earlier deliveries cannot license a new event.

The exploratory web reader did not expose evidence-grade request timestamps, redirects, caps, body sizes, or hashes. Two later current MTBS XML responses reproduce the 2026-07-21 bytes exactly, but their harness allowed redirects without recording whether one occurred and did not pre-enforce the cap. U03 retains all twenty operations and their trace gaps. It does not repeat them or convert current semantics into an exact request pass.

Rights and trace therefore stop the candidate before current occurrence/boundary, Portal, STAC, OData, or Shapely processing. The locked geospatial environment passes, but the legacy vertex-only coverage predicate is not reused and no new geometry code is added. Sentinel, BAER, MTBS, and RAVG are independently represented in the gate record: only current MTBS collection authority passes live; every source retains its own identity/access/rights/provenance/fitness result plus bounded integrity-plan and metadata privacy/security results. The four-source gate validates as `blocked`; `provider_bytes_authorized` remains `false`; Milli is `defer` and unselected.

## Handoff

Run U04 without new provider calls. Compare the exact McKenzie and Milli defer states against prospective role, uncertainty, leakage, custody, privacy, reproducibility, acquisition cost, Phase Two Objective Four, relevant reference outputs, and the portfolio narrative. The report must keep both candidates visible, select neither, render JSON/HTML/PNG deterministically, and state that no source asset, candidate, label, sixth event, dataset, split, baseline, or model advanced.

U04 freezes an offline-only generator rather than another source request. It validates both blocked gate records and the exact five-event Grandview reference, emits a source snapshot plus JSON/HTML/PNG, fails on any selected candidate or provider-byte authorization, and advances software to 0.46.0 because executable/report behavior changes. No dependency or network capability is added. The original-resolution preview PNG now shows all ten comparison dimensions without clipping. The in-app browser rejected the exact local `file://` preview before navigation under its URL security policy; no bypass was attempted, so desktop/narrow HTML validation remains pending. Production artifacts must be generated from the committed code state and pass actual desktop/narrow rendering before U04 can complete.

Independent review found and then cleared three pre-commit defects: all eleven frozen inputs are now byte/hash enforced, production trace must equal clean tracked `HEAD`, and narrow rows retain candidate labels. The first full-suite attempt timed out without a result; the completed rerun reached `543 passed`, `1 skipped`, and `83 subtests passed` with stale 81-versus-82 entry-point count expectations. A focused rerun found the remaining names-list length assertion; all three exact expectations are corrected. The final repository-local logged rerun passes `544 passed`, `1 skipped`, `20 warnings`, and `83 subtests passed in 1004.93s`; focused environment/U04, package, lock, compile, and diff checks also pass. The production quartet and actual desktop/narrow HTML gate remain pending after the source checkpoint.

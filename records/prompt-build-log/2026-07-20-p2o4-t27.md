# P2O4-T27 Prompt / Build Log — Grandview Native Source Fitness

**Issue / branch:** #495 / `codex/p2o4-t27-grandview-source-fitness`

**Start checkpoint:** verified `v0.38.0-green-ridge-owner-response-intake`; main lifecycle `bd6fe2591c4f5dc14c418665bc89d85f33ff48e7`

**Acquisition run:** `BL-2026-07-20-grandview-optical-intake-r001`

The cycle starts by running the current additional-event planning tool. JSON, HTML, and PNG reproduce byte for byte and retain Grandview as the highest-leverage missing event. Repository-local ignored storage contains no Grandview provider package, so the source-pixel gap is real rather than stale status.

Fresh official CDSE OData inspection verifies both frozen UUID/SAFE identities, online state, exact archive sizes, MD5/BLAKE3 values, same Sentinel-2B platform, tile 10TFQ, orbit 113, baseline 05.00, and 1,923,481,794 total bytes. Current official terms and OData documentation resolve lawful experimental use, source attribution, free existing-account access, authenticated delivery, quota respect, and no-paid-service state. `SOURCE-2026-024`, `TERMS-2026-020`, and `PRECHECK-2026-038` bind the decision.

BurnLens 0.39.0 adds one fail-closed exact Grandview pair contract and a secret-safe acquisition command. Seven focused synthetic tests prove exact pair identity, metadata/checksum drift rejection, redacted one-use credentials, working-directory allowlisting, and secret-free failure state.

After the committed contract and precheck passed, acquisition run `BL-2026-07-20-grandview-optical-intake-r001` registered both archives in ignored repository-local custody: 1,923,481,794 provider bytes, exact provider and local hashes, 109 safe members per archive, manifest present, full CRC pass, and no unexpected package entry. Pre completed on attempt one and post on attempt three through the bounded fresh-token route. Credential environment clearing and an independent secret scan pass.

Generator commit `527caeb0c83fb70bdd0af37d11a1215914ca0be9` then opens the actual native pixels. Evidence run `BL-2026-07-20-grandview-source-fitness-r001` measures 62,588 full-boundary pixels, 97.5794% pair eligibility, 647 review-needed pixels, 868 excluded pixels, nine of nine passing registration windows, 0.1158-pixel p95 residual, and 61,073 continuous-dNBR pixels. JSON, HTML, and PNG publish these optical results with source attribution and no label threshold. BAER/MTBS/RAVG pixels, all candidates, owner review, labels, dataset, split, baseline, and model remain deferred.

The candidate worktree passes 308 tests, compilation, 101 tracked JSON parses, exact 3-of-3 reconstruction, original-resolution render inspection, and privacy checks. Two independent clean-branch fixed-epoch builds produce byte-identical 564,981-byte wheels with SHA-256 `c4f3365de40e702c31a77f93a4821bf741b7e77c4766be71795509f1fa8c27de`. The wheel contains 127 entries, 61 commands, and no download/build/private-custody entry; a dependency-complete isolated install reports 0.39.0, passes `pip check`, and opens both new CLI help paths. PR #500 is the release candidate; issue #499 owns exact Grandview official reference-pixel fitness.

A fresh remote-branch clone creates only the expected empty ignored `downloads` harness boundary, passes 308 tests with 12 expected absent-custody skips, compiles, and reconstructs JSON, HTML, and PNG byte for byte through the unchanged original registered package. No checkout rule or custody verifier is relaxed. Fresh merged main and remote annotated-tag gates remain pending.

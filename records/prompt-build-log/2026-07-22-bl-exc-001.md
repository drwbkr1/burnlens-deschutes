# BL-EXC-001 Prompt / Build Log - BurnLens 0.45 Runtime Dependency Contract

**Issue / branch:** #528 / `codex/bl-exc-001-v045-runtime-contract`

**Checkpoint class / trigger:** exception / urgent correctness defect that leaves release and package truth unsafe

**Verified base:** `7d5c07e6b337b6b801b9d41565deedd9d2180b00` on live `origin/main`

**Affected milestone:** P2O4-T33 / issue #521 / PR #527; true merge `7d5c07e6b337b6b801b9d41565deedd9d2180b00`

**Candidate tag state:** `v0.45.0-petes-lake-material-defer` is withheld; no local or remote v0.45 tag or GitHub Release exists

## Exact entry finding

Fresh merged-main verification installed the 800,916-byte candidate wheel / SHA-256 `4e0bd99fec24d693b29be78c7dc943aedeaebaac3c37f1cf48442d507fa2eaa6` with only its declared runtime dependencies. The environment is dependency-consistent and exposes 81 console scripts, but loading every entry point from outside the repository passes 80 and fails one. `burnlens-inspect-petes-lake-reference-fitness` fails with `ModuleNotFoundError: No module named 'pyogrio'`; its imported implementation directly requires Pyogrio, PyProj, and Shapely, while the exercised `pyogrio.read_dataframe()` path also requires GeoPandas. All four packages are declared only in the optional `geo-research` extra.

The documented `dev` profile adds only pytest while describing itself as suitable for repository regression and presenting the full-suite command. A clean dev environment cannot collect `tests/test_petes_lake_reference_fitness.py` because Shapely is absent. The locked geo-research profile passes, but that does not make the base runtime metadata or dev-profile claim true.

This defect blocks the untagged BurnLens 0.45.0 software release. It changes no Petes source, terms, custody, raster, render, material-defer decision, candidate, owner response, prototype label, accepted-event, dataset, split, baseline, or model evidence.

## U02 route decision

U02 uses the exception's explicitly allowed narrower-package-contract exit. Promoting the complete vector stack would expand base runtime from 13 to 22 installed distributions and reverse the verified U09C lean runtime/dev plus opt-in `geo-research` design for one historical evidence command. The runtime contract is therefore the lightweight shipped command surface: every command must load, be callable, and show help; a real optional-geospatial execution must check GeoPandas, Pyogrio, PyProj, and Shapely before repository preflight and fail with a bounded profile instruction when absent or unusable. `dev` runs the core suite with an explicit geo-only skip, while `geo-research` runs the complete suite.

Pushed U02 code `c4387c932ab89ab5857299293ceb469e28f126e4` implements that boundary without changing `pyproject.toml` or `uv.lock`. Focused verification passes 21 tests with one expected conditional skip; the runtime verifier loads and probes all 81 installed launchers; compilation and diff checks pass. Independent review confirms all 33 U05 scientific function ASTs and every moved contract value are exact. Issue comment `#issuecomment-5052107570` records the controlling route amendment before U03.

## Evidence-unit ledger

| Unit | Purpose | Inputs | Required gates | Disposition | Retained failure / next dependency |
|---|---|---|---|---|---|
| `BL-EXC-001-U01` / `BL-2026-07-22-v045-runtime-dependency-contract-r001` | Preserve and classify the merged-main package/profile defect | merge `7d5c07e...`; candidate wheel `4e0bd99f...`; `pyproject.toml`; `uv.lock`; 81 entry points; environment docs | fresh base-runtime install, outside-repository entry-point load, dev collection, exact declaration/import mapping, no-tag proof, no-scientific-delta review | `remediate` | Base wheel loads 80/81 entry points; dev collection fails; register the smallest dependency/profile correction before tagging |
| `BL-EXC-001-U02` / `BL-2026-07-22-v045-runtime-contract-remediation-r001` | Correct the dependency and profile contract | exact U01 evidence; issue #528 and route amendment; registration/capsule checkpoint `882d6d3...` | lightweight runtime command; four-module fail-closed execution gate; exact 81-command load/help verification; dev/core and geo/complete profile truth; focused tests; no scientific delta | `pass` | Pushed code `c4387c9...`; 21 passed / 1 expected skip; independent 33-function AST equivalence; proceed to fresh locked U03 environments |
| `BL-EXC-001-U03` / `BL-2026-07-22-v045-runtime-contract-verification-r001` | Build and verify the corrected 0.45.0 candidate | pushed U02 code; superseded wheel identity; final code `5e55ec8...` / tree `9856fc2...` | runtime loads/probes all 81 commands and bounds geo-only execution; dev core suite with disclosed skips; geo smoke and complete custody-enabled suite; exact U03/U04 replay; two fresh deterministic wheels; metadata/RECORD/privacy/forbidden-path checks | `pass` | REGISTRY-2026-060 retains all three U03 remediation attempts and the superseded 800,916-byte wheel; proceed only to U04 review and ship gates |
| `BL-EXC-001-U04` | Ship and verify the exception | exact reviewed U03 head | one reviewed PR; fresh merged-main runtime/dev/geo/package/replay gates; annotated tag only after every gate passes; exact remote peel; lifecycle handoff | `pending` | no tag, GitHub Release, deployment, provider, scientific, label, data, or model action before pass |

## Allowed and prohibited paths

Allowed tracked changes are limited to dependency metadata and lock state, environment setup/verifier code, focused environment/package tests, the environment workflow and exact install/test wording, and the smallest P2O4-T33 U11/U11R1 release-candidate records needed to retain the failed wheel and bind the corrected one. Lifecycle/status files may change only after exact PR, merge, tag, and next-checkpoint identities exist.

No provider request, source or terms decision, custody mutation, raster computation, threshold, source-precedence change, production Petes candidate/review/response, prototype label, sixth-event promotion, dataset, split, baseline, model, deployment, access change, secret, paid service, irreversible action, or `burnlens-site` work is authorized.

## Exit and rollback

The exception may pass only when base runtime loads and probes every shipped command and bounds optional-geospatial execution, dev supports the documented core regression with exact skips, geo-research supports the complete regression, actual Petes U03/U04 outputs remain byte-identical, two clean wheels match, and fresh merged main plus the remote annotated tag verify exactly. BurnLens remains 0.45.0 because no v0.45 tag has been published.

The existing P2O4-T33 commits, PR #527, and 800,916-byte wheel remain immutable historical evidence. This exception never rewrites them or represents them as the verified release.

## U03 corrected-candidate verification

P2O4-T33 completed PR #527 from reviewed head `925b660119c230d292063be5de1064fdd007e83a` through true merge `7d5c07e6b337b6b801b9d41565deedd9d2180b00`; issue #521 is closed. Fresh merged-main package review then opened this exception before any v0.45 tag existed. U03 final code is `5e55ec8afd193d28c6c71191f0cca64c9aa31c29`, tree `9856fc2bbb68158a402b3ef98dbf6f2c29e055aa`.

U03 retains every bounded attempt. At records checkpoint `d330eb2a7949beb1610d704a3ff75897a7818a02`, two wheels are byte-identical at 801,833 bytes / SHA-256 `b3db6eecefa8166b9ab732920d3970d13a016500ee458ba5ae1e385297a595e7`, but later required gates had not passed. The first clean `dev` proof reported 25 failed / 488 passed / 24 skipped because synthetic wetland-custody tests traversed the real Shapely gate and one genuine polygon test required Shapely; the first exception-branch U04 replay rejected the branch before output; and the first cross-clone comparison exposed checkout-dependent `uv.lock` bytes. Checkpoints `cbc824c1e925bb11a4bf98b7f9422419b254b22f`, `77d32c5e2408fa1215ca02b53ef30a0cb8c69b21`, and `5e55ec8afd193d28c6c71191f0cca64c9aa31c29` correct those failures without expanding the lean runtime or changing scientific computation. Fresh `uv.lock` is exactly 207,817 bytes / SHA-256 `24071eb7b57ba57cc2728635427dc57643d7d96ac8fa58a4030052abad8dcbba`, contains zero CR bytes, and has `text: set` / `eol: lf`. REGISTRY-2026-060 also retains the invalid nested-manifest replay harness, the intentionally unavailable generic pip check, and the wrong-module package-audit import; corrected runs pass without repository, package, or scientific mutation.

Fresh locked `dev` with no custody has 18 distributions, all eight geo modules absent, and passes 499 tests with 25 expected skips, 16 warnings, and 78 subtests; its focused environment gate passes four tests with one expected geo skip and all 81 command help probes. Fresh locked `geo-research` has 66 distributions and passes 515 tests with 23 expected custody skips, 16 warnings, and 78 subtests. The canonical custody-enabled suite passes 537 tests with one expected skip, 20 retained warnings, and 78 subtests.

Two separate fresh no-hardlink clones at pushed evidence checkpoint `184bfc9b92c4d748a7f45f24ffc6d4825eacb616` are byte-identical at 801,911 bytes / SHA-256 `055e56b1f89be8e675960711cdfc80c59983451ee1cfe995d28ddc64d0f266f1`. The wheel contains 170 exact entries: 164 package files and six dist-info files; it exposes 81 unchanged command mappings, exact 170-row RECORD coverage, correct 0.45.0 metadata, and zero forbidden paths. Earlier builds at `77d32c5...` and `5e55ec8...` retain the same identity. Source and installed-wheel replay preserve planned U03 JSON/PNG at 56,285 / `ac9befd021d71ff779ec56ddefc894f4e39b92e64e65c2c4afb815663d1ad443` and 626,846 / `4ed3870e37bf68db24805540f00614c5050c064b621ca3fc5e3c0ef244bf0d42`, replacement U03 JSON/PNG at 60,069 / `1aa88c0021c610e492d2645e3f2c49a4afe96d9d907e2ee4481948a4c58f2ebd` and 588,891 / `fd5b9ae54e1b9c3e0d495e337387d874ae911bd0f586e835b4184312d486d931`, and U04 JSON at 32,991 / `b489bd30b467ab38f7320c9b313f904e0bbe9a33e2bed8b346230b9f48a6053c` with zero accepted pixels. Clean remote-equal `184bfc9...` also reruns the actual U04 command into ignored no-overwrite storage and reproduces that exact identity with zero accepted pixels.

REGISTRY-2026-060 is the exact U03 evidence record. U04 remains pending. The candidate tag is still withheld, and lifecycle/status/roadmap/case-study/manifest changes remain prohibited until the exception PR, merge, fresh-main, and tag identities exist.

# P6O1-T01-U05 deterministic pre-publication package

**Issue:** #578

**Branch:** `codex/p6o1-t01-baseline-first-publication-candidate`

**Exact base:** `6c3e3728f94389ac245306dc84c36c61d68afa13`

**Generator source:** `488bea766dc4e5ab0f03a34f4905213b295cac2b`

**Run:** `BL-2026-07-27-p6o1-t01-u05-pre-publication-package-r001`

## Prompt intent

Assemble only permitted tracked assets into one deterministic, no-overwrite,
safe-extraction package. Preserve the exact U03/U04 bytes, full Phase Five
directory, known issues, route, claims, attribution, and every public-action
boundary.

## Result

`burnlens-phase-six-baseline-first-candidate-v0.1.0` contains 117 files /
16,285,070 bytes. It copies 112 exact tracked source files and adds five
package controls. The ZIP is 14,963,460 bytes at SHA-256
`def1b20fd4709bc813ec286ea46b5e063f4b95576522471a4c448a69779f92c8`.

The root `OPEN-FIRST.html` routes to the exact Phase Six reviewer surface and
presentation. The package preserves repository-relative paths, resolves 165
local links, contains the complete recursively discovered review/case-study
closure, and matches every byte of the exact Phase Five directory to its
immutable archive.

## Verification

Directory and archive validators pass. Two independent in-memory builds equal
each other and the tracked ZIP. Pinned hashes, tracked-source enforcement,
fixed timestamps, regular-file modes, duplicate/encrypted/traversal/symlink/CRC
guards, no-overwrite behavior, manifest/checksum bindings, and private/custody/
model-weight exclusions pass. Six focused tests and sixteen combined Phase
Five/Phase Six tests pass; compile and diff checks pass.

## Retained failure and disposition

The first full suite retains 766 passes, 34 failures, one expected skip, 422
warnings, and 86 passing subtests. R001's package bytes are structurally valid,
but its declared generator commit still contains the 0.55.0 project entry in
`uv.lock`. A fresh `uv sync --locked` fails closed. Thirty historical
current-version assertions, three installed-environment profile checks, and
the expected dirty-worktree checkout contract account for the 34 failures.

R001 is `remediate` and must not advance to U06. Its exact directory, ZIP,
receipt, hashes, and record remain immutable. The next action is a no-overwrite
v0.1.1 / r002 build from a clean commit containing the 0.56.0 lock entry and
updated current-version tests. No public action occurred.

## Corrected r002

Source `575fcb542e356dd77c33daf74a285469ea83dbc4` passes 800 tests,
one expected skip, 422 existing warnings, 86 subtests, `uv lock --check`, and
dependency integrity across 75 installed distributions. It also batches the
historical checkout-attribute probe to remain below the Windows command-line
limit.

Run `BL-2026-07-27-p6o1-t01-u05-pre-publication-package-r002` creates
`burnlens-phase-six-baseline-first-candidate-v0.1.1`: 117 files /
16,285,070 bytes. The deterministic ZIP is 14,963,469 bytes at SHA-256
`5a314b69e6efb64b6058d21cd33b74bef4d14c07f2bd0457eb8eea43c935ab2e`.
Both installed-CLI validators and two independent byte rebuilds pass. R001
remains untouched.

U05 now passes for local pre-publication use. U06 owns real recipient
extraction and rendered QA. No public action occurred.

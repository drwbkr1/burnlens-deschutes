# P4O1-T01 — RBR-primary CV-to-GEOINT milestone

Issue #570 controls one Phase Four milestone on branch
`codex/p4o1-t01-rbr-geoint-milestone`. PR #569 merged and passed fresh-main
lifecycle verification at exact base
`e8745b70c4cfe0d070e08e399efbab09e74cd06f`.

The owner directs BurnLens to complete Phase Four through
`baseline-primary-with-rejected-model-diagnostic`. RBR remains the accepted
analytical method. The U-Net remains a valid trained, evaluated, reproducible,
and rejected model diagnostic; it predicts all 89 selected test cores burned
and scores macro Dice `0.29874213836477986` against RBR `1.0`. Never claim the
model outperformed RBR.

Phase Four cannot be deferred and no Phase 3B milestone may be created.
Lessons may appear only as recommendations for a separately governed
follow-on experiment. The current project does not plan, scope, authorize, or
implement that experiment.

## U01 — architecture and run contract

Entry first verifies eleven frozen control artifacts: modeling AOI, dataset
contract and manifest, split, normalization, baseline evaluation, model
weights, model manifest, inference contract, Phase Three decision, and model
package record. All exact bytes and SHA-256 hashes pass.

The first package-inventory reconstruction is retained as a tool failure. It
uses case-sensitive order and produces `27ccfbc...`, while BurnLens's
established Windows canonical inventory uses case-insensitive order. The
corrected check changes no source or data and reproduces all eight package
members / 583,992 bytes plus the exact 1,185-byte inventory at SHA-256
`ce39c41c...`.

The exact Ward Creek roster also passes before arrays are opened:

- `WCP-001`, burned, 14 core pixels, 26 unknown-ring pixels, all 4,096 input
  pixels valid, EPSG:32610 transform
  `[20, 0, 667220, 0, -20, 4979800]`;
- `WCP-002`, background, 25 core pixels, 40 unknown-ring pixels, all 4,096
  input pixels valid, EPSG:32610 transform
  `[20, 0, 670520, 0, -20, 4981800]`.

Contract `PHASE-FOUR-INTEGRATION-CONTRACT-2026-001` freezes every input,
component owner, run state, output artifact, manifest field, failure behavior,
and no-go boundary. It distinguishes the historical modeling AOI, the exact
Ward Creek integration geography, the source five-state schema, and the
current dataset label schema rather than collapsing them into one version.

The fail-closed loader hashes files without loading arrays. U01 creates no
inference, raster, vector, context, summary, application, package, deployment,
provider transaction, or public claim. U02 may begin only after this contract
passes its focused tests, parsing, compilation, diff, clean-checkout, and
recoverable-push gates.

After first commit `09eaf6b...`, the checkout-attribute inspection finds the
new Phase Four JSON and Markdown paths have LF in the index and current
worktree but no explicit future checkout rule. U01 retains that portability
finding and adds exact Phase Four text/binary attributes before detached
verification. No contract, input, data, model, or output semantic changes.

The next detached-checkout preflight finds the contract-declared
`runs/phase-four/` root is not ignored. It creates no directory or run byte.
U01 adds the explicit ignore rule before any Phase Four attempt, replay, or
intermediate state is allowed, while accepted artifacts remain separately
promotable under tracked `samples/runs/phase-four/`.

Final U01 checkpoint `f43907f0fc770351514410621e8bc07cd6a18195`
repeats in a fresh ignored detached checkout. The three Phase Four evidence
families check out LF; the exact contract remains 19,991 bytes at SHA-256
`a50966b3...`; a fresh locked 18-distribution dev environment passes all four
focused tests, loader validation, compilation, lock freshness, dependency
health, diff, and clean-status gates. The temporary worktree is removed after
verification. U01 passes and U02 is the next eligible unit.

## U02 — deterministic analytical runner

Source `cf67113dbae0e05dd034b0cca5d2a98f2a4bfb9f` adds one
fail-closed runner and CLI. It uses the exact RBR formula and frozen threshold,
loads the frozen U-Net weights and train-only normalization, reads only the
exact Ward Creek feature and input-valid arrays for the fixed integration
display, and exposes no retraining, retuning, selection, or metric-decision
path.

Run `BL-2026-07-26-p4o1-t01-u02-analysis-r001` completes as
`accepted-baseline`. Its 14 ignored files total 104,703 bytes; canonical
inventory SHA-256 is `28739c6d...`; manifest SHA-256 is `c969bb10...`.
WCP-001 has 3,536 RBR-positive pixels and 4,095 rejected-U-Net diagnostic
positives. WCP-002 has 1,669 RBR positives and 3,206 rejected-U-Net diagnostic
positives. These full-patch counts are descriptive run facts, not label
metrics, complete-scar estimates, natural prevalence, or generalization.

An independent formula replay exactly matches both RBR arrays. Both U-Net
probability and binary arrays exactly match the frozen Phase Three Ward Creek
prediction evidence, and exclusions equal inverse input-valid masks. Eight
combined focused tests pass. A deliberate duplicate invocation exits 1 with
`run already exists` and changes none of the 14 existing file hashes.

U02 passes without context use, geospatial product acceptance, provider access,
model superiority, Phase 3B, or second-experiment work. U03 is the next
dependency.

## U03 - deterministic geospatial products

Source `761ef33abd70de8b348b22870a5b9c30a3406c28` produces ten
native-grid GeoTIFFs: accepted RBR score and binary rasters, exclusion rasters,
and visibly rejected U-Net probability and binary diagnostics for both Ward
Creek patches. Every raster retains EPSG:32610, the exact 20 m transform,
64-by-64 shape, declared dtype, nodata, domain, and analytical-status tags.

Raw four-connected polygonization of the accepted RBR masks produces 202 valid
MultiPolygon features without simplification. The GeoPackage opens in the
locked GeoPandas/Pyogrio stack. Rasterizing its polygons back onto each exact
native grid reproduces all 3,536 WCP-001 and 1,669 WCP-002 accepted RBR
pixels. The EPSG:4326 GeoJSON is a web representation, not a replacement for
the authoritative native-grid product.

The first immutable attempt is retained because the Windows fallback rendered
text too small. The second is retained because the sized fallback font lacked
the em-dash glyph. Accepted run
`BL-2026-07-26-p4o1-t01-u03-geospatial-r003` uses a portable separator and its
1280-by-720 quicklook passes direct visual inspection. Its 18 ignored files
total 549,068 bytes; canonical inventory SHA-256 is `79124d13...`; manifest
SHA-256 is `dc751576...`. Thirteen combined focused tests pass, and a
deliberate duplicate invocation changes none of the accepted files.

The quicklook states that the U-Net failed its frozen evaluation and is not the
accepted perimeter. U03 makes no model-superiority, official, operational,
field-validation, or emergency-use claim. It adds no context source, Phase 3B,
second experiment, dataset, split, label, threshold, provider transaction, or
deployment. U04 is the next dependency.

## U04 - source gate and bounded context custody

Fresh primary-source review selects three current USGS The National Map
services and the exact already-governed Ward Creek MTBS boundary. The source
gate separately assesses NTD roads, selected NSD public facilities, the NBD
BLM boundary, and the exact local MTBS product. All 32 required criteria pass
with 35 evidence items, including 23 fresh live observations. TNM service data
are public domain with requested credit; exact MTBS embedded notices permit
acknowledged use while preserving update, position, utility, and no-warranty
cautions.

The selected acquisition is eight credential-free HTTPS GeoJSON queries in a
frozen 10 km envelope, not three bulk state packages. Run
`BL-2026-07-26-p4o1-t01-u04-context-r001` promotes 890,072 bytes under atomic
no-replace custody: 169 road features, three cemeteries, two post offices, one
fire/EMS station, two trailheads, and one BLM polygon. Every response has its
exact byte count and SHA-256, expected count, allowed minimal field roster,
finite EPSG:4326 coordinate domain, valid geometry, and single-link final
file. The generic controlled-intake validator passes the exact 17,290-byte
final contract with file verification.

Independent local reuse validation reopens the exact 4,385,952-byte Ward Creek
MTBS archive at SHA-256 `d94dfb16...`, passes full CRC and all four boundary
member hashes, and confirms one valid EPSG:32610 polygon for event
`OR4494912090120190812`, map `10016337`, with area 8,378,927.386 square
meters. No new MTBS request or provider download occurs.

Roads are not routing, closure, access, or safety authority. Facility points
do not establish current operation, availability, capacity, or emergency
suitability. The BLM layer is generalized planning context, not a legal land
determination. MTBS is analyst-interpreted official-program reference context,
not ground truth, field validation, an operational incident perimeter, or
endorsement. U04 adds no model input, tuning, selection, label, metric,
Phase 3B, second experiment, deployment, or public surface. U05 is eligible.

## U05 - bounded overlays and deterministic observations

Source `2f96cc6387086e1391ce6f0e4bf5a18741456b4a` adds a
fail-closed overlay runner and CLI. It hashes the exact U03 and U04 records,
revalidates the finalized context intake, opens the exact accepted RBR
GeoPackage and governed Ward Creek MTBS archive, and measures only in native
EPSG:32610. The rejected U-Net is excluded from every U05 calculation.

Run `BL-2026-07-26-p4o1-t01-u05-overlay-r001` creates four bounded
EPSG:4326 context layers, five deterministic observations, a manifest,
summary, status, and 1600-by-1000 quicklook. Its ten ignored files total
774,062 bytes; canonical inventory SHA-256 is `770f580e...`; manifest
SHA-256 is `cf235bee...`. All GeoJSON geometries open as nonempty and valid
in the locked GeoPandas stack, and direct visual inspection passes.

The burned prototype patch, WCP-001, contains 141.44 ha of accepted RBR.
Of that footprint, 133.22 ha or 94.19% overlaps the exact
analyst-interpreted MTBS boundary. This is bounded spatial agreement, not
field validation, complete-scar accuracy, or generalization.

The owner-approved background patch, WCP-002, contains 66.76 ha of accepted
RBR, covering 40.75% of the full patch and overlapping MTBS by zero hectares.
The map presents this as first-class baseline false-positive-risk evidence.
No selected TNM road, selected facility point, or BLM boundary intersects
either accepted RBR footprint. Those selected layers remain non-operational
context and do not support access, routing, closure, safety, facility,
property, legal, tactical, or emergency decisions.

Independent validation recomputes both accepted RBR areas from the exact U03
GeoPackage and both MTBS overlap percentages from the exact governed archive.
Twenty-one focused tests, compilation, lock, dependency, command-help,
receipt, coordinate-domain, geometry, deterministic-render, and claim gates
pass. A duplicate invocation exits 1 with `run already exists` and leaves the
accepted manifest hash unchanged.

U05 passes without accepting the U-Net, claiming model superiority, creating
Phase 3B, planning a second experiment, changing a label, dataset, split,
AOI, threshold, or training result, or creating a deployment. U06 is the next
eligible dependency: one repository-owned evidence interface that must expose
the accepted RBR, rejected-model diagnostic, official context roles, exact
lineage, uncertainty, and visible failure states.

## U06 - repository-owned evidence interface

Source `f9f9cc665d9972cf66df3ebc1d0b5f9458f14d32` builds one
self-contained local/offline HTML interface from twelve exact inputs. The
embedded SVG presents accepted RBR, rejected U-Net diagnostic, MTBS, selected
roads, selected facilities, and generalized BLM context as six independent
layers with six opacity controls. Three focus controls, a full textual
equivalent, exact version/run lineage, source-role cards, warning panel, and
accepted/degraded/no-detection/fallback/failed/withheld taxonomy are present.
No external script, stylesheet, image, font, tile, fetch, upload, or network
dependency exists.

The first production candidate,
`BL-2026-07-26-p4o1-t01-u06-interface-r001`, is retained as failed render QA.
Desktop patch focus scales its redundant SVG label until the label obscures
WCP-002. Exact HTML, JSON, and screenshot bytes remain in ignored evidence.
Source `5072a57bc75f63ebb9b66dd5a2173b6b290473d8` applies the bounded
correction: patch labels remain visible in the full-context view and hide only
while the already-labeled focus button identifies the selected patch.

Accepted run `BL-2026-07-26-p4o1-t01-u06-interface-r002` tracks a
177,666-byte HTML file at SHA-256 `7a657ad7...` and a 12,429-byte JSON
manifest at `8686766d...`. The rejected U-Net is off by default. Enabling it
adds magenta hatching while retaining the orange accepted RBR beneath it and
does not change any analytical role or measurement.

Direct file and loopback navigation are retained in-app browser transport
blocks, not application failures. Final local QA uses installed system Chrome
through the repository's browser-automation runtime and one temporary
loopback document request. Desktop 1440-by-1000 and narrow 390-by-844
interactions pass with zero horizontal overflow, console error, page error, or
external request. Native keyboard controls, visible focus, opacity, patch
focus, textual equivalence, patterns and labels, and representative text
contrast pass. The server port and browser process are stopped after QA.

Twenty-six focused Phase Four tests pass. An independent rebuild is
byte-identical, and a duplicate production invocation refuses to overwrite
the accepted outputs. The interface preserves WCP-002's 66.76 ha RBR
false-positive-risk evidence, the U-Net's rejected status, official-source
precedence, and every non-operational warning. It makes no Phase 3B, second
experiment, model-superiority, field-validation, official, operational,
endorsed, or emergency claim. U07 is the next dependency.

## U07 - immutable run package and clean reproduction

Source `733a3c265be2c351a50bfd356d1b45da15cbfec0` adds one fail-closed
deterministic package builder and validator. Run
`BL-2026-07-26-p4o1-t01-u07-package-r001` binds the exact U01-U06 records,
U02 arrays and logs, native U03 rasters and vectors, U04 source/custody
receipts, U05 context and observations, and the U06 self-contained interface
into one repository-owned run package.

The tracked extracted package contains 66 files and 1,795,388 bytes. Its
manifest is `b15f5d00...`, its checksum file is `eb08e659...`, and its exact
487,893-byte ZIP is `91308a2f...`. The receipt is `e2c80230...`. README,
report, warnings, replay, map configuration, status, source inventory,
checksums, and the full evidence roster travel together. RBR remains the
only accepted analytical method. The U-Net remains a rejected diagnostic,
never contributes a measurement, and is not described as outperforming RBR.

Candidate commit `3857d5c...` first exposes that the new binary/text families
need explicit checkout attributes. Commit
`25926b2f3602ec8dd8cdcdafcb2948a406cc8dae` adds the bounded LF and binary
rules without changing package bytes. A fresh detached checkout at that exact
commit creates its environment from the locked dev/model/geo profiles, passes
both tracked package validators, reproduces the ZIP byte-identically, passes
four contract tests, parses 33 JSON files, compiles, and remains clean.

The intentionally broad fresh-checkout U03-U06 builder suite retains
`11 passed, 1 failed, 18 errors`: those legacy builders require ignored U02
through U05 source, provider, and run custody that a fresh clone deliberately
does not contain. This is retained as a custody-prerequisite test-scope result,
not rewritten as success and not treated as an application or package defect.
The correct portable gates operate entirely on the tracked package and pass.

The packaged interface itself passes real 1440-by-1000 and 390-by-844 Chrome
render and interaction QA with zero horizontal overflow, console error, page
error, or external request. Keyboard patch focus, visibility, opacity, and
focused-label behavior pass. The owner also confirms the current rendered
gate. The temporary loopback server, browser, screenshots, and detached
worktree are removed after verification.

A duplicate production invocation exits 1 with `refusing to overwrite
nonempty extracted directory`; the archive, receipt, manifest, and checksum
hashes remain unchanged. U07 passes without a Phase 3B, second experiment,
new label, dataset, split, AOI, threshold, training run, deployment,
public-sharing change, or external submission. U08 is the next eligible
dependency: one milestone release and portfolio handoff.

## U08 - coherent milestone release and Phase Five handoff

The candidate versions BurnLens 0.54.0 and updates the roadmap, phase status,
case study, portfolio quickstart, changelog, version history, prompt/build log,
registry, and devlog around the exact U07 result. The current reviewer path
starts at the self-contained Phase Four interface and exact run package.
Historical reviewer pages and the interim August 6 technical-case-study ZIP
remain immutable rather than being rewritten.

Changed-risk validation passes 41 focused tests, both package validators,
compilation, lock, and dependency health. The first wheel attempt is retained
as failed determinism evidence: two 1,166,315-byte wheels differ because the
wrapper omitted the required fixed epoch. Corrected r002 uses
`SOURCE_DATE_EPOCH=1785094504` and `PYTHONHASHSEED=0`; both 1,166,315-byte
wheels are byte-identical at SHA-256 `ad3ae7c8...`. An isolated CPython
3.12.10 install reports BurnLens 0.54.0, imports Rasterio, GeoPandas, and Torch
2.13.0+cpu, passes dependency health, and all 114 installed command helps.

The first full regression is retained as a pre-commit state failure:
755 passed, four failed, one skipped, 252 warnings, and 86 subtests. Three
environment-profile failures correctly report that the editable installed
distribution still says 0.53.0 while source and lock say 0.54.0. The
checkout-contract failure correctly lists the 48 uncommitted release files.
No application or scientific defect is inferred. The exact next gate is to
sync the locked editable environment, commit the candidate, and rerun the
complete suite at a clean committed head.

The candidate makes no model-superiority, generalization, independent-truth,
field-validation, official, operational, endorsed, emergency, deployment, or
final-submission-ready claim. It creates no Phase 3B or second experiment.
Merge, fresh-main verification, and annotated tag remain pending.

# P4O1-T01 — RBR-primary CV-to-GEOINT milestone

Issue #570 controls one Phase Four milestone on branch
`codex/p4o1-t01-rbr-geoint-milestone`. PR #569 merged and passed fresh-main
lifecycle verification at exact base
`e8745b70c4cfe0d070e08e399efbab09e74cd06f`.

The owner directs BurnLens to complete Phase Four through
`baseline-primary-with-rejected-model-diagnostic`. RBR remains the accepted
analytical method. The U-Net remains a valid trained, evaluated, reproducible,
and rejected model diagnostic; it predicts all 89 selected test cores burned
and scores macro Dice `0.29874213836477986` against RBR `1.0`. Never claim the
model outperformed RBR.

Phase Four cannot be deferred and no Phase 3B milestone may be created.
Lessons may appear only as recommendations for a separately governed
follow-on experiment. The current project does not plan, scope, authorize, or
implement that experiment.

## U01 — architecture and run contract

Entry first verifies eleven frozen control artifacts: modeling AOI, dataset
contract and manifest, split, normalization, baseline evaluation, model
weights, model manifest, inference contract, Phase Three decision, and model
package record. All exact bytes and SHA-256 hashes pass.

The first package-inventory reconstruction is retained as a tool failure. It
uses case-sensitive order and produces `27ccfbc...`, while BurnLens's
established Windows canonical inventory uses case-insensitive order. The
corrected check changes no source or data and reproduces all eight package
members / 583,992 bytes plus the exact 1,185-byte inventory at SHA-256
`ce39c41c...`.

The exact Ward Creek roster also passes before arrays are opened:

- `WCP-001`, burned, 14 core pixels, 26 unknown-ring pixels, all 4,096 input
  pixels valid, EPSG:32610 transform
  `[20, 0, 667220, 0, -20, 4979800]`;
- `WCP-002`, background, 25 core pixels, 40 unknown-ring pixels, all 4,096
  input pixels valid, EPSG:32610 transform
  `[20, 0, 670520, 0, -20, 4981800]`.

Contract `PHASE-FOUR-INTEGRATION-CONTRACT-2026-001` freezes every input,
component owner, run state, output artifact, manifest field, failure behavior,
and no-go boundary. It distinguishes the historical modeling AOI, the exact
Ward Creek integration geography, the source five-state schema, and the
current dataset label schema rather than collapsing them into one version.

The fail-closed loader hashes files without loading arrays. U01 creates no
inference, raster, vector, context, summary, application, package, deployment,
provider transaction, or public claim. U02 may begin only after this contract
passes its focused tests, parsing, compilation, diff, clean-checkout, and
recoverable-push gates.

After first commit `09eaf6b...`, the checkout-attribute inspection finds the
new Phase Four JSON and Markdown paths have LF in the index and current
worktree but no explicit future checkout rule. U01 retains that portability
finding and adds exact Phase Four text/binary attributes before detached
verification. No contract, input, data, model, or output semantic changes.

The next detached-checkout preflight finds the contract-declared
`runs/phase-four/` root is not ignored. It creates no directory or run byte.
U01 adds the explicit ignore rule before any Phase Four attempt, replay, or
intermediate state is allowed, while accepted artifacts remain separately
promotable under tracked `samples/runs/phase-four/`.

Final U01 checkpoint `f43907f0fc770351514410621e8bc07cd6a18195`
repeats in a fresh ignored detached checkout. The three Phase Four evidence
families check out LF; the exact contract remains 19,991 bytes at SHA-256
`a50966b3...`; a fresh locked 18-distribution dev environment passes all four
focused tests, loader validation, compilation, lock freshness, dependency
health, diff, and clean-status gates. The temporary worktree is removed after
verification. U01 passes and U02 is the next eligible unit.

## U02 — deterministic analytical runner

Source `cf67113dbae0e05dd034b0cca5d2a98f2a4bfb9f` adds one
fail-closed runner and CLI. It uses the exact RBR formula and frozen threshold,
loads the frozen U-Net weights and train-only normalization, reads only the
exact Ward Creek feature and input-valid arrays for the fixed integration
display, and exposes no retraining, retuning, selection, or metric-decision
path.

Run `BL-2026-07-26-p4o1-t01-u02-analysis-r001` completes as
`accepted-baseline`. Its 14 ignored files total 104,703 bytes; canonical
inventory SHA-256 is `28739c6d...`; manifest SHA-256 is `c969bb10...`.
WCP-001 has 3,536 RBR-positive pixels and 4,095 rejected-U-Net diagnostic
positives. WCP-002 has 1,669 RBR positives and 3,206 rejected-U-Net diagnostic
positives. These full-patch counts are descriptive run facts, not label
metrics, complete-scar estimates, natural prevalence, or generalization.

An independent formula replay exactly matches both RBR arrays. Both U-Net
probability and binary arrays exactly match the frozen Phase Three Ward Creek
prediction evidence, and exclusions equal inverse input-valid masks. Eight
combined focused tests pass. A deliberate duplicate invocation exits 1 with
`run already exists` and changes none of the 14 existing file hashes.

U02 passes without context use, geospatial product acceptance, provider access,
model superiority, Phase 3B, or second-experiment work. U03 is the next
dependency.

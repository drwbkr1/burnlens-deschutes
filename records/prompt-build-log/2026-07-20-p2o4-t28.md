# P2O4-T28 Prompt / Build Log - Grandview Official Reference Fitness

**Issue / branch:** #499 / `codex/p2o4-t28-grandview-reference-fitness`

**Start checkpoint:** verified `v0.39.0-grandview-source-fitness`; lifecycle main `f1f1326dff848825d6f3bbc38841f410c6d00d73`

The cycle reruns the exact v0.39 report and reproduces JSON, HTML, and PNG byte for byte. It confirms that zero Grandview official reference pixels—not optical quality or registration—is the highest-leverage weakness.

Fresh official research captures exactly three standard WFS rows for event `OR4446612140020210711`: BAER `10019092`, RAVG `10019464`, and MTBS `10023989`. The 1,942-byte response has SHA-256 `4cec9365b0564a67d120c29db0419e33f84f8ef8135dd51440609a4f53d564c2`. The exact RAVG warning says forest-calibrated modeled values are less reliable across the event's sparse/non-tree cover. Current portal delivery, reprocessing, projection, program, and no-cost request terms are recorded before submission.

The new request contract freezes every identity, threshold, timing, model, comment, all 18 product families, and native UTM. Three synthetic tests prove drift rejection, recipient withholding, exact POST binding, atomic preservation, and no overwrite. A successful live capture was followed by one transient WFS timeout; the contract therefore requires another fresh successful normalization immediately before the single queue POST.

That request-time normalization passed. Run `BL-2026-07-20-grandview-reference-request-r001` submitted one POST and preserved the accepted 16-byte response plus exact metadata and recipient-withheld receipt only in ignored no-overwrite custody. The connected Gmail and repository-local identities matched without publishing the address; the request environment was cleared. One read-only mailbox search found no immediate delivery. No automatic retry, duplicate request, provider archive, reference pixel, candidate, label, dataset, split, baseline, or model exists.

The exact completion email later arrived from the official USGS sender and named all three expected map IDs. BurnLens kept the recipient and retrieval URL private, made no mailbox mutation, verified the exact HTTPS provider host, and preserved one 22,076,790-byte ZIP / SHA-256 `ea30fe613931d9b90c1405a653ac01fe6bfaa7ee80f3ed02480422271ff874bf` under ignored no-overwrite custody. Safe delivery preflight passed 53 members and all event/program identities.

Exact delivered FGDC/ISO terms resolve MTBS/RAVG use with acknowledgement and BAER unthresholded preliminary use with acknowledgement; restricted BARC products are absent. Twenty native 30 m EPSG:32610 rasters and three boundary packages pass deterministic inspection. On the verified 62,588-pixel optical boundary, MTBS covers 61,795 pixels, classes 2-4 occupy 58,438, and 56,943 also have valid optical comparison. The exact RAVG sparse/non-tree warning is enforced by reducing every RAVG raster to model-limited context rather than independent affirmative evidence. Background truth, candidates, owner decisions, labels, dataset, split, baseline, and model remain unchanged.

Generator source `9f28f822c114788a1a9bb09a52fab22616be0376` creates exact JSON/HTML/PNG run `BL-2026-07-20-grandview-reference-fitness-r001`. The original-resolution render is legible, visibly distinguishes RAVG as limited, and shows zero labels/background truth. Exact 3-of-3 reconstruction, 316 custody-worktree tests, compilation, and privacy scans pass. Fresh remote head `6c62ca4285178f741ef0cc55f640d60fcc5db295` passes 303 tests with 13 expected custody skips, compilation, 104 JSON parses, and reconstruction through unchanged original custody. A dirty-worktree wheel is rejected after fresh-head line-ending comparison; two fixed-epoch fresh-head wheels are byte-identical at 582,467 bytes / SHA-256 `b5e50359784fa748477cd9ff1c21e66083f0adce3682aea5e81c6163b930e269`. The 133 clean entries, 64 commands, exact pinned dependency versions, both relevant help paths, and `pip check` pass. Issue #503 owns the next bounded affirmative-background evidence checkpoint.

# P2O4-T18 Prompt / Build Log - Six-Candidate Owner Region Review Surface

**Date:** 2026-07-18

**Issue / branch:** #457 / `codex/p2o4-t18-region-review-surface`

## Result

BurnLens `0.30.0` adds `burnlens-build-region-owner-review-surface` and binds the exact v0.29 pilot report plus all six candidate raster bytes before building an offline owner yes/no/uncertain surface. It does not regenerate or modify any region. The cards show actual optical, dNBR, official-reference, core, and unknown-ring evidence and expose the exact raster hash.

Authoritative run `BL-2026-07-18-region-owner-review-surface-r002` publishes ten response-free files. A frozen-metadata reconstruction matches all ten exactly. Real desktop and 390 by 844 browser inspection passes six-of-six local-image loading, no overflow, no external resources or console warnings/errors, semantic native controls/status, incomplete-state blocking, the complete six-decision path, attestation, and hashed draft generation. The browser harness cannot upload a native file, so no software response is retained; deterministic JavaScript syntax and exact template/binding tests cover the load contract.

The surface remains at zero owner region responses and zero region labels. Yes is necessary but insufficient, no and uncertain remain excluded, and a later exact response-intake checkpoint must rerun reproducibility, source, quality, terms, uncertainty, and event-level leakage gates. Dataset, split, baseline, and model versions remain absent.

The release candidate passes 246 tests, compilation, dependency health, and 82 JSON parses. Two fixed-epoch builds produce identical 450,783-byte wheels with SHA-256 `69175042db3c6a9c1233d3d9ca503c03bf06716cf84a4f528f86cb9fd0d0b9a1`; isolated inspection reports BurnLens 0.30.0, 44 commands including the new builder, 96 clean entries, and a passing dependency check.

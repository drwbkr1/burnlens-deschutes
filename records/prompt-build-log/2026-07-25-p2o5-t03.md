# P2O5-T03 dataset, split, baselines, and model-readiness milestone

Issue #562 starts from exact verified main
`1debea556a5fb2fc8eda87a234f106655cdc37db` on
`codex/p2o5-t03-dataset-split-baselines`.

## U01 - exact contract

Implementation source `0a1d5338a599b3cfa0a9932f12e6dbbc74b1dcd6`
reopens the v0.51 replacement candidate and every tracked source, terms,
proposal, owner-intake, and raster binding.

Run `BL-2026-07-25-p2o5-t03-u01-dataset-contract-r002` freezes a common
six-channel native 20 m Sentinel input, explicit core/unknown/unreviewed
semantics, one fixed patch per candidate, train-only normalization,
split-before-patch grouping, and sealed-test evaluation.

The independent readiness decision passes all required evidence and counts
while preserving `training_authorized=false`. Exact contract, audit, and
decision bytes replay. PRECHECK-2026-090 and REGISTRY-2026-084 retain the
complete evidence and the superseded precommit exercise.

U01 creates no dataset or split. U02 must score all 54 valid whole-event
assignments under a predeclared deterministic ranking and lock one assignment
before U03 may materialize patches.

## U02 - deterministic split lock

Implementation source `c719bf79b4c01cd4016c33649c17f26f9d4ada97`
enumerates all 90 complete 2/2/2 assignments and independently reproduces 54
valid choices. The frozen ranking first balances source regime and transfer
status across every role, then training evidence, held-out balance, test
recency, role year span, and canonical IDs.

Run `BL-2026-07-25-p2o5-t03-u02-whole-event-split-r001` locks
`burnlens-whole-event-split-v0.1.0`:

- train: Green Ridge and Tepee, 109 core pixels;
- validation: Grandview and McKay, 89 core pixels;
- sealed test: Ward Creek and Windigo, 89 core pixels.

Every role contains one event per exact source regime and one preserved
never-tuned-transfer event. The ranking and split replay exactly. No imagery
pixel was read, the test open count remains zero, and training remains
unauthorized.

## U03 - native-grid dataset materialization

Implementation source `59610b232107b0027d949e907b66a56a22722ad2`
rehashes all twelve exact registered Sentinel archives and stages the derived
dataset before no-overwrite promotion.

Run r001 fails before promotion because the implementation treats unsampled
Grandview grid areas as invalid despite the event-level all-pass registration
decision. Remediation `3e383ec413dad5ece1245951660197051a401e1c`
accepts an event grid only when every sampled registration window passes and
the machine decision is `PASS_*`; any explicit review/exclusion window remains
spatially binding.

Run `BL-2026-07-25-p2o5-t03-u03-dataset-r002` creates
`burnlens-dataset-v0.1.0`: 49 files, 1,388,556 bytes, twelve native 64 by 64
patches, 287 core pixels, and 531 explicit unknown-ring pixels. Manifest
SHA-256 is `e0b7ac66...`; canonical tree SHA-256 is `9fb4f839...`.

A second full archive rehash and materialization reproduces every dataset byte.
Test patches are mechanically materialized under the frozen contract, but
their analytical open count remains zero. Independent QA, normalization,
baselines, models, metrics, and training remain closed pending U04 and later
units.

## U04 - independent dataset QA

Initial source `d63c8a5ad9de825e13804394c295205393be6133`
independently reopens all twelve registered archives and reconstructs every
dataset patch without importing the U03 materializer.

Runs r001-r003 remain visible failures. R001 exposed and then closed an exact
Git-source binding gap. R002 passed the data gates but failed narrow rendering
on fixed-width tables. R003 fixed the tables but retained overflow from the
machine decision token. Sources `dcb2afc...`, `4df233f...`, and `86d5e45...`
apply those bounded remediations.

Run `BL-2026-07-25-p2o5-t03-u04-dataset-qa-r004` passes:

- twelve of twelve exact archive identities and single-link custody;
- 48 of 48 dataset file bindings and twelve source-to-patch reconstructions;
- 287 core pixels and 531 explicit unknown-ring pixels;
- zero exact duplicate features, cross-role spatial overlap, or cross-role
  source-product overlap;
- train-only normalization across 14,723 pixels per channel;
- exact JSON, HTML, PNG, and normalization replay;
- real 1280 by 720 and 390 by 844 render gates with no overflow or browser
  warning.

Only train and validation patches render. Test analytical-open count remains
zero. The accepted U04 decision authorizes U05 baseline preregistration only;
no baseline, metric, model, or training authorization exists.

The clean committed U04 checkpoint at `289ed09...` passes the full repository
suite: 683 tests, one expected skip, 228 retained warnings, and 86 subtests in
713.56 seconds.

## U05 - preregistered non-model baseline

Fresh USGS, MTBS, USDA Forest Service, and ESA sources justify transparent RBR,
dNBR, and dNDVI threshold families plus constant references. Source
`b4720cabd26adf77859a4e0024428aa80fe9a119` implements the complete frozen
fit, selection, metric, uncertainty, single-test-open, and render contract.

Protocol `31eb08ae...` commits before any array opens. Train-only fitting and
validation-only selection then freeze RBR threshold `0.041043221950531006` in
selection `061596f7...` before the test opens.

The one logical test opening evaluates every frozen family together. RBR,
dNBR, and dNDVI each classify all 89 owner-approved prototype cores across Ward
Creek and Windigo correctly. RBR remains selected by validation precedence.

R001 retains an excluded-pixel render warning. R002 retains the otherwise
passing report because it omits the candidate-selection-bias limitation. R003
preserves every analytical value, replays exactly, and passes real desktop and
narrow rendering with explicit limits.

The perfect result is bounded to selected prototype cores. Candidate
construction may favor spectral separability; unknown rings, unreviewed pixels,
complete scars, natural prevalence, independent truth, field validation, and
generalization remain unmeasured. U06 must decide model readiness. Training
remains unauthorized.

The clean committed U05 checkpoint at `ca0e977...` passes 691 tests, one
expected skip, 228 retained warnings, and 86 subtests in 541.42 seconds.

## U06 - bounded U-Net model readiness

Fresh official PyTorch 2.13 research and a metadata-only local resolver probe
establish a compatible CPU-only route on CPython 3.12.10 with
`setuptools==82.0.0`. No package is installed and no model code or training is
created.

Source `d4d3e955...` implements a fail-closed audit of the complete exact
dataset, split, QA, normalization, baseline, Phase Two, and model-family
package. The audit passes nine substantive gates. RBR remains 1.0 on all frozen
primary selected-core test metrics.

Run r001 passes replay and desktop rendering but retains narrow overflow from
the long decision tokens. Source `4caf37e...` permits those machine-readable
tokens to wrap. Accepted run
`BL-2026-07-25-p2o5-t03-u06-readiness-r002` then replays five outputs exactly
and passes 1280 by 720 plus 390 by 844 browser gates with empty diagnostics.

Decision `AUTHORIZE_BOUNDED_UNET` with qualifier
`REJECTION_FIRST_SINGLE_MODEL_EXPERIMENT` binds one small CPU-only U-Net, exact
channels, masks, normalization, architecture, optimizer, seed, deterministic
execution, validation selection, single model-test evaluation, finite checks,
and same-environment replay. It becomes executable only after verified U07.

The experiment cannot become the analytical winner on this frozen metric
because RBR is already at its ceiling. Matching is a valid trained-model result
but not added value; any lower or invalid result rejects the model and retains
RBR. No model, weights, training run, model evaluation, inference, deployment,
or final-submission-ready claim exists.

The clean accepted-output checkpoint at `535e7f3...` passes the canonical full
suite: 695 tests, one expected skip, 228 retained warnings, and 86 subtests in
582.05 seconds. Separate explicit `unittest` discovery passes 692 tests with
one expected skip in 540.969 seconds.

## U07 - coherent release candidate

BurnLens 0.52.0 registers the six dataset/readiness command routes and updates
the repository-owned README, roadmap, phase status, version history,
changelog, quickstart, case study, and devlog.

The first reviewer build retains a role-indexing failure that points the Petes
image and decision to dataset/split files. The second fixes the roles but
compresses away the candidate-selection-bias cause. Both exact attempts remain
ignored and immutable.

Accepted run `BL-2026-07-25-p2o5-t03-u07-portfolio-r003` binds exact
dataset/split/baseline/readiness/Petes inputs through unique evidence roles.
Its JSON and HTML replay exactly. Real desktop and 390-pixel Chromium checks
pass with both evidence images, correct links, keyboard-first skip navigation,
explicit non-generalization limits, null model version, empty diagnostics, and
zero external requests.

The first full-suite invocation uses the lean profile and is invalid because
GeoPandas is intentionally absent. The restored locked geospatial profile
finds two release-contract failures: a stale 99-command test and a missing LF
rule for the new devlog. Correction `4655db6...` changes no scientific or
portfolio bytes. The focused fixes pass, followed by 695 full-suite passes,
one expected skip, 228 warnings, and 86 subtests in 649.22 seconds.

Two fixed-epoch exact-archive builds produce byte-identical 1,050,456-byte
BurnLens 0.52.0 wheels at SHA-256 `eff2396b...`. The 221 entries are unique,
safe, correctly licensed and versioned, and private-path free. An isolated
CPython 3.12.10 environment installs 13 compatible distributions and passes
all 105 command help routes. One invalid zero-command roster query remains
retained before the delimiter-safe pass.

U07 ships through issue #562 / PR #563 at merge
`dfb11c8b823e224aceb76be74003464973e33c2d`. Fresh main repeats all 695
tests with one expected skip and 86 subtests, both exact reviewer outputs, and
two byte-identical 1,050,456-byte wheels at SHA-256 `eff2396b...`. Annotated
tag object `7041ef76ff4aac17f3bc2f8ba07b427dc858d2bf` peels exactly to the
merge. No model, weights, training run, evaluation, inference, deployment, or
final-submission claim exists.

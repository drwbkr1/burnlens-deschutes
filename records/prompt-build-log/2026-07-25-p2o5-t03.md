# P2O5-T03 dataset, split, baselines, and model-readiness milestone

Issue #562 starts from exact verified main
`1debea556a5fb2fc8eda87a234f106655cdc37db` on
`codex/p2o5-t03-dataset-split-baselines`.

## U01 - exact contract

Implementation source `0a1d5338a599b3cfa0a9932f12e6dbbc74b1dcd6`
reopens the v0.51 replacement candidate and every tracked source, terms,
proposal, owner-intake, and raster binding.

Run `BL-2026-07-25-p2o5-t03-u01-dataset-contract-r002` freezes a common
six-channel native 20 m Sentinel input, explicit core/unknown/unreviewed
semantics, one fixed patch per candidate, train-only normalization,
split-before-patch grouping, and sealed-test evaluation.

The independent readiness decision passes all required evidence and counts
while preserving `training_authorized=false`. Exact contract, audit, and
decision bytes replay. PRECHECK-2026-090 and REGISTRY-2026-084 retain the
complete evidence and the superseded precommit exercise.

U01 creates no dataset or split. U02 must score all 54 valid whole-event
assignments under a predeclared deterministic ranking and lock one assignment
before U03 may materialize patches.

## U02 - deterministic split lock

Implementation source `c719bf79b4c01cd4016c33649c17f26f9d4ada97`
enumerates all 90 complete 2/2/2 assignments and independently reproduces 54
valid choices. The frozen ranking first balances source regime and transfer
status across every role, then training evidence, held-out balance, test
recency, role year span, and canonical IDs.

Run `BL-2026-07-25-p2o5-t03-u02-whole-event-split-r001` locks
`burnlens-whole-event-split-v0.1.0`:

- train: Green Ridge and Tepee, 109 core pixels;
- validation: Grandview and McKay, 89 core pixels;
- sealed test: Ward Creek and Windigo, 89 core pixels.

Every role contains one event per exact source regime and one preserved
never-tuned-transfer event. The ranking and split replay exactly. No imagery
pixel was read, the test open count remains zero, and training remains
unauthorized.

## U03 - native-grid dataset materialization

Implementation source `59610b232107b0027d949e907b66a56a22722ad2`
rehashes all twelve exact registered Sentinel archives and stages the derived
dataset before no-overwrite promotion.

Run r001 fails before promotion because the implementation treats unsampled
Grandview grid areas as invalid despite the event-level all-pass registration
decision. Remediation `3e383ec413dad5ece1245951660197051a401e1c`
accepts an event grid only when every sampled registration window passes and
the machine decision is `PASS_*`; any explicit review/exclusion window remains
spatially binding.

Run `BL-2026-07-25-p2o5-t03-u03-dataset-r002` creates
`burnlens-dataset-v0.1.0`: 49 files, 1,388,556 bytes, twelve native 64 by 64
patches, 287 core pixels, and 531 explicit unknown-ring pixels. Manifest
SHA-256 is `e0b7ac66...`; canonical tree SHA-256 is `9fb4f839...`.

A second full archive rehash and materialization reproduces every dataset byte.
Test patches are mechanically materialized under the frozen contract, but
their analytical open count remains zero. Independent QA, normalization,
baselines, models, metrics, and training remain closed pending U04 and later
units.

## U04 - independent dataset QA

Initial source `d63c8a5ad9de825e13804394c295205393be6133`
independently reopens all twelve registered archives and reconstructs every
dataset patch without importing the U03 materializer.

Runs r001-r003 remain visible failures. R001 exposed and then closed an exact
Git-source binding gap. R002 passed the data gates but failed narrow rendering
on fixed-width tables. R003 fixed the tables but retained overflow from the
machine decision token. Sources `dcb2afc...`, `4df233f...`, and `86d5e45...`
apply those bounded remediations.

Run `BL-2026-07-25-p2o5-t03-u04-dataset-qa-r004` passes:

- twelve of twelve exact archive identities and single-link custody;
- 48 of 48 dataset file bindings and twelve source-to-patch reconstructions;
- 287 core pixels and 531 explicit unknown-ring pixels;
- zero exact duplicate features, cross-role spatial overlap, or cross-role
  source-product overlap;
- train-only normalization across 14,723 pixels per channel;
- exact JSON, HTML, PNG, and normalization replay;
- real 1280 by 720 and 390 by 844 render gates with no overflow or browser
  warning.

Only train and validation patches render. Test analytical-open count remains
zero. The accepted U04 decision authorizes U05 baseline preregistration only;
no baseline, metric, model, or training authorization exists.

The clean committed U04 checkpoint at `289ed09...` passes the full repository
suite: 683 tests, one expected skip, 228 retained warnings, and 86 subtests in
713.56 seconds.

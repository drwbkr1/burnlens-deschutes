# P2O4-T36 - August 6 portfolio reviewer experience

## Authority and scope

**Issue:** #540

**Branch:** `codex/p2o4-t36-portfolio-reviewer-experience`

**Base:** `origin/main@3dc057a210da024d4bba4e1db41e8b5c891663aa`

**Portfolio deadline:** August 6, 2026

This checkpoint assembles one repository-owned local reviewer journey around
already verified evidence. It cannot change labels, reopen dataset fitness,
create a dataset, split, baseline, or model, deploy, or change public-sharing
state.

## Current-tool diagnosis

The actual Windigo intake command reproduced six events and 12 balanced
prototype regions while keeping dataset, split, baseline, and model null.
Its isolated result page rendered truthfully but exposed no route to method,
failure, reproduction, lineage, or the case study.

The highest-leverage reviewer weakness was evidence fragmentation.

## Product decision

Build one deterministic, JavaScript-free, offline landing page that:

- starts with the verified Windigo result;
- shows the Petes Lake stop as retained evidence;
- gives a two-minute review path;
- explains source roles and limitations;
- exposes commit, software, target, AOI, schema, label-set, and run lineage;
- shows dataset, split, baseline, and model as explicit null values; and
- routes to the quickstart, case study, decisions, and release audit.

## Build and QA state

Source commit `6cdb752b7284bf6da35cd3e899338f78100643fd`
adds the generator, CLI, tests, quickstart, version 0.48.0, and the 87th
command. Commit `7ffb8ce74350c34f60c36765e194a2aab29dbcd9`
adds an inline favicon and corrects its local-link test.

Run `BL-2026-07-24-portfolio-reviewer-experience-r002` produces:

- JSON: 5,675 bytes /
  `cfab6176a9e5f1e9c4636e56658cf1ded9092825c3f55c7ebfb9968b2a68a078`;
- HTML: 14,602 bytes /
  `e133a22978d73335c8a67fa39ae5ff48279b67ac6f71c615d95f62c021fda3ae`.

An ignored replay matches exactly. Desktop 1440 by 1000 and narrow 390 by 844
rendering pass. Both images load, all nine local destinations return HTTP 200,
keyboard order starts with the reviewer path, the console is clean, and no
external URL loads. The owner confirmed the exact page renders correctly.

The first generated page's missing-favicon request and the first corrected
favicon-test assumption remain visible as retained failures.

## Current state

U01-U05 pass. U06 release verification remains pending. The output is a
release candidate, not yet a shipped or tagged release.

No scientific, label, dataset, split, baseline, model, deployment, access, or
public-sharing state changes.

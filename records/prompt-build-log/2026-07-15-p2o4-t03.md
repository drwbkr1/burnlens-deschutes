# P2O4-T03 - Cross-Event Source Fitness

## Entry metadata

| Field | Value |
|---|---|
| Date | 2026-07-15 |
| Task | P2O4-T03 - acquire and prove fitness of four frozen cross-event products |
| Issue | #361 |
| Branch / base | `codex/p2o4-t03-cross-event-source-fitness` / `2523207ad10ca88d822bd34d96bb482feded6072` |
| Target software | BurnLens `0.11.0` |
| Acquisition source | `9693f0f5d1476740e06d4a69620072e70cd2002d` |
| Current state | Issue #361 / PR #362 analytically merged at `6a6da910849daefa918ed56af6631b2ec44bc211`; pre-tag trace-remediation issue #363 is active; tag withheld |

## Cycle decision

The synchronized 0.10 workflow reproduces byte for byte and again selects Tepee/McKay while stating that availability is not pixel fitness. The highest-leverage weakness is the four exact source archives themselves. Issue #361 therefore freezes a no-expansion acquisition, integrity, native-pixel, and pair-registration checkpoint.

## Research and precedence

Fresh official CDSE token, OData, Sentinel-2, and terms sources confirm the documented authenticated whole-product route and current SCL/band semantics. Live OData and STAC records preserve exact scene identity but expose route-specific archive sizes/checksums. `SOURCE_PRECEDENCE-2026-005` makes OData authoritative for the acquired archive bytes and retains STAC as discovery provenance.

## Pre-data validation

- 127 tests pass; sixteen directly exercise the four-asset contract, credential removal, route disclosure, drift rejection, promotion failure, safe CLI state, explicit processing-baseline selection, OneDrive-safe temporary naming, bounded retry behavior, arbitrary event geometry, native-grid registration, full-boundary quality accounting, visual gates, and deterministic rendering.
- Compilation and dependency health pass.
- Live public OData refresh accepts all four exact products.
- No provider byte or secret is committed.

## Authenticated run and protected pause

Authenticated run `BL-2026-07-15-cross-event-optical-intake-r001` exercised the existing DPAPI-backed CDSE wrapper. CDSE ended the first product response at `1,026,064,938` of `1,129,978,013` expected bytes. The client returned `DOWNLOAD_SIZE_MISMATCH`, registered no raw package, retained only the resumable ignored `.part`, removed credential environment values, and wrote a bounded private failure state with no credential, token, or authorization data.

OneDrive then exposed the partial through a transient second hard link. The download client now rejects any multiply linked partial before making a provider request (`PART_FILE_MULTILINK_NOT_ALLOWED`). The aliased inode was preserved under ignored held evidence while a byte-identical, single-link repo-local copy was prepared for `r002`; no external staging path was deleted or bypassed.

`r002` failed before provider access on a transient public-metadata `URLError`. `r003` reached `853,540,864` bytes on a fresh full-object response, then stopped making progress while OneDrive created another staging hard link; it was terminated and recorded without registration or secret retention. A live authenticated range diagnostic returned HTTP 200, the full object length, and no `Content-Range`, so the route cannot be described as resumable.

Fresh Microsoft primary guidance states that temporary `.tmp` files are not synced and names beginning `~$` are invalid for OneDrive. `r004` reused the accepted Tepee pair, completed McKay pre, and failed closed after McKay post reached `57,264,698` bytes and its unprefixed `.zip.tmp` gained a second link before retry. A live prefixed probe stayed single-linked with OneDrive running. The cross-event client now combines both exclusions as `~$<SAFE>.zip.tmp` and performs at most five fresh-token full-object attempts for early EOF/request/stream failures. All final byte, checksum, container, link, atomicity, and re-verification gates remain binding. Existing BurnLens provider workflows retain their historical `.part` behavior.

## Independent verifier implementation

The source-fitness path verifies the registered four-archive package before opening imagery, preserves the OData byte contract and STAC discovery provenance, reads full frozen MTBS geometries, uses native 10 m TCI only for preview, quantifies native 20 m B04/B8A/B12/SCL evidence without reprojection or resampling, and measures event-scaled pair-local registration from independent gradients. Irregular-boundary quality is counted only for pixel centers inside the complete source geometry. Rectangular surrounding context may support correlation but cannot become quality, label, or dataset evidence.

Synthetic fixtures prove software behavior only. They did not substitute for the later four real provider products or rendered real-output review.

## Successful acquisition and final storage finding

Run `BL-2026-07-16-cross-event-optical-intake-r005` registered all four exact OData archives / 4,551,170,756 bytes under ignored local raw storage. All four pass provider MD5/BLAKE3, local SHA-256, size, path, ZIP/SAFE/root/manifest/CRC, atomic promotion, and post-promotion checks. Credential environment values were removed and no secret-bearing material is retained.

The longer real-package observation disproved the assumption that a `~$...tmp` name alone could keep a durable small manifest single-linked. OneDrive repeatedly aliases `.burnlens-registration.json` and once restored the exact Tepee-pre inode under its historical `.zip.part` name. BurnLens reverified its exact size/SHA-256 and restored only the repo-local registered name. The generic verifier remains strict. Cross-event verification permits the metadata-manifest link only after one-read SHA-256 and complete registration/asset comparison; every provider archive remains single-linked. The exception is public evidence, not an invisible local waiver.

## Real source-fitness result

Source `d28c0368c5b71b0c982d056d3db9409a1725f697` generated final run `BL-2026-07-16-cross-event-source-fitness-r005` at `2026-07-16T01:50:11Z`.

- Tepee: 86.5217% pair-eligible, 8.3441% review-needed, 5.1341% excluded; W-01 excluded, W-02 review-needed, W-03 pass; p95 0.0976 pixel.
- McKay: 100% pair-eligible; all three windows pass; p95 0.1008 pixel.
- Decision: `ACCEPT_CROSS_EVENT_SOURCE_FITNESS_WITH_EXCLUSIONS`.
- JSON: 75,267 bytes / `c5f33e66b475e8df3f3b53217beaf048bca3a2b4c7760566ee49b095b8ce4769`.
- HTML: 6,935 bytes / `6bd2299f138ed42cc2f8ee0f20592b085095eb63c6d7ebdbd18fff2469ecf650`.
- PNG: 921,511 bytes / `02e7835bf2d2eff817e3c4a8f5bf3b4c8c41d4b65809667addcdf4a541a3aa97`.

Original-resolution PNG and live semantic HTML review pass. The candidate has 128 passing tests, compilation and dependency health, a 183,557-byte wheel with SHA-256 `6c39d511e6b7ac5786cad68016be3320239954e4c1c78a748b310106f126cf78`, isolated `0.11.0` import, and four cross-event entry points.

## Pre-tag trace remediation

PR #362 squash-merged the analytical checkpoint at `6a6da910849daefa918ed56af6631b2ec44bc211`, closing issue #361. Fresh no-hardlink merged-main verification passed 128 tests, compilation, dependency health, wheel build/import, exact artifact hashes, and original-resolution rendering, but semantic trace readback found a release-blocking identifier contradiction: `MANIFEST-2026-012` named implemented schema `burn-scar-five-state-schema-v0.1.0` while the report's `label_schema_version` named design protocol `burn-scar-label-protocol-v0.1.0`.

The tag was withheld and issue #363 opened. Source `cf1d9101e2760bf7d779b6fae68e605bb8809c1c` adds a frozen feasibility-schema gate and separates `label_protocol_version` from `label_schema_version`. Real run `BL-2026-07-16-cross-event-source-fitness-r006` preserves all event pixels, windows, metrics, decisions, lineage fields, and input hashes from `r005`. Its JSON/HTML/PNG pass original-resolution and live-browser review with both identifiers visible. The remediated candidate passes 129 tests, compilation, dependency health, and a 183,761-byte wheel / SHA-256 `a969490d1c9f46c494804c0b1a2625c4afea4d2e7fc8f4e39791f0b9f0a4a302` / isolated import. Remediation PR/merge/tag gates remain pending.

## Boundary

No label, dataset, partition, patch set, baseline, model, application, deployment, accuracy, generalization, official, operational, emergency-ready, field-validated, or endorsed status may be created or implied.

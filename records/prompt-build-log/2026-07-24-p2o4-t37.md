# P2O4-T37 - deterministic August 6 submission bundle

Issue #544 starts from verified main
`07cc4177de193b1261ee468e577245c11c1d4a52`.

The current v0.48 reviewer page reruns exactly. The remaining reviewer-visible
weakness is handoff friction. P2O4-T37 therefore adds one standard-library,
no-overwrite bundle builder rather than another interface.

Source `7d972bfa3aaf86c09db3226d2b7447cdd7e16e08` adds BurnLens
0.49.0 and command 88. The first environment smoke correctly finds installed
metadata still at 0.48.0; the locked editable environment is refreshed.

Initial production r001 then exposes five missing LF checkout contracts and is
retained in ignored failure custody. Remediation `660f54f...` adds exact LF
rules and binds the cross-platform bytes.

Final run `BL-2026-07-24-august6-submission-bundle-r002` produces a
deterministic 784,940-byte ZIP at SHA-256
`3d50c6ec627e9b15c63fa84a034e60dddbcccbdd27f182af316a08de3bd13b53`
and a 774-byte receipt at SHA-256
`08b507a3adddb66a1b50838cbe23c3e13e6d6b9742d4d3fa274a4c7d282fddbf`.

R002 replay bytes match. The archive has 14 safe unique members and an
internally verified roster. The final extracted start page passes
desktop, narrow, image, nine-link, keyboard, console, and zero-external-request
checks in installed Chrome.

The first full candidate suite passes 557 tests and fails only 29 stale
current-package `0.48.0` assertions after the intentional `0.49.0` bump. A
bounded correction updates those current-package expectations while retaining
the historical v0.48 report identity. The first two-minute affected-test run
times out without a reported failure; the longer bounded rerun passes all 187
affected tests, 21 existing warnings, and four subtests.

The corrected full suite passes 586 tests, one expected skip, 22 warnings, and
86 subtests. Exact replay r003 still matches r002. Two exact Git ZIP archives
of `97ef6e2...`, built under `SOURCE_DATE_EPOCH=1784868552`, produce
byte-identical 887,000-byte wheels at
`57b4cc8338926b583ef976d6bee698ecedf1be869944d6dee69ced78cda3da50`.
The wheel has 187 safe unique entries and 88 commands. An isolated Python
3.12.10 environment installs 13 compatible distributions and passes all 88
help routes.

The first binary-TAR isolation attempt was corrupted by PowerShell's text
pipeline and rejected before build. File-based Git ZIP archives supersede it.
`RELEASE-AUDIT-2026-003` is valid and blocked only on PR, merge, fresh-main
repetition, and remote tag verification.

U01-U05 pass. U06 local candidate gates pass; repository lifecycle gates
remain. Dataset, split, baseline, model, deployment, access, and
public-sharing state remain unchanged.

PR #545 merges at `8d4b81f...`. The first fresh-main focused run retains one
30-second command-help timeout after nine passes. The exact command, complete
geo profile, and unchanged 10-test focused suite then pass. Fresh main
reconstructs the exact ZIP, receipt, and wheel; installs 13 compatible
distributions; passes all 88 commands; and passes 586 full tests, one expected
skip, 22 warnings, and 86 subtests.

Remote annotated tag object `f46c73aaaa10fb0e91a4cd6f1e94085f3bf50b72`
peels exactly to merge `8d4b81f...`. `RELEASE-AUDIT-2026-003` is verified.
U01-U06 pass. Final submission-readiness review is next.

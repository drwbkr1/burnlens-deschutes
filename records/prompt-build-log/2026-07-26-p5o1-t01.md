# P5O1-T01 — Phase Five reliability release candidate

Issue: #574

Branch: `codex/p5o1-t01-reliability-release-candidate`

Verified base: `3c0ec9ef893b0e610c6c38c70a191e5e67c09ca9`

## Selected weakness

The verified Phase Four interface communicates accepted, degraded,
no-detection, fallback, failed, and withheld states, but only the accepted
path has been exercised end to end. The highest-leverage Phase Five weakness
is therefore real failure detection and recovery rather than additional
analytical or presentation scope.

The owner confirmed the exact interface renders correctly. Both extracted and
ZIP package forms pass the canonical validator. Browser automation attempted
to open the exact local `file://` artifact but the browser security policy
blocked the route. No bypass, alternate browser automation, or policy
circumvention was attempted.

## U01 — QA and release-control contract

Run ID: `BL-2026-07-26-p5o1-t01-u01-contract-r001`

The contract binds the immutable v0.54 ZIP, receipt, package manifest,
checksum roster, interface, status record, and release audit. It fixes the
six-unit dependency order, five mandatory failure injections, a bounded WCAG
2.2 AA-oriented accessibility review, security and integrity checks,
project-specific offline performance budgets, defect severity, release
states, rollback posture, and prohibited scope.

Fresh primary-source research records W3C WCAG 2.2, OWASP archive-path
traversal guidance, and CISA SBOM resources. These sources support test
selection only; BurnLens does not claim formal accessibility certification,
a broad penetration test, or vulnerability-free software.

RBR remains the accepted analytical method. The trained U-Net remains a valid
but rejected diagnostic. No retraining, retuning, second experiment, Phase
3B, analytical-source change, package mutation, deployment, public-sharing
change, or model-superiority claim is authorized.

The first validation attempt, retained as r001a, ran `uv sync` without the
`dev` extra. The sync correctly removed pytest, so the test command failed
before collecting tests. PowerShell then continued to the contract and package
validators because native command exit codes were not checked explicitly.
Those later validators passed, but r001a is not a test pass.

Corrected r001b restores the locked `dev`, `model`, and `geo-research`
profiles and stops after every nonzero native exit. Ten focused
contract/package tests pass with 20 existing NumPy warnings. The new contract
CLI reports six units and five injections. Both canonical package forms pass
at 66 payload files and ten GeoTIFFs. JSON parsing, the dependency lock, and
`git diff --check` pass.

Exact U01 outputs:

- contract: 10,836 bytes / SHA-256
  `a187a9d12adea288700b25c0e3a9c18ce08dee7f5f45c46a7989ca8dc588bc4c`;
- primary-source research: 2,401 bytes / SHA-256
  `a688467433bb8c4de15bd02fb64dd0dc053d1cea1c6499bdd91f1cfd87dfa670`;
- fail-closed loader: 7,976 bytes / SHA-256
  `d7131942c315b7e4d912aa011468375e53683d5b9b8e7dffe9209ec8f34cbd5f`;
- validator CLI: 1,237 bytes / SHA-256
  `10abcb0fcad9a98ed318e53baf47446f3c439b7cba5cfef7601a347e118271f1`;
- focused tests: 3,011 bytes / SHA-256
  `f1ce0b7d690a761e8131373e09de9e59da99e558d4cfb5bac5b60b45ba208244`.

Disposition: `pass`. Next dependency: `P5O1-T01-U02`.

## U02 — fail-closed package diagnosis and recovery

Pre-fix run r001 executed all five contract injections against the untouched
v0.54 validator:

- missing `interface/index.html`: rejected with exact checksum mismatch;
- corrupt `interface/index.html`: rejected with exact checksum mismatch;
- archive path traversal: rejected as unsafe/corrupt;
- partial package: rejected only through an uncontrolled raw
  `FileNotFoundError` for `CHECKSUMS.sha256`;
- manifest with wrong accepted method, wrong route, and wrong run ID:
  unexpectedly returned `PACKAGE_VALIDATION_PASS`.

Both canonical package forms still passed after the probe. The partial and
binding results make r001 a retained high-severity remediation result, not a
pass.

The remediation binds package ID, repository, issue, unit, run pattern, route,
accepted method, rejected diagnostic, entrypoint, checksum roster, manifest
inventory, and exact file roster. It rejects symlinks, unsafe payload paths,
unrostered files, invalid ZIPs, duplicate/encrypted/directory members, member
count drift, extraction-budget overflow, and CRC failure with controlled
errors. A deterministic runner derives five invalid ZIPs from the exact
canonical archive, records their identities, requires the exact diagnosis,
and revalidates both untouched canonical forms after every fixture.

Seven focused validator/runner tests pass with 160 existing NumPy warnings.
Production evidence remains pending until this code-only state is committed
and clean.

Implementation commit `b868ff007670b98b385eaeb2060b942fb38c7a4c` was
pushed before production execution. Run
`BL-2026-07-26-p5o1-t01-u02-failure-injection-r002` then generated five exact
invalid archives in ignored repository-local custody:

- missing member: 428,587 bytes / `3db12453...`;
- corrupt member: 428,540 bytes / `a73edde7...`;
- path traversal: 487,461 bytes / `72a39070...`;
- binding mismatch: 487,875 bytes / `781d5880...`;
- partial package: 4,693 bytes / `25e1a1f9...`.

Every fixture receives its exact controlled diagnosis. Both untouched
canonical package forms revalidate after every injection, the canonical ZIP
remains `91308a2f...`, the canonical interface remains `7a657ad7...`, no
`escape.txt` exists, and no accepted output is created. A separate replay with
the same timestamp, run ID, and source commit reproduces the public 4,590-byte
JSON / `1c00be85...` and 3,610-byte HTML / `9aef845e...` byte-for-byte.

The HTML has a strict offline CSP, explicit non-operational warning,
text-only failure diagnoses, narrow reflow CSS, and baseline/rejected-model
language. Its real browser render is not claimed here because the local
`file://` automation route remains policy-blocked; U03 owns exact rendered and
keyboard evidence.

The 7,104-byte U02 record has SHA-256 `c5ff046e...`. Its portable validator
rebuilds all five fixture ZIPs from the tracked canonical archive instead of
depending on ignored fixture custody, verifies both tracked public outputs,
and revalidates the canonical package. Fourteen focused contract,
package, and failure tests pass with 180 existing NumPy warnings.

Disposition: `pass`. Next dependency: `P5O1-T01-U03`.

## U03 — accessibility, rendered-browser posture, and reviewer clarity

Source inspection found one degraded-mode defect in the canonical interface:
the rejected U-Net SVG group did not carry an initial `hidden` attribute.
JavaScript hides it during normal initialization, but disabled or failed
script execution could expose the diagnostic even though the interface says
it is off by default.

The remediation creates a separate Phase Five reliability surface and leaves
the verified Phase Four bytes unchanged. It:

- hides the rejected model in the HTML before any script runs;
- adds a focusable main-content skip target;
- explains the no-script state while keeping accepted RBR and the textual
  equivalent available;
- adds a four-step first-time reviewer path and the exact U02 failure result;
- preserves native buttons, checkboxes, ranges, visible focus, the complete
  text equivalent, non-color failure language, 390-pixel reflow,
  reduced-motion behavior, and the strict offline CSP;
- binds the exact canonical interface and failure report.

Five new interface tests plus the historical Phase Four interface and U02
tests pass: 14 tests with 164 existing NumPy warnings. Production execution
and exact owner desktop/narrow render remain pending until the implementation
is committed and clean.

Implementation commit `81473b03e897ced8a682b0bf3d428a75bab674ff` was
pushed before production execution. Run
`BL-2026-07-26-p5o1-t01-u03-reliability-interface-r001` produces:

- HTML: 179,808 bytes / SHA-256 `9c5b65dd...`;
- JSON: 3,000 bytes / SHA-256 `33a9cf2f...`.

An independent deterministic replay reproduces both files exactly. Static DOM
audit finds 27 unique IDs, one main region, one no-script region, three
buttons, twelve inputs, zero duplicate IDs, and zero external runtime
references. All declared contrast pairs pass; the lowest recorded content
accent ratio is 6.219 and the focus indicator ratio is 14.264. The HTML remains
below the 250,000-byte budget.

The owner confirmed the exact generated candidate at branch commit
`572cecad18edb4e3685db653c51c6e10d2641887` with the response
`desktop, narrow, and keyboard pass`. Receipt
`PHASE-FIVE-RELIABILITY-INTERFACE-OWNER-RENDER-RECEIPT-2026-001` binds that
confirmation to the 179,808-byte HTML with SHA-256 `9c5b65dd...`; the receipt
is 1,346 bytes with SHA-256 `94872637...`.

Disposition: `pass`. The confirmation covers owner-observed desktop render,
390x844 narrow render, and keyboard journey. It is not independent
accessibility certification, third-party validation, or a formal WCAG
conformance claim. Next dependency: `P5O1-T01-U04`.

## P5O1-T01-U04 — security, integrity, license, claims, and performance assurance

The U04 implementation adds a release-scoped assurance runner and public
offline report. The first focused run failed because the secret rule matched
unquoted assignments but missed JSON-style quoted credential keys. The test
failure is retained; the rule was corrected before production. Nineteen
focused U01-U04 tests then passed with 160 retained historical NumPy warnings.
Implementation commit
`e14717aeef7ac23c22d37b701327f63ac8613df2` was pushed before the clean
production execution.

Run `BL-2026-07-26-p5o1-t01-u04-assurance-r002` binds the immutable
487,893-byte Phase Four ZIP (`91308a2f...`), the 1,795,388-byte extracted
package, and the owner-confirmed 179,808-byte interface. Its public outputs
are:

- JSON: 31,276 bytes / SHA-256 `89c0c789...`;
- HTML: 3,974 bytes / SHA-256 `ef666ae3...`;
- controlling record: 7,928 bytes / SHA-256 `752148bf...`.

Dependency health, safe archive structure, checksums, schema and binding,
source rights, public privacy, unsupported claims, external runtime requests,
and secret scans pass. All 75 installed distributions expose license
metadata; no dependency bytes are redistributed in the Phase Four ZIP.
Three directory and three ZIP validations pass. The observed maximum is
2.913567 seconds against the 5-second contract budget; archive, extracted,
and interface byte budgets also pass.

`pip-audit` 2.10.1 returns exit code 1 and two raw records for one unique
setuptools 82.0.0 advisory: GHSA-h35f-9h28-mq5c / CVE-2026-59890. The
current GitHub advisory snapshot classifies it as medium, CVSS 3.1 score 6.1,
fixed in 83.0.0. The exact raw audit and advisory snapshots remain ignored
with hashes. U04 records the bounded Windows/NTFS, no-`MANIFEST.in`,
ASCII/NFC tracked-path, no-sdist, ZIP-only mitigation and does not suppress
the finding or claim the environment is vulnerability-free.

The exact HTML was served over a temporary loopback server, inspected through
its rendered DOM, and visually checked at desktop size. The accepted RBR
route, rejected U-Net limitation, exact run/commit/archive identity, assurance
table, disclosed finding, budgets, and limitations are readable. The local
server was stopped and no external request occurred.

Disposition: `pass-with-disclosed-medium-finding`. The U-Net remains rejected
and did not outperform RBR. No analytical output, deployment, public-sharing,
official, operational, field-validation, endorsement, or emergency-use claim
changed. Next dependency: `P5O1-T01-U05`.

The first bounded full-regression batch then found one stale QA expectation:
the environment profile still hard-coded Phase Four's 114-command release
roster while the current Phase Five source exposes 119 exact commands. The
verifier itself correctly checked and exercised all 119 commands. Correction
commit `4283a292a31ec31f9c1eb84ccb01b3970bb2e96a` derives the expected names and
count from the current `pyproject.toml` roster; the standalone runtime smoke
passes one selected test in 73.75 seconds.

The corrected first three bounded regression batches pass 615 tests, 53
subtests, and one expected skip. Batch 4 passes 98 tests before the inherited
P2O4-T39 checkout-contract test correctly stops because the U04 registry,
research record, prompt log, public artifacts, and assurance record remain
uncommitted. That clean-checkout gate will be rerun after this evidence
checkpoint is committed; it is retained as a repository-state failure rather
than represented as a product regression.

After evidence commit
`4b3d648ee186338262e9701894286d3f3dac1fb3`, the clean Batch 4 rerun passes
all 159 tests and 33 subtests, including the P2O4-T39 checkout-contract guard.
The final one-file batch passes four tests. Across all five bounded batches,
the complete 779-test collection finishes with 778 passed, one expected skip,
86 subtests passed, and 412 retained NumPy deprecation warnings. No failing or
running regression process remains.

## P5O1-T01-U05 — clean reconstruction, release identity, and rollback

Implementation commits `724f7a3e412806a5bff06963dea38f5104350cb4` and
`75d1b77ed8a3faf0cbcf75b7f049fdb5afa9b115` add the clean-reconstruction
gate and then require the executing module to load from the exact clean
checkout. Run `BL-2026-07-26-p5o1-t01-u05-clean-reconstruction-r004` uses a
fresh remote-equal short-path checkout with `core.longpaths=true`, Python
3.12.10, 75 locked distributions, and a 12.553-second environment sync against
the 900-second budget.

The clean checkout executes 119 of 119 command help routes in both geospatial
and model profiles. Two fixed-epoch candidate wheels are byte-identical at
1,198,080 bytes / SHA-256 `e619d3b7...`. An independent environment installs
the exact lock plus that wheel with `--no-deps`, imports BurnLens only from
isolated `site-packages`, preserves the exact locked transitive versions, and
repeats both 119-command profiles with byte-identical environment receipts.
This is a commit-bound pre-freeze candidate at software version 0.54.0, not a
new v0.54 release.

The tracked 66-file package reconstructs to the exact 487,893-byte canonical
ZIP at SHA-256 `91308a2f...`. Extracted directory, canonical ZIP, and
reconstructed ZIP all validate. Ten NPY/GeoTIFF pairs preserve exact binary
arrays, float arrays within the 0.000001 contract with observed maximum
difference zero, EPSG:32610, and 64-by-64 shapes. RBR remains accepted, the
U-Net remains rejected and did not outperform RBR, WCP-001 remains 141.44 ha
with 94.19% MTBS overlap, and WCP-002 remains 66.76 ha with zero MTBS overlap.

The naive clean-clone suite retains 711 passes, two exact custody failures, 25
custody-dependent errors, 47 skips, and 86 passed subtests. No application or
scientific defect is inferred. The explicit portable roster then passes 711
tests with 47 skips, two exact custody assertions deselected, and zero
failures or errors.

A separate clean detached checkout verifies tag object `4a7a54b...` peeling
to `8660ccb...`, validates the package, and passes 114 of 114 command routes
in both profiles without moving the canonical branch. The locked runtime's
setuptools 82.0.0 creates deterministic 1,166,315-byte wheels at
`b20c517d...`, but exact comparison with the historical wheel finds only the
`WHEEL` generator identity and dependent `RECORD` differ. A dedicated
build-only setuptools 82.0.1 environment reproduces two exact historical
1,166,315-byte wheels at SHA-256 `ad3ae7c8...`.

The omitted historical builder identity is recorded as medium known issue
`P5-U05-KI-001`. Its impact and exact workaround are visible; it is not a
Phase Six blocker because rollback succeeds exactly. Earlier clone-wrapper,
path-depth, unlocked-install, import-contamination, source-isolation, and
builder-mismatch attempts remain retained.

Public outputs are JSON 9,135 bytes / SHA-256 `510eec2e...` and HTML 6,048
bytes / SHA-256 `9227c6af...`. The exact HTML passes desktop and 390x844
narrow browser inspection with no horizontal overflow, external resource,
warning, or error. One browser full-page stitching artifact duplicated and
compressed the page despite correct DOM geometry; normal viewport captures
pass and the stitching artifact is not used as render evidence.

Disposition: `pass-with-visible-medium-builder-identity-issue`. No analytical
byte, dataset, split, label, threshold, model decision, deployment,
public-sharing status, Phase 3B, or second experiment changed. Next dependency:
`P5O1-T01-U06`.

## P5O1-T01-U06 â€” baseline-first candidate freeze and Phase Six recommendation

The first package attempt,
`burnlens-phase-five-baseline-first-candidate-v0.1.0`, remains exact ignored
evidence but is superseded because its source predates the current-version and
checkout-contract corrections. Corrected run
`BL-2026-07-26-p5o1-t01-u06-release-candidate-r002` binds builder source
`dd1c270421250ce312d548b9571f91b4b806536b` and freezes candidate
`burnlens-phase-five-baseline-first-candidate-v0.1.1`.

The corrected package contains 23 files / 981,264 bytes. Its deterministic
ZIP is 646,513 bytes / SHA-256
`691c4bddb6754d74ca858a0b801fb21e62103032184425d2ba1b1648df1b0c26`.
Both package forms validate, and an ignored no-overwrite replay reproduces the
ZIP exactly. The exact Phase Four ZIP remains unchanged at 487,893 bytes /
`91308a2f...`.

Microsoft Edge passes 1440-by-1000 and 390-by-844 renders, all six local links,
both core interfaces, keyboard-first focus, zero overflow, zero external
resources, and zero browser errors. The loopback server is stopped.

Candidate freeze commit `1c799cd3f40c37013c71d9af9b15caea1d9c5417`
produces two byte-identical 1,207,948-byte BurnLens 0.55.0 wheels at SHA-256
`1d0e862c9c7d30f148352ebcc45f22a9deb2e010dd61ba63a02db50f700177f6`.
A fresh 75-distribution CPython 3.12.10 environment imports only the installed
wheel and passes dependency health plus all 120 geospatial and 120 model
command routes.

Four regression outcomes remain visible:

- r001: 758 passed / 31 failed before version and checkout corrections;
- r002: 788 passed / one dirty-checkout failure during the candidate freeze;
- r003: 787 passed / two LF-checkout failures for the Phase Five objectives;
- r004: 789 passed / one expected skip / 86 subtests at clean pushed commit
  `a53bcd85413c524ae9fd0ac6007ba59ba778d532`.

The r003 failure is a real portability defect, not an analytical failure.
Commit `a53bcd8...` adds the exact LF rule; both failed assertions and the full
suite pass afterward.

The controlling U06 record is
`records/phase-five/release-candidates/PHASE-FIVE-RELEASE-CANDIDATE-RECORD-2026-001.json`,
11,367 bytes / SHA-256 `76c2c129...`. The release audit is
`records/phase-five/release-audits/RELEASE-AUDIT-2026-001.json`, 7,005 bytes /
SHA-256 `9f47ecbd...`.

Disposition:
`accepted-baseline-first-candidate-pending-milestone-verification`. Zero
critical/high findings are open. The U04 setuptools advisory and U05
historical builder-identity omission remain two visible medium findings with
impact and workaround. RBR remains accepted; the U-Net remains rejected and
did not outperform RBR. Phase Six is recommended only after the single
milestone PR, reviewed merge, fresh-main verification, and annotated tag pass.
No GitHub Release, deployment, sharing change, external submission, Phase 3B,
or second experiment is created.

# P6O1-T01-U06 — real recipient and rendered QA

- Issue: #578
- Unit: `P6O1-T01-U06`
- Run: `BL-2026-07-27-p6o1-t01-u06-recipient-qa-r001`
- Exact base: `d046f17837262476e07c2e3e2e3b8933d470bb90`
- Candidate: `burnlens-phase-six-baseline-first-candidate-v0.1.1`
- Archive: 14,963,469 bytes / `5a314b69e6efb64b6058d21cd33b74bef4d14c07f2bd0457eb8eea43c935ab2e`
- Decision: `pass-local-recipient-qa`

## Prompt and boundary

Resume the controlled Phase Six milestone from the exact U05 candidate. Validate
the real extracted recipient route at desktop and 390×844 widths, keyboard
first, offline, with complete link and asset closure, safe extraction,
fresh-checkout reconstruction, rollback, privacy, rights, claims, and source
boundaries. Do not change analytical evidence or start any public action.

## Evidence

The no-overwrite extraction contains the expected 117 files / 16,285,070 bytes.
Every relative path, byte count, and SHA-256 equals the tracked directory form,
and the installed validator reports `PHASE_SIX_CANDIDATE_VALIDATION_PASS`.

The exact extracted `OPEN-FIRST.html`, canonical Phase Six surface, Ward Creek
interface, and seven-slide presentation pass real browser QA. Desktop
1440×900 and narrow 390×844 have zero horizontal overflow and zero console or
page errors. The WCP-002 focus control and rejected-U-Net diagnostic toggle
work without changing analytical role. Independent Chrome keystrokes prove
that Tab exposes `Skip to package guide` first and Return moves the URL and
visible focus to `#main`.

All 115 manifest-listed files return HTTP 200 with exact byte counts. The
manifest and checksum controls also return HTTP 200 with exact byte counts, so
all 117 package files are covered. Twenty HTML files expose 138 references and
zero external auto-fetch or click-only URLs.

Fresh-checkout r001 is retained failed: the long ignored clone path hit Windows
filename limits and the builder rejected the incomplete checkout. R002 uses a
short ignored repository-local path plus `core.longpaths`; it starts clean,
validates the tracked ZIP, and rebuilds the exact 14,963,469 bytes at
`5a314b69...`. The predecessor remote annotated tag still resolves through
object `3307214...` to merge `7066dcd...`, and its 646,513-byte Phase Five ZIP
validates at `691c4bdd...`.

The first focused regression attempt runs before the U06 evidence commit:
seven tests pass and the historical checkout-contract test fails exactly
because the changed worktree cannot yet equal `HEAD`. This is retained as a
timing-invalid attempt; the same suite must pass from the committed checkpoint
before push.

## Disposition

U06 passes locally. RBR remains accepted, the trained U-Net remains rejected,
and WCP-002 stays first-class false-positive-risk evidence. No dataset, split,
label, threshold, source, model, or analytical byte changes. No Phase 3B,
deployment, GitHub Release, access, ownership, public sharing, publication, or
external submission action. U07 release audit and publication-gate decision is
next.

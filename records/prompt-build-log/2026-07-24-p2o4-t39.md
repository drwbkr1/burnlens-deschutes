# P2O4-T39 - Replicated-regime replacement event

Issue #554 resumes the full model-bearing BurnLens critical path from verified v0.50. It permits exactly one complete replacement event before the full six-event sufficiency rerun. Ward Creek is primary. Akawana is the sole metadata-only fallback and remains unopened while Ward Creek passes.

## U01 source gate

`P2O4-T39-U01` runs the current scientific state first: all 90 prospective 2/2/2 assignments still fail source-regime replication because Darlene is the sole NIFC-context event. The smallest credible remediation is one complete current-MTBS event that can replace Darlene prospectively.

Fresh official Portal, MTBS, CDSE STAC, CDSE OData, MTBS methods, CDSE terms, Sentinel legal-notice, and USGS rights checks agree on Ward Creek and one exact Sentinel-2A pair. The full 3,340-vertex MTBS polygon is valid. Both selected STAC footprints pass Shapely `covers`, not the obsolete vertex-only shortcut. Current OData supplies exact sizes, MD5, BLAKE3, online state, acquisition time, tile, orbit, processor baseline, and product type.

The final source-gate record validates ready with two sources, 16 required criteria, 31 evidence items, 12 fresh live items, and no blocker or warning. The exact r008 HTML passes real 1440×1000 and 390×844 rendering, including explicit mobile field labels, no horizontal overflow, and no browser warning or error.

U01 retains seven preceding attempts: a WFS timeout, provider/computed-area normalization correction, stale legal URL, invalid first gate schema, first valid ignored gate, a late child-process output with a rejected commit string, and the first valid tracked presentation with generic mobile labels. None acquired a provider archive.

The source implementation is recoverably pushed through `41b451d180f962aa1216384925c650fa43fcb9d6`. Tracked U01 artifacts are frozen in SOURCE-2026-038, TERMS-2026-033, PRECHECK-2026-073, and REGISTRY-2026-069.

## U02 optical custody

U02 adds one fail-closed, sequential, exact-pair custody command. The first
credential-free run catches two mistyped U01 digest constants before credential
use or provider bytes. Correction
`3b3406b5e875889b4c282130d1ed5270e33a7572` adds an exact-byte regression and
is pushed before the provider transaction begins.

Run `BL-2026-07-24-ward-creek-optical-intake-r001` registers the exact
1,198,399,787-byte pre archive and exact 1,198,420,414-byte post archive. Each
passes provider MD5/BLAKE3, local SHA-256/MD5/BLAKE3, safe SAFE structure, full
CRC, single-link, registration, post-promotion rehash, EPSG:32610, exact 10 m
and 20 m grids, nodata, SCL, quality-mask roster, and temporal-order gates.
The ignored contract and promoted files pass the independent controlled-intake
validator.

Tracked report `WARD-CREEK-OPTICAL-CUSTODY-2026-001.json` is 16,317 bytes /
SHA-256 `a8d89779b7508b439fee6cb5bc99dd926a62c56ab58da181b0f1b40b1bcc1f2f`.
PRECHECK-2026-074 and REGISTRY-2026-070 bind the private states, manifests,
contract, archives, tests, and retained preflight failure.

## U03 exact request and delivery custody

Source commit `21eff8dd85cbf795bef68e0f0113fe5272eb286e` freezes one
standard Ward Creek MTBS map and ten MTBS-only product families. Run
`BL-2026-07-24-ward-creek-reference-request-r001` receives one explicit
16-byte `{"success":true}` queue response. Recipient-withheld request custody
is ignored and no-overwrite. The 3,413-byte tracked request report has SHA-256
`ad8f70ee3cbda8fcff77755486d0cb400a3a5b3c2bc09b99813e8a95abd3d54f`.

Read-only Gmail verification finds one exact `usgs.gov` completion message.
The recipient, message identity, and private HTTPS route remain withheld.
One bounded GET captures one 4,385,952-byte archive at SHA-256
`d94dfb1609c882fdd26119b2be03cea486af1bbb85e4c9607f108f9455f61d18`.
The 16-member archive passes safe-path, encryption, link, exact-roster, and
full-CRC checks.

Terms-first inspection hashes and reads both embedded XML notices before any
raster pixel. They allow bounded use with reasonable acknowledgement and
retain update, location, warranty, and fitness cautions. PRECHECK-2026-076 and
REGISTRY-2026-071 preserve custody and notice identities. The committed native
fitness inspector is the next gate.

Fitness r001 is retained as a failed trace attempt. It generated ignored
outputs with a mistyped 40-character commit and was caught before staging.
The r002 remediation requires the supplied commit to equal repository `HEAD`
before archive or pixel inspection.

Fitness r002 binds source commit
`b5272bc28275b100fa142fee46fec5a2c97576f9`. It passes exact terms,
boundary, analyst-threshold, five-raster, class-domain, optical-custody,
100-percent pair-quality, and nine-window registration gates. Nearest-neighbor
comparison records 19,700 MTBS class-2/3 pixel centers without claiming
resolution gain or creating candidates.

The exact r002 JSON, PNG, and HTML are 62,022, 385,352, and 3,227 bytes. The
PNG passes visual inspection. The owner later confirms that the exact r002 HTML
renders correctly.

Detached replay at exact source `b5272bc...` reproduces HTML and PNG but not
JSON. The only differences are four extraction paths whose values depend on
the checkout and run directory. PRECHECK-2026-078 and REGISTRY-2026-073 retain
the 62,022-byte tracked JSON and 62,086-byte replay JSON as a deterministic
failure. R003 removes only these non-evidentiary paths, uses a new report ID
and no-overwrite output directory, and must pass all three exact-byte replay
gates. Scientific results do not change.

Fitness r003 binds scientific source commit
`b1614fe0260c46570366e6bbe22f74fd24cb7523`. Its exact JSON, PNG, and HTML are
61,346, 385,352, and 3,227 bytes at SHA-256 `f31bc51...`, `7797c384...`, and
`a34b408e...`. A detached replay from a deliberately different extraction
location reproduces all three files byte-for-byte. The r003 PNG is
byte-identical to the owner-confirmed r002 image, and the HTML layout is
unchanged apart from the report image name, run ID, and source-commit text.
That evidence closes the render-equivalence gate without hiding the prior
browser-policy limitation.

The first r003 wheel is retained as a package failure. A runtime-only install
cannot load the new command's help because its wrapper eagerly imports optional
`geopandas`. Wrapper-only commit `0006643...` delays the optional import and
adds regression coverage without changing scientific output. The remediated
946,331-byte wheel has SHA-256 `ca0cdf52...`; a fresh CPython 3.12.10
runtime-only environment passes dependency compatibility, all pinned runtime
packages, and 94 of 94 installed command help checks. Missing optional
geospatial dependencies now produce exact `geo-research` setup guidance. Final
repository verification passes 623 tests, one expected custody skip, 58
existing NumPy deprecation warnings, and 86 subtests.

## Current decision

U01, U02, and U03 dispositions are `pass`. R001, r002, and the first package
attempt remain immutable failure evidence. U04 is now the sole eligible unit
and must establish affirmative background independently of the burned
proposal. No candidate, owner response, label, dataset, split,
baseline, model, metric, GEOINT inference, deployment, or external submission
advances.

## U04 independent affirmative-background evidence

U04 reopens the accepted U03 report and exact optical/MTBS custody before
testing one bounded method. The inherited four-signal near-zero stability
thresholds transfer without tuning. Eligible pixels require seven stable
neighbors of nine and must lie outside the complete native MTBS class 1-6
domain plus 60 meters. No MTBS class, outside-domain state, low change, SCL, or
apparent stability is sufficient alone.

Run `BL-2026-07-24-ward-creek-background-evidence-r001` binds implementation
commit `390231c855fe27f09f1062db847e51f3f99a4e2b`. All nine context registration
windows pass. The exact route has 21,266 pixels / 850.64 hectares in 1,015
components, including 167 components of at least one hectare. It selects no
component and creates no candidate.

Three distinct extraction/output locations reproduce the 27,905-byte JSON,
213,458-byte PNG, and 4,224-byte HTML byte-for-byte. The exact PNG passes visual
inspection. A fresh runtime-only wheel install exercises all 95 command help
paths and supplies explicit `geo-research` guidance. Final repository
verification passes 630 tests, one expected custody skip, 58 existing NumPy
deprecation warnings, and 86 subtests.

Programmatic local HTML navigation is blocked by browser security policy and is
not bypassed. U04 remains `machine-pass-render-pending` until the exact tracked
HTML receives desktop and narrow confirmation. U05 remains closed.

## U04 owner render closure

The owner confirms that the exact tracked 4,224-byte HTML at SHA-256
`f0c4bd76c0f9bc230653f096a3effd5e68125b4cf0ad49d7f5507b45082f4b3b`
renders correctly at both desktop and narrow widths. This resolves the sole
pending U04 gate without changing the exact artifact or machine evidence.

U04 now passes and U05 is eligible but not started. No candidate, owner
decision, label, dataset, split, baseline, or model advances in this closure.

## U05 deterministic two-class proposal

Run `BL-2026-07-24-ward-creek-region-proposal-r001` binds scientific source
commit `5cc266da918862106ed225d52a0f2d809c132954`. It reopens immutable
U03/U04 custody and recomputes all optical, reference, quality, registration,
route, uncertainty, and proposal-time leakage evidence.

The frozen fixed-bin intact-component selector creates exactly `WCP-001`, a
14-pixel burned core with 26 unknown-ring pixels, and `WCP-002`, a 25-pixel
background core with 40 unknown-ring pixels. The burned core is 11 pixels below
the target because it is the nearest eligible intact component. BurnLens does
not clip, pad, merge, or expand it.

Candidate, replay, and tracked runs reproduce the 8,433-byte JSON,
85,888-byte PNG, 5,059-byte HTML, and both TIFFs byte-for-byte. The exact HTML
passes desktop 1280x720 and narrow 390x844 browser checks with no page overflow,
external request, console warning, or console error.

Focused U04/U05 tests pass 12 tests. Final repository verification passes 635
tests, one expected custody skip, 58 existing NumPy deprecation warnings, and
86 subtests. The first two ordinary 965,739-byte wheels differ only in six
distribution-metadata timestamps and remain retained failed attempts.
Fixed-epoch builds are byte-identical at SHA-256
`c9426e77393be91a32991e5997cc94153c3411bab430208d96485276b439d70d`.
A fresh isolated CPython 3.12.10 runtime passes dependency health and all 96
installed command help paths.

U05 passes. U06 may build only one exact blank two-candidate owner
yes/no/uncertain batch. Both candidates remain unreviewed. No owner response,
label, dataset, split, baseline, or model advances.

## U06 exact owner-review handoff

Run `BL-2026-07-24-ward-creek-owner-review-surface-r001` binds source commit
`ba2a482161b5d34f9cef702dce422d888e108b4f`. Candidate, replay, and tracked
runs reproduce all six files exactly. The 18,875-byte HTML has SHA-256
`750d1e67...`; the ordered manifest is `f6db197a...`.

The exact manifest-driven surface contains only `WCP-001` and `WCP-002`.
It discloses the burned core's 14-pixel size, offers one yes/no/uncertain choice
per candidate, and has no bulk, default, prior, confidence, or missing-answer
shortcut.

Real 1280x720 and 390x844 interaction passes both cards, blank state, images,
controls, final summary, export, and locked-browser state. The single automated
export is explicitly software QA, removed from Downloads, and not owner
evidence. The handoff is reloaded blank.

Focused review suites pass 39 tests and 44 subtests. The first runtime probe
retains a transient existing-command timeout; the rerun passes all 97 commands.
One full-suite attempt is retained incomplete after exceeding the prior runtime
inside the existing heavy profile section; it is not called a pass. The
milestone still requires a fresh full suite before PR.

Two fixed-epoch 970,313-byte wheels reproduce at SHA-256 `5b11aa4d...`; a
fresh isolated CPython 3.12.10 runtime passes dependency health and all 97
commands.

U06 is `prepare-pass-owner-response-pending`. BurnLens stops for one exact
completed export. U07 remains blocked. No owner response, label, dataset,
split, baseline, or model advances.

## U06 exact response lock before reveal

BurnLens finds one plausible completed export and no competitor. The
content-withheld validator proves exact surface, ordered-manifest, roster,
candidate-binding, chronology, attestation, completion, and filename-hash
agreement without reading either decision or note.

Run `BL-2026-07-25-ward-creek-owner-response-lock-r001` preserves the exact
1,041-byte payload at SHA-256 `aadd221d...` in ignored no-overwrite custody.
The 2,692-byte private receipt at `dba7d81a...` records that decisions and
notes remain unread. The source bytes rehash unchanged.

U06 exact-byte custody passes at `pass-decisions-unrevealed`. Aggregate
reconciliation may begin only after this tracked lock checkpoint. U07 remains
blocked, and no label or data/model artifact advances.

## U07 owner-response reconciliation and prototype promotion

The locked response contains two yes decisions. BurnLens does not treat either
as sufficient. Final run
`BL-2026-07-25-ward-creek-owner-response-intake-r002` recomputes every exact
source, terms, custody, raster, route, quality, registration, uncertainty,
event-identity, leakage, reproducibility, and both-class gate from implementation
commit `a7f599c`.

Both classes pass together. Ward Creek adds a 14-pixel burned core and a
25-pixel background core; all 66 ring pixels remain unknown and excluded.
`owner-approved-prototype-region-labels-v0.5.0` now contains 14 balanced
regions / 325 core pixels / 13.00 hectares / 599 excluded ring pixels across
seven events.

The required readiness audit passes every required non-count gate while
deferring whole-pool coverage and evaluation design to U08. It records
`training_authorized=false`.

R001 is retained because the real 390 by 844 page overflowed on the long
machine decision. A bounded wrapping fix is committed and pushed before r002.
R002 passes actual 1280 by 720 and 390 by 844 rendering with loaded imagery,
no horizontal overflow, responsive metrics, contained decision text, and zero
browser warning/error logs. Focused intake tests pass 8 of 8.

U07 passes. U08 is eligible to construct the exact McKay, Tepee, Green Ridge,
Grandview, Windigo, and Ward Creek candidate, exclude Darlene, and rerun every
Phase Two sufficiency gate. No dataset, split, baseline, model, metric,
training authorization, inference, deployment, or external submission
advances.

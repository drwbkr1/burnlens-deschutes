# P2O4-T08 - First Returned Response Lock

## Entry metadata

| Field | Value |
|---|---|
| Local date | 2026-07-16 |
| Task | P2O4-T08 - preserve, operator-lock, and publicly attest the first returned response |
| Issue / branch | #384 / `codex/p2o4-t08-first-reviewer-response-lock` |
| Base | `6b13c4f5f128615f42b8897e9c6d8442446a86a2` |
| Software | BurnLens `0.16.0` |
| Generator source | `ec41129f9322022f28b8f788a2e08ae22145471b` |
| Candidate public artifacts | `9fbd97fcb66fd76172fff949580f469fc43b3f40` |
| Public-QA run | `BL-2026-07-16-label-review-response-lock-qa-r001` |
| Shipment state | Candidate; PR, merge, tag, and fresh merged-main verification pending |

## Cycle-start finding

The exact v0.15.0 reviewer workflow and browser path passed. One real completed response then arrived. The highest-leverage weakness was no longer interface behavior but pre-reveal evidence custody: the exact external bytes needed immutable local preservation, a private receipt, and a public proof that did not disclose first-pass content before a second reviewer.

## Implementation

- Recomputed the external file at 16,443 bytes / SHA-256 `485e80f2563f8b723c16daa8b5e5a1172dd88e8f2a28a7dd8608c933fa64eed9`.
- Preserved an exact ignored copy under repository-local reviewer-response storage without modifying the external source.
- Created and preserved an ignored 2,508-byte private receipt / SHA-256 `67599f794a1310e9523cded095787c918ed47d88c439e240a0b41ea6e5eb9835`.
- Kept both private files ignored and untracked; the reveal remains operator-declared unopened.
- Added a public verifier independent of the lock builder. It validates the packet, completed response contract, exact expected bytes and hashes, private receipt schema and binding, origin classification, chronology, and downstream-version nulls.
- Added JSON, semantic HTML, and rendered PNG outputs that publish only bounded completeness and cryptographic evidence.
- Added fail-closed tests for response drift, receipt drift, overwrite, content leakage, verifier independence, versioning, packaging, and rendering.

## Candidate validation

- The exact private response and receipt match their operator-supplied byte counts and SHA-256 digests.
- All 56 response units and the proposal-free contract pass.
- The private receipt binds the same response, packet, issue, reviewer slot, evidence origin, and chronology.
- Public privacy audit finds zero forbidden response keys, sensitive-value leaks, private filename leaks, or private path leaks.
- Semantic HTML contains the decision, 56-unit completion, one-of-two response state, reveal withholding, and no external resource.
- Original-resolution 1800 by 1320 PNG is readable.
- All 158 repository tests pass; compileall and dependency health pass. One known NumPy 2.5 deprecation warning remains in historical label-proposal QA.
- Two detached-source fixed-epoch wheels are byte-identical at 292,068 bytes / SHA-256 `c6a0f320b393ea7aca3aebdc93da97f7ed34901d30df298c1986d1ee4b78ee28`.
- Isolated install reports BurnLens `0.16.0`, 26 entry points, the new public-QA CLI, and the packaged browser controller.
- No deployed BurnLens application exists.

## Evidence separation and next gate

The public report intentionally withholds the first response’s label, sufficiency, confidence, and reason distributions; reviewer experience; timestamps; notes; and private filenames/paths. Software does not qualify the reviewer or verify the reveal history. Obtain and lock a second qualifying response before comparison, reveal, adjudication, label acceptance, dataset candidacy, or partition work.

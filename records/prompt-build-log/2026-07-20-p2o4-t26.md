# P2O4-T26 Prompt / Build Log - Green Ridge Owner Response Intake

**Issue / branch:** #491 / `codex/p2o4-t26-green-ridge-owner-response-intake`

**Intake source:** `78368833a73401e098af21bf999ab6674d2e8f9f`

**Run:** `BL-2026-07-20-green-ridge-owner-response-intake-r001`

**Next issue:** #495

The cycle begins from verified v0.37 and the real owner-operated workflow. Before decisions are opened, BurnLens validates the newest 893-byte export's hash-named filename, exact surface/run/proposal/raster bindings, UTF-8 structure, completion, attestation, timestamps, and two-record contract. It then preserves exact response and receipt bytes in ignored repository-local no-overwrite custody. The private reconciliation is separately hash-bound and ignored.

After custody passes, the response contains two yes decisions. Each exact 25-pixel core re-passes proposal identity, raster bytes, CRS/grid/nodata/domain, connectivity, one-pixel unknown-ring, resolved source/terms, quality, registration, uncertainty, and event-identity gates. One burned and one background region join `owner-approved-prototype-region-labels-v0.2.0`.

The cumulative set is four burned and four background prototype regions, 186 core pixels / 7.44 ha, and 333 excluded unknown-ring pixels across four events. The public JSON/HTML/PNG contains aggregate counts and exact bindings but no notes, unit decisions, private paths, or filenames. Original-resolution PNG inspection and 299 tests pass. The first clean branch clone reconstructs all three outputs but correctly fails its tracked-JSON hash because the new nested intake path lacks an LF checkout rule. The candidate adds that explicit nested JSON/HTML contract; the corrected clean clone then reproduces all three outputs byte for byte.

Two independent corrected clean-branch builds at fixed `SOURCE_DATE_EPOCH=1784569487` produce byte-identical 552,457-byte wheels with SHA-256 `ea80b167b29064fb6f53dd5971acf4289cf1e7e2616c9a6abe8d62616b70e29e`. The wheel contains 123 entries, zero private/download/build entries, and 59 console commands. A dependency-complete isolated install reports BurnLens 0.38.0, exact pinned dependency versions, a clean `pip check`, and passing help for both new Green Ridge response commands.

No dataset, split, baseline, model, metric, accuracy, independent-ground-truth, inter-rater, field, official, endorsed, operational, or emergency-ready claim is created. Issue #495 owns Grandview native source fitness as the next bounded route toward the six-event outcome.

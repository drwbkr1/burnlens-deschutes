# P2O2-T02 prompt/build record - atomic exact-pair intake

## Identity

- Date: 2026-07-13 local / 2026-07-14 UTC
- Issue: #325
- Branch: `codex/p2o2-t02-intake-transaction`
- Base commit: `a3df1cac8d859b27f44d52ef177b5e937dc5fc99`
- Repository version: `v0.3.0-intake-transaction-baseline`
- Package version: `0.3.0`
- Contract: `paired-intake-contract-v0.4.0`
- Phase outcomes served: Phase Two Objective Two source-stack fitness and Objective Three immutable intake boundary
- Pull request: #326
- Checkpoint merge commit: `ee1a1d678ad888b595dc3c7b215f787ea5156582`
- Lifecycle sync issue: #327

## Cycle finding

The merged toolchain still reproduced its access and AOI outputs exactly, and provider intake remained owner-credential blocked. The highest-leverage zero-secret weakness was transactional: BurnLens could validate individual responses, but no executable gate prevented a partial Sentinel/fire/geolocation set from entering raw registration.

## Bounded improvement

- Pin one exact three-asset package contract with current public provider metadata.
- Fail closed on missing, unexpected, renamed, size-mismatched, malformed, corrupt, unsafe, checksum-invalid, pair-mismatched, or destination-colliding input.
- Record local SHA-256, MD5, and BLAKE3 only after the complete package passes.
- Write registration provenance before one same-filesystem atomic directory promotion.
- Exercise partial rejection, checksum-tamper rejection, successful complete promotion, and cleanup using temporary synthetic fixtures.
- Render the real credential-blocked state separately from synthetic transaction proof.

## Primary sources reviewed

- Copernicus Data Space Ecosystem OData documentation and public product metadata
- Copernicus Data Space Ecosystem token guidance
- NASA CMR UMM metadata for both exact NOAA-21 pair members
- NASA Earthdata Login data-access and application-authorization documentation
- PyPI project metadata for `blake3==1.0.9`

Exact identities and findings are recorded in `ACCESS-2026-005`, `PRECHECK-2026-005`, and `PAIRED_INTAKE_TRANSACTION_DECISION.md`.

## Verification before PR

- Existing merged baseline: 16 tests pass; access and AOI report rebuilds remain byte-identical.
- Candidate: 37 tests pass; `compileall` passes; isolated install reports BurnLens `0.3.0`, BLAKE3 `1.0.9`, Pillow `12.2.0`, and no broken requirements.
- CLI help passes.
- Rehearsal run: `BL-2026-07-14-paired-intake-rehearsal-r001`.
- Full contract SHA-256: `5135b6b0b554e533df98ede568b1eafbd45c692b73a1e1abd3e50ba098f0958d`.
- JSON SHA-256: `94e311fd608f9c10e024138d9eff6abf0f70187a69c031264e91cb8d9d1af234`.
- HTML SHA-256: `b76cbf50f60dd112430616b4a472ac440444cbf48194a0672df91876e78ea20c`.
- PNG SHA-256: `c38bf7fc825dd780affe3f8d1080cffb3bdb90ef2164cb27b1295b4e54bbfcd0`.
- A second clean run reproduces all three files byte for byte.
- Original-resolution visual review passes: real blocked state, zero-provider metrics, exact asset rows, synthetic-only proof, limitations, and warning are legible and unambiguous.
- Provider assets/bytes, credentials, retained synthetic bytes, datasets, labels, baselines, and models: zero.

## Pre-shipment transparency correction

An additional cycle audit found that report schema `0.1.0` tied its `public_metadata_refresh.performed_at_utc` field to the caller-supplied run time even though the deterministic rehearsal performs no provider request. Schema `0.2.0` replaces that field with a fixed `public_metadata_snapshot.observed_at_utc`, a false `live_refresh_performed_by_this_run` flag, and the `ACCESS-2026-005` evidence record. A new regression test proves a 2030 run time cannot inflate the 2026 observation.

A subsequent filesystem audit found that ordinary path checks followed symlinks and allowed multiply-linked files, which could make registered raw bytes alias mutable storage outside quarantine. The validator now rejects link-like quarantine/destination paths and linked asset files before hashing. The contract also hashes the complete invariant list together with the three asset records; a regression test proves an invariant change changes the digest.

A forced atomic-rename failure then exposed that the provisional registration manifest remained in quarantine and blocked a safe retry. Contract `v0.3.0` rolls that manifest back on rename failure. The new test verifies the quarantine remains valid, no destination appears, and a subsequent ordinary promotion succeeds.

The last immutability audit added registered-package verification. Contract `v0.4.0` binds registration schema `0.2.0` to the full contract digest and exact asset hashes, then re-runs container and current-byte checks on demand. The rehearsal now mutates a promoted synthetic file without changing its size and renders a fourth passing check when the verifier rejects it.

## Decision and handoff

Accept the transaction contract as pre-intake reliability evidence, not source fitness. The rendered package remains a truthful historical pre-authorization run. On 2026-07-14 the owner subsequently authorized both provider credentials and reported that their machine-bound setup passes; `ACCESS-2026-006` records that fact without secret material. No credential was loaded and no provider request or byte was added by this synchronization. After lifecycle sync issue #327 ships, use this exact contract for one new issue-backed acquisition and inspection checkpoint.

## Shipment

PR #326 merged at `ee1a1d678ad888b595dc3c7b215f787ea5156582`, closing issue #325. The annotated `v0.3.0-intake-transaction-baseline` tag object `b13a7107241ac2f52e7f1a945ed95c3b38298a59` remotely dereferences to that exact commit. All 37 tests, compilation, dependency health, deterministic reconstruction, rendered review, and repository/secret checks passed before merge; all 37 tests, compilation, and dependency health passed again from merged `main`. Issue #327 records the lifecycle-only synchronization before credentialed intake begins.

# P2O4-T25 Prompt / Build Log - Green Ridge Owner Review Surface

**Issue / draft PR / branch:** #487 / #492 / `codex/p2o4-t25-green-ridge-owner-review-surface`

**Generator:** `994f0b186d0cd3fce6cdc0146f6d7ddb0b675e10`

**Artifacts:** `6da33ff846a55fc60e27c1f39d4d7a0488bbff81`

**Run:** `BL-2026-07-20-green-ridge-owner-review-surface-r001`

**Next issue:** #491

The cycle begins from lifecycle-synchronized v0.36 main and reruns the current proposal on exact ignored custody inputs. JSON, HTML, PNG, and both candidate rasters reproduce exactly before surface generation. No fresh external research is needed because no source, terms, access, safety, or provider claim changes.

The candidate surface binds both exact raster hashes and renders five evidence views per question. It discloses proposition rules and limitations before blank yes/no/uncertain controls, preserves gold ring pixels as unknown, provides hash-named LF exports, validates exact bindings on reload, clears stale choices, requires attestation, locks completed sessions, and states a no-overwrite custody contract.

Six outputs reproduce byte for byte. Original-resolution evidence inspection, controller syntax, 291 tests, compilation, 98 JSON parses including the candidate manifest, and diff checks pass. The first working-tree wheel is rejected after a fresh clone exposes CRLF source-byte drift. Two independent fresh-checkout fixed-epoch wheels are identical at 540,356 bytes / SHA-256 `6d3137454f521221668986ec26c65b03d74e68fbab8db2ff3caf7c5a60c8a917`; each contains 120 clean entries and 57 commands. Dependency-complete isolated installation, `pip check`, version 0.37.0, and CLI help pass. Earlier deliberately dependency-free probes failed closed on missing packages and are not package defects.

The in-app browser rejects automated inspection of the local file URL under its security policy, and Codex did not bypass that boundary. The owner then operated the rendered local surface and produced one completed, attested, hash-named two-record export. BurnLens independently verifies its filename hash plus surface, run, schema, completion, attestation, and record-count bindings while deliberately leaving decision values unopened until issue #491 can preserve the exact bytes. This is direct evidence that the owner completion/export path works. Mobile automation is deferred as nonessential to this owner-only offline surface; that task-order adjustment does not change a phase outcome or public-use boundary.

Zero owner responses, labels, dataset, split, baseline, model, metric, accuracy, independent-ground-truth, inter-rater, field, official, endorsed, operational, or emergency-ready claims are created. Issue #491 owns later exact response intake only after release verification.

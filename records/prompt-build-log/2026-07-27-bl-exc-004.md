# BL-EXC-004 Phase Six fresh-main fixture

Date: 2026-07-27

Issue: #580

Branch: `codex/bl-exc-004-phase-six-fresh-main-fixture`

Run: `BL-2026-07-27-bl-exc-004-fresh-main-fixture-r001`

## Trigger

P6O1-T01 merged through PR #579 at `35e68a5...` with exact reviewed-tree
equality. Fresh main started clean, but its default lean environment had no
pytest. After the locked `dev` profile was installed, five Phase Six tests
failed before application execution because the ignored `downloads/` parent
does not exist in a fresh checkout.

This is an urgent-correctness exception because the milestone could not claim
fresh-main verification while its own focused roster required undeclared local
state.

## Remediation

Both Phase Six test classes now create `ROOT / "downloads"` during class setup.
The directory remains ignored and is used only as a parent for self-cleaning
temporary directories. No application, package, model, data, or scientific
byte changes.

## Verification

- canonical focused Phase Six tests: 13 pass;
- expected pre-commit checkout-byte attempt: 14 pass / one fail;
- clean remote exception clone at `0ea5543...`: 15 pass;
- both Phase Six candidate validators: pass;
- structured release audit: `verified`, zero warnings;
- locked dev dependency health: 18 distributions compatible;
- candidate ZIP remains 14,963,469 bytes / `5a314b69...`.

## Decision

`pass-pending-exception-pr-merge`

After one reviewed exception merge and exact fresh-main repetition, BurnLens
may return to the owner publication stop gate. No tag, GitHub Release,
deployment, public-sharing change, publication, or external submission is
authorized here.

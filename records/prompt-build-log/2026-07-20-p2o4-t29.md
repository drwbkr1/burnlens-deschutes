# P2O4-T29 Prompt / Build Log - Grandview Background Evidence

**Issue / branch:** #503 / `codex/p2o4-t29-grandview-background-evidence`

**Start checkpoint:** verified `v0.40.0-grandview-reference-fitness`; lifecycle main `c08c861cb73c5ca8ebfa711f240426b413fe48f6`

The cycle first runs the actual v0.40.0 report and reconstructs all three outputs byte for byte. It confirms that affirmative background evidence, rather than optical or MTBS fitness, is the highest-leverage weakness.

Fresh official CDSE STAC/OData research freezes one 2022-06-28 Sentinel-2B, 10TFQ, relative-orbit-113 scene with full 3 km context coverage and 0.169377% product cloud metadata. The product is baseline 05.10 rather than the original pair's 05.00. A fail-closed 950,987,607-byte OData contract plus current Sentinel terms are committed before one protected credential-backed acquisition. Exact provider hashes and local SHA-256 pass; all provider bytes remain ignored.

Real execution exposes and resolves three bounded failures without relaxing gates: expected processing baseline becomes explicit per source; registration is measured on the complete event boundary because the larger context contains legitimate tile-edge nodata; and two review-needed registration windows become spatial exclusions. A first route using BAER continuous-raster validity rejects itself because the raster is rectangular. The corrected implementation parses and rasterizes the exact delivered BAER/MTBS/RAVG polygon vectors.

Generator `08e04b71836ffbcb8d59761c53586ece6f85687f` creates run `BL-2026-07-20-grandview-background-evidence-r001`. Seven registration windows pass, two are excluded, none fail, and p95 residual is 0.0944 pixel. The unchanged stability protocol plus vector union and 60 m buffer yields 67,782 evidence pixels / 2,711.28 ha and 181 components at least one hectare. JSON/HTML/PNG are 29,336 / 4,248 / 497,800 bytes and reconstruct exactly. Zero candidates, owner responses, labels, dataset, split, baseline, or model are created. Release verification remains pending.

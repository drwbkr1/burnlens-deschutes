# P2O4-T24 Prompt / Build Log - Green Ridge Region Proposal

**Issue:** #483

**Branch:** `codex/p2o4-t24-green-ridge-region-proposal`

**Generator:** `690137f1530a00ca79b8f5831f700541809b2a2b`

**Artifacts:** `1858a7a945957fd0cc3d123b325529f9931806c3`

**Run:** `BL-2026-07-20-green-ridge-region-proposal-r001`

The cycle begins by rerunning verified v0.35 from lifecycle-synchronized main. It confirms 26,126 eligible background-route pixels, 128 hectare-scale components, and zero candidates. The existing region-candidate design supplies the bounded next move: fixed 0.05 dNBR evidence partitions, intact eight-connected components, a 25-pixel ranking target that never clips, deterministic hash tie-breaking, and one-pixel unknown rings.

The actual run proposes one burned and one background region. Each core has 25 20 m pixels / 1.0 ha. Their unknown rings contain 50 and 37 pixels. Both exact EPSG:32610 rasters encode 0 outside, 1 core, 2 ring, nodata 255, and tags that explicitly record `label_created=false`.

JSON, HTML, PNG, and both rasters reproduce byte for byte. Original-resolution visual inspection, raster domain/grid/tag inspection, 287 tests, compilation, 94 JSON parses, and diff checks pass. Two fixed-epoch wheels are byte-identical at 531,038 bytes / SHA-256 `bd9c26bc985b85eb021704c3a525b72b3403ceabc34b08ae59107489c3808321`; each has 118 clean entries and 56 commands. An isolated BurnLens 0.36.0 install and dependency check pass. PR, merge, fresh-main, and tag gates remain pending.

Issue #487 owns a separately versioned blank owner yes/no/uncertain surface. This checkpoint creates zero owner responses or labels and no dataset, split, baseline, model, metric, accuracy, ground-truth, field, official, endorsed, or operational claim.

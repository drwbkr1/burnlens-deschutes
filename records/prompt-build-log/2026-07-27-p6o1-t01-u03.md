# P6O1-T01-U03 repository-owned portfolio surface

**Issue:** #578

**Branch:** `codex/p6o1-t01-baseline-first-publication-candidate`

**Exact base:** `86919b141118fb37366982e4c7e02e2f4fe75c19`

**Production run:** `BL-2026-07-27-p6o1-t01-u03-portfolio-surface-r002`

**Production source:** `4e673166b1c8b3cecdadc94ccf0fa5611be58e20`

## Prompt intent

Turn the passed U02 narrative into one deterministic, offline, accessible
repository-owned surface that wraps exact evidence. Keep RBR primary, the
U-Net rejected, WCP-002 visible, source precedence explicit, and every public
action closed.

## Implementation and retained attempts

Commit `2889d6e...` adds the generator, validator, CLI, and five focused tests.
Its staged diff check reported three generated-README trailing spaces, but the
first wrapper continued into the commit. Corrected descendant `d621632...`
normalizes the source.

Rendered draft `r001` passes visual, layout, link, and scroll checks but leaves
keyboard focus on the document body after its skip link. Those exact
reconstructed bytes remain in the U03 record. Commit `4e67316...` makes the
main landmark natively focusable. Final `r002` receives a fresh run ID.

## Production result

The final surface has three files:

- `index.html`: 14,798 bytes / `f0fb4b57...`;
- `MANIFEST.json`: 6,247 bytes / `31c68cd8...`;
- `README.md`: 859 bytes / `c8cee600...`.

The machine-readable U03 record is 6,296 bytes / `93339c74...`.

It is static and script-free, makes no external request, and exposes the exact
accepted RBR result, rejected U-Net comparison, WCP-001/WCP-002 observations,
evidence chain, limitations, known issues, trace, attribution, and public
boundary.

## Real rendered QA

Microsoft Edge returns HTTP 200 at 1440x1000 and 390x844. Both viewports have
zero horizontal overflow, console errors, or page errors. The page has zero
scripts and external links. All nine repository evidence targets return 200.
Keyboard first focus is the skip link; activation scrolls to and focuses
`MAIN#main`. Browser and loopback server are stopped. No screenshot is
retained.

## Disposition

U03 passes for local pre-publication use. It changes no analytical, dataset,
split, label, source, baseline, model, or immutable Phase Four/Five package
byte. U04 may add bounded demo, presentation, citation, and maintenance
materials. Public action remains closed.

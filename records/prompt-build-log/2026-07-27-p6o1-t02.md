# P6O1-T02 owner-authorized publication and Codex Sites closeout

Date: 2026-07-27

Issue: #584

Branch: `codex/p6o1-t02-sites-publication-closeout`

Runs:

- `BL-2026-07-27-p6o1-t02-u01-publication-authorization-r001`
- `BL-2026-07-27-p6o1-t02-u02-sites-source-r001`

## Authorization and boundary

The owner explicitly authorized the pending release actions and directed
submission through Codex Sites. Work stays strictly inside
`C:\Projects\Active\burnlens-deschutes`; the separate `burnlens-site`
repository is not read, used, or changed.

RBR remains the accepted bounded method. The exact trained U-Net remains a
reproducible rejected diagnostic and did not outperform RBR. This checkpoint
changes no label, dataset, split, AOI, source raster, threshold, weight,
analytical raster/vector, metric, or scientific conclusion.

## Work and primary-source research

The checkpoint:

1. reran the exact candidate validators, release audit, dependency health, and
   focused entry tests at verified main `55ba588...`;
2. rechecked the current official Copernicus Sentinel legal notice, USGS The
   National Map terms, and MTBS product/method pages on 2026-07-27;
3. created a repository-owned Vinext Sites source with exact source attribution,
   public limitations, lineage, baseline/model separation, and evidence routes;
4. bound 13 public evidence files to canonical tracked sources by exact bytes
   and SHA-256;
5. removed unused starter database/lint dependencies and remediated the initial
   dependency audit to zero known vulnerabilities;
6. passed build, Node render/binding tests, Python binding/privacy tests,
   desktop and narrow Chrome rendering, keyboard-first skip navigation, and
   the exact Ward Creek rejected-model diagnostic interaction.

## Retained failures

The local `vinext start` wrapper served HTML but 404ed generated CSS and
JavaScript assets. That route is rejected; local browser QA uses the working
Vinext development render, and production must pass on the actual Codex Sites
deployment. Two allowed bespoke social-card attempts also failed their local
text/render gate, so the site deliberately omits `og:image`.

## Current disposition

U01 passes. U02 is `pass-local-pre-publication`. No tag, GitHub Release, saved
Sites version, deployment, public URL, or closeout claim exists at this branch
checkpoint. The next dependency is the reviewed milestone merge, followed by
exact release/deployment creation and production verification.

## Do not carry forward

Do not carry forward the rejected `vinext start` preview as production
evidence, either failed social card, a model-superiority claim, an
independent-ground-truth claim, or any private/provider/custody path.

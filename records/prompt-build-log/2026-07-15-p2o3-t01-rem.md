# P2O3-T01-REM prompt/build record - registration LF reconstruction

## Identity

- Date: 2026-07-15
- Issue: #349
- Branch: `codex/p2o3-t01-registration-line-endings`
- Base / analytical merge: `c01cdb12033e7a9440ad0502b92a8887fd79ed1d`
- Remediation source: `d2c90f47e473d74638300856a3b3150aa169e579`
- Released tag: `v0.8.0-content-registration-baseline`

## Failure preserved

Clean merged-main verification after PR #348 passed 93 tests, compilation, dependency health, exact registered-pair verification, and PNG reconstruction. The committed JSON and HTML working-tree files hashed differently from fresh generator output. Inspection found 1,302 and 19 CRLF sequences respectively in the Windows checkout, while reconstructed outputs had zero. Ignoring end-of-line whitespace produced no content diff.

The `.gitattributes` contract covered target and optical evidence but omitted the new registration paths. The artifact generator and stored Git blobs were already LF; cross-platform checkout was not controlled. The release tag was not created.

## Bounded correction

- Add `text eol=lf` for `samples/registration/phase-two/*.json` and `*.html`.
- Add regression coverage that requires both exact attribute rules.
- Change no generator, algorithm, measurement, artifact blob, hash, decision, source, AOI, or claim.

## Fresh-clone verification

A no-hardlink clone inside ignored repository `build/` checked out remediation source `d2c90f47e473d74638300856a3b3150aa169e579`. Both evidence text files contain zero CRLF sequences and retain their exact recorded hashes. From that clone, using the exact ignored raw package and frozen run arguments, JSON, HTML, and PNG reconstruct byte for byte:

- JSON `f0682f51bebe93d970774748c23317d8759bae4b7e34478fb6659d9606b28645`;
- HTML `b8ed29f1f18acc75275e0cbef0fd6d0b1d6764ef2737d1f5e53bb45d5fcd407f`;
- PNG `70ba55e6a15f2b712bd167e8d3d0736daf19284bae0f3f0f721a07a3e1c017f1`.

All 94 tests and compilation pass in that fresh clone. The local clone and reconstruction remain ignored and will be removed after release verification.

## Boundaries and handoff

No source access, credential, provider transfer, raw-byte publication, label, dataset, split, baseline, model, metric, application, deployment, or public-sharing change occurred. Merge this remediation, repeat the fresh merged-main gate, then create and independently dereference the annotated tag only if every check passes.

## Merge and release verification

PR #350 merged issue #349 at `1297471be45200c40f9f40746e85b437ce6e0c0d`. A second no-hardlink clone from that merged `main` checked out both text artifacts with zero CRLF sequences and reconstructed JSON, HTML, and PNG byte for byte. All 94 tests, compilation, dependency health, wheel construction, isolated import, and tracked-download exclusion passed. The merged-main wheel is 115,120 bytes with SHA-256 `98100911ab3b75cd38aac740e1ffe7cecf212b9164f8a1000f2ccf4072dfd497`; package and registration software versions both import as `0.8.0`.

Annotated tag object `14edfad3ce89dbd9179a54eb1e29811e41d258c0` was pushed and independently read back from the remote. It peels exactly to remediation merge `1297471be45200c40f9f40746e85b437ce6e0c0d` as `v0.8.0-content-registration-baseline`. No analytical artifact changed during remediation or release.

# P3O1-T01 — bounded rejection-first U-Net

Issue #566 begins from exact verified lifecycle base
`c290e90c76e29c2bd33423f5c51878c950412b1d` on
`codex/p3o1-t01-bounded-unet-milestone`. It executes one coherent six-unit
milestone: environment lock, reference implementation, protocol freeze,
single substantive training/checkpoint selection, one sealed test opening, and
same-environment replay/package/decision.

## U01 — environment and source lock

Run `BL-2026-07-25-p3o1-t01-u01-environment-r001` adds an opt-in
`model-research` profile with exact `torch==2.13.0` and
`setuptools==82.0.0`, a Windows CPU Codex selector, an offline deterministic
smoke, focused tests, and operator documentation. Implementation commit
`ddf32dbfee5a29e0fe362859c456e7a362fee20c` is pushed remote-equal.

The fresh detached worktree installs 28 distributions from the exact
259,657-byte lock at SHA-256 `87afed69...`, passes dependency integrity,
verifies all 105 installed command launchers, and runs PyTorch `2.13.0+cpu`
with CUDA absent, deterministic algorithms enforced, and both thread pools set
to one. The synthetic finite convolution replay is exact at SHA-256
`0640f0d2...`.

The audit records one setuptools advisory and zero torch findings. Its path is
inapplicable under the current Windows/no-`MANIFEST.in`/no-sdist boundary and
becomes a stop trigger if that boundary changes. U01 does not claim a clean
vulnerability slate.

The retained first combined validation wrapper timeout is not rewritten as a
pass. Bounded reruns and the fresh detached reconstruction pass. U01 hashes the
frozen Phase Two inputs but opens no dataset array and creates no model,
weights, training, test evaluation, inference, deployment, or scientific
claim. The next eligible unit is U02.

## U02 — reference implementation

Cumulative source `552f47d629f275fc732a36d9d7362b3e3ffc40a8`
implements the exact 117,473-parameter six-channel U-Net plus the
manifest-bound train/validation loader, train-only normalization, exact binary
loss mask, masked BCE, finite checks, Adam, early stopping, no-overwrite
checkpoint recovery, deterministic controls, masked metrics, and fail-before-
array test lock.

The first focused suite retains one incorrect architecture-test expectation;
the passing revision checks exact primitive layer counts and parameters. The
first implementation commit also retains one EOF formatting warning, corrected
by the cumulative source commit. A fresh locked model worktree verifies 105
commands and passes 24 reference/model-readiness/baseline tests.

Synthetic one-step training replays the same loss and state SHA-256 twice. The
permitted real smoke uses only four train and four validation patches, performs
one ephemeral train step and a forward-only validation pass, and reruns cleanly
with warnings treated as errors. No checkpoint or weights are promoted, no
substantive training occurs, and sealed Ward Creek/Windigo arrays remain
unopened. U03 is next.

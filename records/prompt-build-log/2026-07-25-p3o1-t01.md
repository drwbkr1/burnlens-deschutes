# P3O1-T01 — bounded rejection-first U-Net

Issue #566 begins from exact verified lifecycle base
`c290e90c76e29c2bd33423f5c51878c950412b1d` on
`codex/p3o1-t01-bounded-unet-milestone`. It executes one coherent six-unit
milestone: environment lock, reference implementation, protocol freeze,
single substantive training/checkpoint selection, one sealed test opening, and
same-environment replay/package/decision.

## U01 — environment and source lock

Run `BL-2026-07-25-p3o1-t01-u01-environment-r001` adds an opt-in
`model-research` profile with exact `torch==2.13.0` and
`setuptools==82.0.0`, a Windows CPU Codex selector, an offline deterministic
smoke, focused tests, and operator documentation. Implementation commit
`ddf32dbfee5a29e0fe362859c456e7a362fee20c` is pushed remote-equal.

The fresh detached worktree installs 28 distributions from the exact
259,657-byte lock at SHA-256 `87afed69...`, passes dependency integrity,
verifies all 105 installed command launchers, and runs PyTorch `2.13.0+cpu`
with CUDA absent, deterministic algorithms enforced, and both thread pools set
to one. The synthetic finite convolution replay is exact at SHA-256
`0640f0d2...`.

The audit records one setuptools advisory and zero torch findings. Its path is
inapplicable under the current Windows/no-`MANIFEST.in`/no-sdist boundary and
becomes a stop trigger if that boundary changes. U01 does not claim a clean
vulnerability slate.

The retained first combined validation wrapper timeout is not rewritten as a
pass. Bounded reruns and the fresh detached reconstruction pass. U01 hashes the
frozen Phase Two inputs but opens no dataset array and creates no model,
weights, training, test evaluation, inference, deployment, or scientific
claim. The next eligible unit is U02.

## U02 — reference implementation

Cumulative source `552f47d629f275fc732a36d9d7362b3e3ffc40a8`
implements the exact 117,473-parameter six-channel U-Net plus the
manifest-bound train/validation loader, train-only normalization, exact binary
loss mask, masked BCE, finite checks, Adam, early stopping, no-overwrite
checkpoint recovery, deterministic controls, masked metrics, and fail-before-
array test lock.

The first focused suite retains one incorrect architecture-test expectation;
the passing revision checks exact primitive layer counts and parameters. The
first implementation commit also retains one EOF formatting warning, corrected
by the cumulative source commit. A fresh locked model worktree verifies 105
commands and passes 24 reference/model-readiness/baseline tests.

Synthetic one-step training replays the same loss and state SHA-256 twice. The
permitted real smoke uses only four train and four validation patches, performs
one ephemeral train step and a forward-only validation pass, and reruns cleanly
with warnings treated as errors. No checkpoint or weights are promoted, no
substantive training occurs, and sealed Ward Creek/Windigo arrays remain
unopened. U03 is next.

## U03 — frozen protocol and rendered preflight

Source `fbb2e923ae7f9ca9ed7dbb317e4235a236ae2411` freezes the
exact environment, data roles, one-model config, seed, optimizer, loss,
stopping and selection order, compute budget, retention paths, required U04-U06
artifacts, and the only authorization-bound mechanism that can open the sealed
test in U05.

Run `BL-2026-07-25-p3o1-t01-u03-preflight-r001` performs exactly two
train/validation-only epochs. It uses four train patches / 109 cores and four
validation patches / 89 cores, renders the complete history, retains no weights
or checkpoint, and leaves the Ward Creek/Windigo test roster unopened. The
protocol and JSON/HTML/PNG outputs reproduce byte-for-byte in a fresh detached
worktree.

The exact loopback HTML passes desktop and 390-by-844 layout inspection with no
horizontal overflow, external resource, or browser log. The in-app browser
timed out while capturing the narrow screenshot, so that width is verified by
rendered DOM dimensions and content rather than an image capture. Direct local
file navigation was policy-blocked and not bypassed.

The first U03 suite retains one brittle prose-oracle failure; the corrected
suite passes. Fresh detached verification passes 28 focused tests, compilation,
lock freshness, and diff checks. No substantive model, candidate checkpoint,
test metric, inference, or model-value claim exists. U04 is next and may run
exactly one substantive training experiment under the frozen protocol.

## U04 — one substantive run and validation-only candidate

Source `5179a745f091c29d095461d511633f055967ef91` adds the bounded
training harness without changing the frozen model, optimizer, loss, selection,
early-stopping, threshold, data-role, or one-test-opening contract. The first
focused suite retains five pre-training failures: two stale synthetic fixtures
and three checks against a stale editable install. Corrected fixtures plus an
exact locked-environment resync pass 29 tests with one expected geo skip. A
fresh detached model environment then passes 35 focused tests, 107/107 command
checks, deterministic CPU smoke, compile, lock, diff, and real
train/validation entry.

Run `BL-2026-07-25-p3o1-t01-u04-training-r001` is the only substantive
experiment. It stops after 35 append-only epochs and selects epoch 10 from
validation only. The selected train masked BCE is `0.6315077543258667`;
validation masked BCE is `0.6219634259684702`; event-class macro Dice is
`0.7253521126760564`; macro IoU is `0.7051282051282051`; and worst-event
macro Dice is `0.4507042253521127`.

The aggregate conceals an important event failure. Grandview's balanced 50
cores are classified exactly, while McKay's selected checkpoint predicts all
39 cores as burned and misses all seven background cores. That result remains
validation evidence, not a test or generalization claim.

The ignored run retains 43 files / 3,592,037 bytes with canonical inventory
SHA-256 `e260d8b98b20b523f7b636bb95a179a13addfb7441f853bdeebaa1b7d2f54b19`.
All nine promoted artifacts, 35 epoch records, selection/stopping result, and
reloadable checkpoint/weights relationships pass independent verification. The
exact rendered surface passes direct desktop and narrow inspection with a
contained horizontally scrollable table; the browser tool did not complete a
horizontal-scroll gesture, so that interaction is retained as a tool limit.

U04 freezes `burnlens-unet-binary-v0.1.0` as a candidate only. Ward Creek and
Windigo remain unopened. No accepted/released model, test metric,
hyperparameter search, second model, inference, deployment, or model-value
claim exists. U05 becomes eligible only after these U04 bytes are committed and
pushed.

## U05 pre-open — evaluator and no-retry mechanism frozen

Source `aa6b7f385224943a4550657318117dbec1b038c2` freezes the one
test-evaluation path before authorization. It hardens the exact patch roster,
requires the clean exact HEAD and every candidate binding, rejects a consumed
opening ID across ignored and tracked receipts, and produces the complete
aggregate/event/class, BCE, precision/recall, area-difference, threshold,
probability-status, baseline-comparison, prediction-array, geospatial-render,
and opening-receipt evidence required by issue #566.

The first suite retains an import-path collection failure with zero array
access. Four validation-only renderer iterations retain and correct narrow
overflow, overlapping annotations, and false test-opening wording. Exact
preflight `BL-2026-07-25-p3o1-t01-u05-preflight-r005` passes 39 focused
tests, compile/lock/diff, desktop and narrow render checks, and a byte-identical
three-artifact replay.

Ward Creek and Windigo remain sealed at open-count zero. The next action is not
evaluation yet: one exact metadata-only `AUTHORIZED_NOT_OPENED` record must be
generated, inspected, committed, and pushed first.

## U05 — one consumed test opening and honest model rejection

Authorization `8e0e6442196a5995f39b7fb7bd857a25f8a59698a4f6ccceaed03aa36f25194b`
is committed at exact remote-equal source
`4f71ec7c61ac0ca1b1dcfcd995d1444c9ec00c9e`. It binds the frozen
config, weights, selection, environment, Ward Creek/Windigo event IDs, and the
ordered four-patch roster. Run
`BL-2026-07-25-p3o1-t01-u05-test-r001` consumes that opening from zero to one.

The result is finite and unequivocal. The candidate predicts all 89 selected
cores as burned: 39 true burned and 50 background. Event-class macro Dice is
`0.29874213836477986`, macro IoU is `0.21474358974358976`, worst-event
macro Dice is `0.2641509433962264`, and masked BCE is
`0.7280717492103577`. Ward Creek macro Dice is `0.2641509433962264`;
Windigo is `0.3333333333333333`. The model is rejected as the analytical
winner against frozen RBR's 1.0.

The test package retains 12 files / 340,542 bytes at canonical inventory
SHA-256 `f850ed080d0cb7bb75143ae54f743c7750a4d2c4153e46eebde86107c4b71d5a`.
Independent checks match every receipt and working-byte promotion, reload all
eight probability/prediction arrays, prove binary outputs equal probability at
the frozen 0.5 threshold, and recompute denominators and the baseline decision.
The first inventory verifier retains a PowerShell host-compatibility failure;
the corrected verifier changes no evidence.

The exact desktop and narrow page has no document overflow or external
resource, keeps its four geospatially annotated patches and tables contained,
and visibly states the one-opening, no-retry, prototype-label,
no-ground-truth, and no-acceptance boundaries.

This is not a failed training claim or a manufactured success. It is a valid
trained and evaluated model artifact that does not add analytical value on the
frozen test. The model remains unaccepted and unreleased. U06 must replay
training exactly, verify the immutable U05 evidence without a second opening,
and package the model or valid rejected-model result with its model card,
inference contract, and Phase Four decision.

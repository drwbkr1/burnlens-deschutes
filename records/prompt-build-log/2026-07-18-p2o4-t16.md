# P2O4-T16 Prompt / Build Log - Label-Region Remediation Plan

**Date:** 2026-07-18

**Issue / branch:** #449 / `codex/p2o4-t16-label-region-plan`

## Result

BurnLens `0.28.0` binds the verified sufficiency, official-source scout/capture, and seven-product fitness reports. It replaces point buffering and arbitrary tiling with a deterministic evidence-coherent contiguous-region contract, a one-native-pixel unknown ring, exact owner yes/no/uncertain review, and no partial acceptance. It requires at least six immutable events, two events per eventual role, both binary classes plus unknown boundaries in every event, replicated evidence regimes, never-tuned transfer events, and no event majority before dataset fitness can reopen.

Fresh official research preserves source roles and cautions. Milli is first for sensor-continuity reconnaissance but remains tile-seam blocked. GW Fire, Two Bulls, and Cache Mountain are reference/sensor-shift priorities, not automatic Sentinel-pool events. The next acquisition rule is metadata-first scouting for at least three comparable Central Oregon Sentinel-era fires under one shared optical/region protocol.

Authoritative run `BL-2026-07-18-label-region-remediation-plan-r003` supersedes the visually rejected `r001` raster plate, reconstructs JSON/HTML/PNG byte for byte, and passes original-resolution PNG plus live semantic HTML/runtime review. No source pixels, region labels, dataset, split, baseline, or model are created.

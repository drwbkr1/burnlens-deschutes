# P6O1-T01-U07 release audit and publication-gate decision

Date: 2026-07-27

Issue: #578

Branch: `codex/p6o1-t01-baseline-first-publication-candidate`

Run: `BL-2026-07-27-p6o1-t01-u07-release-audit-r001`

## Prompt and controlling decision

Complete the current BurnLens project through the approved
`baseline-primary-with-rejected-model-diagnostic` route. Preserve the trained
U-Net failure as a first-class result, make no superiority claim, complete
Phase Four through Six, and stop before any public-sharing action that requires
owner approval.

## Cycle-start observation

The exact U05 package and U06 recipient route passed, but U07 still had to
prove the full changed-risk surface: complete regression, deterministic wheel,
isolated installed commands, fresh-clone package reconstruction, current
rights/claims/privacy posture, rollback, and live repository truth.

## Work performed

1. Re-read the Phase Six objective, issue #578 contract, unit records, current
   status surfaces, release audit matrix, and live GitHub state.
2. Passed the full 800-test suite at the initial U06 head, lock/dependency,
   compile, JSON, package, diff, deterministic wheel, and isolated installed
   runtime gates.
3. Installed the exact 1,228,628-byte wheel into a fresh locked CPython 3.12.10
   environment; all 75 distributions and 122 command routes passed.
4. Retained a new fresh-clone failure: Phase Six checksum rosters lacked an
   explicit recursive LF checkout rule, so strict package validation failed.
5. Added the bounded `.sha256` checkout rule and regression test at
   `cd383a3791d7bb39db076c60fa9c6b5f2993f1c0`.
6. Repeated the fresh clone, both package validators, exact package rebuild,
   and full suite. The corrected head passed 801 tests, one expected skip, 422
   existing warnings, and 86 subtests.
7. Retained a second closeout failure when the required living-case-study
   update no longer matched the U05 builder's current-source pin. Changed the
   builder to reuse the tracked frozen package snapshot after initial
   creation; 13 focused tests and a post-closeout exact rebuild pass.
8. Compared the result with the Phase Six objective, issue requirements,
   accepted RBR/rejected U-Net narrative, Phase Five rollback reference, and
   controlling public-action boundaries.

## Decision

`ready-for-owner-publication-gate`

This is a verified local pre-publication candidate, not a published release.
One coherent milestone PR may now open. After reviewed merge verification, any
tag, GitHub Release, deployment, access/ownership/public-sharing change,
publication, or external submission remains a separate owner stop gate.

No dataset, split, label, source, AOI, threshold, model weight, metric,
analytical raster, vector, or scientific conclusion changed.

# P2O1-T03 prompt/build record — VIIRS access integrity

## Identity

- Date: 2026-07-13 local / 2026-07-14 UTC
- Issue: #317
- Branch: `codex/p2o1-t03-viirs-inspection`
- Base commit: `1858f3d5ad3c193f997bef4f9142c996e174af27`
- Proposed repository version: `v0.1.2-access-integrity-baseline`
- Phase outcome served: Phase Two source access and reproducibility

## Cycle finding

The current repository had no runnable path and the next paired source action was blocked on a CDSE credential. A bounded NASA-only format inspection appeared possible from earlier `303` HEAD behavior. The first issue-backed GET proved that assumption false: a default client ended at `401`, while a browser-style client returned small Earthdata Login HTML bodies with final `200` statuses and data-looking filenames.

The highest-leverage evidence weakness therefore became delivery integrity. Without an independent body check, BurnLens could register login HTML as NetCDF/HDF5 and corrupt every later provenance, parsing, and label decision.

## Bounded improvement

- Narrow issue #317 from real-pixel inspection to fail-closed access integrity.
- Add a Python `0.1.2` validator that requires expected HDF5/NetCDF-4 magic and plausible size, rejects HTML/login content, withholds SHA-256 for rejected responses, and exits nonzero if either pair member fails.
- Add deterministic normalized JSON, semantic HTML, and PNG renderers.
- Generate `VIIRS-ACCESS-PRECHECK-2026-001` from the real rejected responses, then delete all response and rerun bodies.
- Reconcile active access, source, terms, roadmap, status, claims, version, and decision records.

## Primary sources reviewed

- NASA Earthdata exact `VJ214IMG.002` and `VJ203MODLL.021` product pages
- NASA Data Use and Citation Guidance
- Earthdata Login registration, application authorization, and data-access documentation
- NASA VIIRS active-fire and geolocation user guides

Exact URLs, supported facts, dates, and limitations are recorded in `ACCESS-2026-003` and `TERMS-2026-002`.

## Verification results before shipment

- Python 3.12.13; Pillow 12.2.0.
- An isolated `pip install --no-deps --no-build-isolation --target ... .` succeeds when pip build/cache paths are kept inside the ignored repository workspace; installed version/import is `0.1.2`. The first attempt failed only because pip attempted to write its cache outside the sandbox.
- Eight unit/contract checks pass, including separate credential/non-credential failure decisions and the accepted-payload render path.
- The real precheck exits `2` twice, as required for a fail-closed result.
- Fixed-input JSON and HTML hashes are equivalent across two full runs.
- The report rebuild CLI reproduces the committed HTML and PNG hashes exactly.
- Visual QA found missing em-dash glyphs in the first PNG; ASCII separators corrected them, and the second 1600×1200 review passed.
- JSON parses; all manifest output hashes match.
- Provider source asset count/bytes, credentials used, signed URLs retained, and rejected response bytes retained are all zero.
- No analytical pixel, detection, label, dataset, baseline, model, map, or application claim is permitted.

## Gate result

`STOP — DELIVERY VALIDATOR PASSES; OWNER APPROVAL REQUIRED FOR BOTH EARTHDATA AND CDSE CREDENTIALS`.

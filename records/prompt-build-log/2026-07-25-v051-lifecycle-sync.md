# v0.51 lifecycle and portfolio sync

Issue #560 begins from verified corrected main
`c39a2c543be2f7884eff264a733821f367f49776`. Annotated tag object
`61fad1ab87d2158b4a862f9866f72ac6ad70da7e` remotely peels exactly to that
checkpoint.

The sync updates current status, roadmap, changelog, version history, release
audit, living case study, reviewer quickstart, and repository-owned portfolio
page. It replaces the stale Darlene-bearing count-minimum presentation with
the exact McKay, Tepee, Green Ridge, Grandview, Windigo, and Ward Creek
sufficiency result.

Portfolio source `7228be5bce01165775a46767d898ae03b893b8cc` validates the exact
9,429-byte readiness JSON, 6,357-byte HTML, and 101,221-byte PNG before
building. It also preserves the exact Petes Lake material-defer inputs.

Two builds of run `BL-2026-07-25-v051-portfolio-sync-r001` reproduce a
5,931-byte JSON at SHA-256 `ff5fb042...` and a 14,774-byte HTML page at
SHA-256 `92003963...`.

Real 1280 by 720 and 390 by 844 browser checks pass. Six replacement events,
12 prototype regions, 54 valid assignments, all ten readiness gates, zero
datasets/models, four null analytical versions, both source images, contained
layout, and empty browser logs are visible.

The page states that the pass authorizes only a separate dataset, split, QA,
and baseline checkpoint. No dataset, split, baseline, model, accuracy,
training authorization, inference, deployment, final submission, or external
sharing is created.

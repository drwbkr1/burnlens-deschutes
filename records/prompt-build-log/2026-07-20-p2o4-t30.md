# P2O4-T30 Prompt / Build Log - Grandview Region Proposal

**Issue / PR / branch:** #508 / #512 / `codex/p2o4-t30-grandview-region-proposal`

**Generator:** `78bb79eb04d76cff4ed183274de881557b176413`

**Run:** `BL-2026-07-20-grandview-region-proposal-r001`

The cycle begins by rerunning verified v0.41 from lifecycle-synchronized main. All route outputs reproduce byte for byte, confirming that the next bounded weakness is the absence of a reviewable unit.

The actual run transfers the established fixed-bin intact-component protocol and proposes one 25-pixel core per class. The burned route uses MTBS classes 2-4 plus finite pre/post dNBR and explicitly excludes RAVG modeled classes. The background route retains every v0.41 optical, vector, boundary, and registration gate. Unknown rings contain 46 and 52 pixels.

JSON, HTML, PNG, and both EPSG:32610 rasters render actual evidence and bind source/software/schema/run identity. All 326 tests pass across two complete bounded shards; fresh remote head passes 312 tests with 14 expected custody skips, 108 JSON parses, exact reconstruction, canonical 602,463-byte packaging, isolated import, and dependency health. Local `file://` browser automation was policy-blocked and not bypassed; PNG, semantic HTML, links, and rasters were validated independently. Zero owner responses or labels are created. Fresh-main and tag verification remain pending; issue #511 owns the separately versioned blank owner surface.

PR #512 merges analytically at `0905711e3f1dc2e281ff830ff3b9d94ed2c50f66`. Fresh main correctly fails exact JSON reconstruction because the new directory lacks an LF checkout rule; CRLF normalization is otherwise identical. Issue #513 adds only the missing JSON/HTML LF contract. Fresh remediation head `d3b11e78e03492f8348b980f7304864ded85b06a` passes exact five-output reconstruction, tests, JSON parsing, and canonical package identity. The tag remains withheld until the remediation merge passes fresh-main gates.

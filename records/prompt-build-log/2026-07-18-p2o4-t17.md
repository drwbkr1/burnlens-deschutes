# P2O4-T17 Prompt / Build Log - No-Promotion Region-Candidate Pilot

**Date:** 2026-07-18

**Issue / pull request / branch:** #453 / #455 / `codex/p2o4-t17-region-pilot`

## Result

BurnLens `0.29.0` executes the verified region-first plan against the actual Darlene, McKay, and Tepee optical/reference packages. It selects exactly one proposed burned and one proposed background intact component per event using frozen five-state class, quality/registration eligibility, exact categorical reference context, and a fixed 0.05 dNBR bin. The one-hectare target ranks intact components only; it never clips or expands one. Every candidate receives a one-native-pixel unknown ring.

Authoritative run `BL-2026-07-18-region-candidate-pilot-r006` publishes six full-grid EPSG:32610 candidate GeoTIFFs plus private-safe JSON, semantic HTML, and a 1800 by 1960 evidence plate. The result contains 136 core and 246 ring pixels, zero owner region responses, zero region labels, and no dataset, split, baseline, or model.

Real desktop and 390 px browser QA passes after the generator fixes page overflow and contains the wide inventory table. All six links return 200, the evidence image loads, no external resource is requested, and console/runtime logs are empty. A fresh second pipeline run reconstructs all nine public artifacts and the ignored private mapping byte for byte. The tracked suite passes 241 tests, compilation, and dependency health.

Two independent clean-source, fixed-epoch wheels are byte-identical at 440,972 bytes with SHA-256 `d97e11058aaf18c8a146df3eb5392788061c5daa784620bd20ac1c09e8197d35`. Inspection from inside the isolated environment reports BurnLens `0.29.0`, all 43 console commands including `burnlens-build-region-candidate-pilot`, 94 wheel entries, zero forbidden paths, and no broken dependencies. Repository-root inspection is deliberately excluded because local editable metadata can shadow the isolated distribution.

The next allowed checkpoint is a separately versioned six-candidate owner yes/no/uncertain review surface. No earlier point decision is inherited as a region decision.

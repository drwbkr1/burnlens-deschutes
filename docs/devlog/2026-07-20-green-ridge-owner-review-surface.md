# Devlog - Green Ridge Owner Review Surface Candidate

Verified v0.36 left two exact Green Ridge proposals inspectable but not answerable. Issue #487 turns those immutable raster bytes into a blank owner review candidate without collecting a response or promoting a label.

Run `BL-2026-07-20-green-ridge-owner-review-surface-r001` first rebuilds all five proposal outputs from the exact ignored optical/reference custody inputs. Every regenerated size and SHA-256 matches the tracked release. The surface then verifies both native rasters and renders ten actual evidence panels: pre, post, extended optical, dNBR, and MTBS/RAVG agreement for each core and gold unknown ring.

The controller reuses the established yes/no/uncertain contract and tightens reload behavior: it validates all incoming units before mutation, clears stale radio state, rejects invalid note/decision structure, uses hash-named LF-terminated exports, and disables response, loader, completion, and export controls after completion. No-overwrite custody and later separately versioned intake are explicit.

All six surface outputs reproduce byte for byte. Original-resolution evidence inspection, Node syntax, 291 tests, compilation, 98 JSON parses including the candidate manifest, and reproducible packaging pass. Two fixed-epoch wheels are identical at 541,301 bytes / SHA-256 `6770a4d296ed728726ba696094285d661cceda409e7e8fe3892c988e23b87bce`; the dependency-complete isolated install reports BurnLens 0.37.0, no broken requirements, and working CLI help.

The agent cannot navigate directly to the local file because the in-app browser blocks `file://` navigation. The checkpoint remains unshipped until real desktop/mobile interaction, download, reload, and lock behavior can be observed. Zero responses, labels, dataset, split, baseline, or model are created. Issue #491 owns exact response intake after release verification.

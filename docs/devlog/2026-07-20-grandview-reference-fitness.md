# Devlog - Grandview Reference Fitness

The cycle began by running the verified v0.39 report. Optical custody, quality, and registration still passed; zero official reference pixels remained the highest-leverage evidence weakness.

BurnLens refreshed the current official portal identities and requested exactly one native-UTM bundle for BAER `10019092`, RAVG `10019464`, and MTBS `10023989`. The completion email arrived from the official USGS sender. Gmail was read-only; the recipient and private retrieval route stayed private. One HTTPS download from the exact provider host produced a 22,076,790-byte ZIP / SHA-256 `ea30fe613931d9b90c1405a653ac01fe6bfaa7ee80f3ed02480422271ff874bf` under ignored no-overwrite custody.

Safe preflight passed all 53 members before pixel access. Exact delivered metadata resolved use and attribution while confirming that restricted BAER BARC products were absent. Every one of the 20 native rasters opened on 30 m EPSG:32610 grids, with nodata, domains, transforms, boundary components, and program attributes preserved in the report.

The rendered result is deliberately asymmetric. MTBS provides analyst-interpreted categorical evidence across 98.7330% of the verified optical boundary, with 58,438 class 2-4 pixels and 56,943 also passing optical pair validity. RAVG looks visually strong but its own record warns that forest-calibrated modeled values are less reliable over Grandview's sparse/non-tree cover. The surface therefore labels RAVG as limited context rather than using its apparent agreement to inflate confidence. BAER remains preliminary unthresholded change context.

The original-resolution PNG and semantic HTML were inspected directly, and all three outputs reconstruct byte for byte. The full custody worktree passes 316 tests. A first dirty-worktree wheel was rejected when fresh-head byte comparison exposed line-ending differences in historical package files. The fresh remote head then passes 303 tests with 13 expected custody skips, compilation, 104 JSON parses, and exact reconstruction through unchanged original custody. Two fixed-epoch fresh-head wheels are byte-identical at 582,467 bytes / SHA-256 `b5e50359784fa748477cd9ff1c21e66083f0adce3682aea5e81c6163b930e269`; the dependency-complete target install reports BurnLens 0.40.0 with exact pinned dependencies, 64 commands, 133 clean entries, passing help, and no broken requirement.

Issue #503 now owns affirmative Grandview background evidence. No candidate, owner response, label, dataset, split, baseline, or model advances in this checkpoint.

PR #504 squash-merges at `cdbe49883fb5330c9e7af036a847841b714dafae`. Fresh merged main repeats the absent-custody test, JSON, compilation, reconstruction, and canonical-wheel gates. Annotated tag object `48c1c9677b9b41a734be2dcdc4a9691caf4e6688` remotely peels to the checkpoint.

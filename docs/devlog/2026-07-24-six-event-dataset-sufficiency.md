# Six-event dataset sufficiency

BurnLens now reconstructs the exact six-event prototype-region evidence instead of relying on event counts.

The evaluator reopens all 12 tracked region rasters. It verifies CRS, nodata, class domains, core counts, unknown rings, connectivity, source and terms bindings, owner promotion gates, event shares, and every prospective 2/2/2 whole-event assignment.

Eight gate families pass. Leakage/split fitness and evaluation design block. Darlene is the only NIFC-context event, so all 90 assignments confound that regime with one split role.

The first generated attempt bound an incorrect expanded commit string and was withheld. The second valid attempt exposed output-directory-dependent JSON paths. Both remain ignored evidence. Final r003 binds exact source `e102874c...`; all six tracked outputs reproduce byte for byte, and an independent audit returns the same block decision.

The first full-suite run reported 590 passes, one skip, and three pre-existing contingency-bundle failures. Exact base `1c4df18...` contains an 86,889-byte current case study while the historical builder correctly requires the frozen 84,039-byte v0.48 asset. The corrected regression reconstructs from the verified ZIP's frozen roster, validates the preserved ZIP, and proves current mutable case-study bytes cannot be silently rebundled. Final full-suite verification passes 595 tests, one expected skip, 58 warnings, and 86 subtests.

Package QA retains three attempts. The first verifier used an invalid PowerShell selector. The second built two different wheels without a fixed epoch. Final r003 fixes the epoch and reproduces two 899,515-byte wheels at SHA-256 `c3aa970694add791d17b78ee688108435bcac8921514071410dd87bf0ae84ca5`. An isolated CPython 3.12.10 install verifies BurnLens 0.50.0, 13 compatible distributions, 89 commands, and the new help route.

The tracked PNG passes original-resolution inspection. The in-app browser refuses local-file navigation, so the owner opens the exact tracked HTML and confirms correct desktop and narrow-width rendering. Release audit `RELEASE-AUDIT-2026-004` verifies the candidate without changing its scientific block.

# BurnLens 0.54 is verified

BurnLens now has one verified baseline-primary CV-to-GEOINT release. RBR is
the accepted analytical method. The trained U-Net remains a reproducible but
rejected diagnostic after collapsing to all-burned test predictions; it is
not presented as outperforming RBR.

The Ward Creek run packages native rasters, accepted RBR vectors, bounded
official context, deterministic observations, visible false-positive-risk
evidence, an accessible offline interface, and exact replay material. Its
66-file package and 487,893-byte ZIP validate from tracked bytes.

Fresh remote main makes one important boundary explicit: builder tests that
recreate dataset and Phase Four intermediates need controlled provider and run
custody that a clone intentionally lacks. BurnLens retains that failed broad
attempt, passes the declared tracked-only suite and standalone package
validators, reproduces the reviewed wheel exactly, and passes all 114 commands
in an isolated runtime.

Release tag `v0.54.0-rbr-geoint-milestone` points to exact merge
`8660ccba893b7e3acfdc361e663a6b8d59d52a34`. No deployment or GitHub
Release exists. Phase Five receives the verified package, interface, failure
evidence, and rollback posture; it does not inherit a reliability acceptance
claim.

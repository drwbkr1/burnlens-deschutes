# 2026-07-21 - Grandview owner review surface

The cycle starts by rerunning the real v0.42 proposal from the exact ignored optical and reference custody inputs. All five outputs match their verified hashes, and original-resolution inspection identifies the missing owner-facing review surface as the highest-leverage weakness.

The new surface binds both exact rasters and creates two blank yes/no/uncertain questions. The burned unit shows pre/post/extended optical, pre/post dNBR, and MTBS classes 2-4. The background unit shows pre/post/extended optical, anniversary dNBR, and the affirmative background route. Both preserve the excluded gold uncertainty ring. RAVG modeled classes are explicitly context-only and absent from affirmative evidence.

The hash-named response controller validates surface, run, response schema, proposal report, candidate order, raster hashes, decisions, notes length, and owner attestation. A completed response locks in-browser controls, but exact intake remains a later checkpoint. Six public outputs reproduce byte for byte; focused real-custody tests, controller syntax, semantic checks, original-resolution review, and blank-state checks pass. Owner responses and labels created: zero.

# 2026-07-17 - Current Reference Bundle Fitness

The cycle began by running the current inventory against the verified `v0.23.0-official-source-scout` baseline. The highest-leverage weakness was no longer delivery: it was whether the exact current BAER/RAVG/MTBS bytes were safe, legally usable in a bounded role, spatially compatible, and evidentially useful for reopening the 56 owner-review units.

Two exact USGS deliveries were preserved without overwrite in ignored repository-local quarantine. All 35,890,432 compressed bytes and 164 members passed hashes, CRC, path, collision, encryption, symlink, and nested-archive checks. Seven product identities match the accepted request. Standard products are 30 m EPSG:32610; legacy Tepee BAER is 30 m EPSG:26910.

Terms inspection found the binding distinction. MTBS and RAVG permit use with acknowledgement and cautions. BAER marks BARC as preliminary, and the legacy Tepee metadata restricts further distribution of thresholded BARC4/BARC256. BurnLens therefore leaves those exact pixels private and excludes them from public evidence and label promotion. Darlene BAER delivered no classified raster despite including SBS metadata.

BurnLens `0.24.0` adds a fail-closed exact-archive inspector. It samples permitted categorical products to each frozen proposal grid with nearest neighbor, makes no source-resolution gain claim, and reports five-state comparisons. McKay and Tepee burned proposals receive complete MTBS/RAVG affirmative support; their background candidates remain almost entirely non-affirmative. Darlene RAVG and unthresholded BAER dNBR add useful but non-independent evidence.

The first mobile HTML render overflowed because the machine decision and table exceeded the viewport. The final semantic report wraps the decision, contains the table in an explicit scroll region, and has zero document overflow at 390 px; desktop and original-resolution PNG checks also pass with no browser errors.

All 207 repository tests pass in both working-tree and detached-candidate runs. The detached checkout reconstructs all three outputs exactly. Two fixed-epoch wheels are byte-identical at 383,199 bytes / SHA-256 `127a7262ac7d5845cec3f353d4a74ca05b5c6c184ebddf578b2fbc1a34b80888`; an isolated install exposes the new command and no private/download files. The checkpoint moves zero labels. Next is the owner yes/no/uncertain surface for all 56 original units, not a dataset or model.

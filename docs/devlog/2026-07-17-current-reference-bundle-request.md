# Devlog - Exact Current Bundle Request Evidence

**Date:** 2026-07-17

**Issue / PR / parent:** #421 / #422 / #416

The cycle began by rerunning the current inventory and inspecting the real official viewer workflow. The highest-leverage evidence weakness was not another threshold or label rule: BurnLens had frozen catalog identities but had not preserved the exact current fire-level bundle request or the provider cautions that should govern later pixel interpretation.

The viewer's current route supplied six standard map IDs and one explicit nonstandard Tepee BAER bundle path. BurnLens submitted one bounded UTM request for 18 applicable product families. The official endpoint returned `{"success":true}`. The exact 16 bytes remain ignored and private; the public evidence stores only their size/hash and an accepted/pending distinction.

A fresh full-metadata WFS capture also exposed four scientifically material cautions. Darlene and Tepee RAVG have forest-model applicability limits; McKay RAVG has an early-post-scene overestimation risk; Tepee BAER is nonstandard. The new fail-closed capture freezes the exact assessment metadata and refuses drift, duplicate IDs, geometry, oversized responses, or an unsuccessful queue result.

The rendered 1800-by-1400 card and semantic HTML show seven products requested, 18 product families, zero archives received, and zero labels promoted. Recipient and retrieval details are withheld. All 195 repository tests, compilation, dependency health, JSON/HTML semantics, local image linkage, privacy scans, and original-resolution visual review pass at candidate stage.

This checkpoint deliberately does not close #416. Exact archive delivery, bundle-specific terms, structure, grids, masks, class domains, and pixel fitness remain pending. Candidate label state remains 6 burned, 0 background, and 50 ignored; there is still no dataset, split, baseline, or model.

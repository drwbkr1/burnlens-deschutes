# Devlog - Grandview Source Fitness

The verified v0.38 tool still ranked Grandview as the highest-leverage missing event. The cycle reproduced the current additional-event plan byte for byte, refreshed both exact CDSE OData records, and bound current Sentinel terms before touching provider data.

The protected CDSE wrapper downloaded the two frozen archives sequentially. The pre archive completed on the first attempt; the post archive used the bounded fresh-token retry path and completed on the third. Both final files passed provider checksums, local hashes, SAFE structure, manifests, and full CRC inspection before atomic registration. Credentials, tokens, headers, local paths, run state, and raw bytes remained outside Git.

Actual native pixels are strong optical evidence: 62,588 boundary pixels, 97.5794% pair-eligible quality, and nine of nine registration windows passing with 0.1158-pixel p95 residual. The rendered pre/post views show broad continuous change, but BurnLens does not promote appearance or dNBR into severity or labels. Official Grandview reference pixels remain the binding weakness, and affirmative background evidence requires its own later gate.

The canonical original-resolution PNG and semantic local HTML were inspected directly. The surface presents actual pixels, exclusions, measured registration, source attribution, trace identifiers, and the no-label boundary. Exact reconstruction and release packaging remain required before this candidate becomes a verified checkpoint.

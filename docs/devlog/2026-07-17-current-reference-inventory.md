# 2026-07-17 - Current Cross-Program Reference Inventory

The cycle started from the verified single-reviewer reconciliation rather than reopening label ideation. Exact proposal/QA and reconciliation-QA reruns reproduced the authoritative outputs byte for byte. Aggregate private diagnosis showed that weak background evidence, seasonal/phenological ambiguity, cloud/shadow, and cross-source disagreement—not software drift—were the highest-leverage weakness.

Fresh official research exposed a more important source problem: the 2026 Burn Severity Portal says its archive was reprocessed and redistributed in late 2025. The frozen `SOURCE-2026-013` annual ImageServer clips remain valid provenance for the published proposal, but they cannot silently govern a new promotion decision.

The current viewer's property-only WFS query returned seven exact records: Darlene BAER/RAVG, McKay MTBS/RAVG, and Tepee BAER/MTBS/RAVG. BurnLens `0.21.0` turns that finding into a bounded fail-closed capture. Missing, changed, duplicate, geometric, unexpected, or type-drifted results are rejected.

`CROSS-EVENT-REFERENCE-INVENTORY-2026-001` renders the inventory and the correct decision: acquire exact current bundles, then defer labels, dataset, and model until terms, bytes, structure, grids, masks, class domains, and cross-program pixel fitness pass.

The 1800-by-1340 evidence card initially clipped its safety warning. The warning was split across two lines and the corrected original-resolution card was inspected. Installed Chrome also rendered the complete semantic HTML at 1440 by 2600.

All 188 tests pass in both the working tree and a clean detached checkout. Two fixed-epoch clean-checkout wheels are byte-identical at 348,031 bytes / SHA-256 `da2585a2eb820b0706a763a29d05b7b859b9af62bc6c3e7a6414b22c7f8e60ec`. An initial isolated metadata check was contaminated by the repository-root legacy egg-info on `sys.path`; rerunning from the isolated environment proves BurnLens `0.21.0`, 34 console entry points, the new entry point, 75 wheel entries, and zero private/download/build entries.

Zero labels move. One reviewer remains one reviewer. Product delivery and pixel fitness—not threshold shopping—are next.

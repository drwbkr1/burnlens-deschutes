# Devlog - Grandview Background Evidence

The cycle began by rerunning the actual verified v0.40.0 tool. Exact reconstruction passed and affirmative background evidence remained the highest-leverage weakness.

Fresh official CDSE STAC reconnaissance selected one same-platform, same-tile, same-orbit near-anniversary scene. The fail-closed contract was committed before acquisition. Protected DPAPI-backed credentials requested one exact OData archive; 950,987,607 bytes matched provider MD5/BLAKE3 and were atomically registered only in ignored custody. No credential, token, private route, or provider byte entered Git.

The real pipeline exposed two useful failures. First, the shared source reader rejected the 05.10 archive because earlier events were explicitly 05.00; the fix made the expected baseline per-call while preserving 05.00 as the default. Second, whole-context registration missed the 90% finite-coverage gate because the pre scene has 89.1% finite data across the 3 km rectangle. Registration was therefore measured on the complete event boundary while all context nodata stayed visible in the search. Seven windows pass and two review-needed windows become mandatory spatial exclusions.

The first completed route then rejected itself because BAER's continuous dNBR raster is finite across a rectangle and cannot represent the official perimeter. The corrected implementation parses the exact delivered BAER/MTBS/RAVG polygon bytes and rasterizes those vectors. Only after this correction does the route open: 67,782 pixels / 2,711.28 ha and 181 components at least one hectare. RAVG modeled classes remain disallowed.

The original-resolution PNG and semantic HTML show pre, post, extended, three-program footprint union, coherent stability, route evidence, all warnings, and zero candidates or labels. The custody worktree passes 325 tests; a fresh remote-head checkout passes 312 tests with 13 expected custody skips and reconstructs all three outputs exactly through unchanged ignored custody. Two fixed-epoch wheels are byte-identical at 597,078 bytes / SHA-256 `90f8c3843e64af8218e44843a25d87ca23cd33ee690d9e89a1e8c50c70240696`; an isolated dependency-complete environment imports BurnLens 0.41.0 from the installed wheel, exposes 66 commands, and passes dependency health. Fresh merged-main and remote-tag verification remain release gates.

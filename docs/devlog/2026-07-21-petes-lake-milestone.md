# Petes Lake Six-Event Evidence Milestone

## 2026-07-21 - U01 entry gate r003

Issue #521 begins from exact governance base `0d41942dc6c3c307a9a146a2d38fb816e038bb42` under `checkpoint-policy-v0.1.0`. The current tool reconstructs the frozen six-event plan exactly, and the real current render still makes the right weakness visible: Petes Lake is metadata only, while pixels, both classes, uncertainty, owner review, and every dataset/model gate remain closed.

The first live rehearsal exposed that the durable capsule had not yet been recorded, so it is retained as `remediate`. Capsule commit `43635eb20183f49864397f8b74db4f49eb7a3b7e` precedes the qualifying run. A post-capsule r002 capture then failed closed on a transient STAC request; a direct official probe returned HTTP 200, and a new immutable r003 completed rather than overwriting the failed identity.

Fresh official r003 checks preserve the frozen event/map and Sentinel pair. CDSE reports both exact archives online with current MD5 and BLAKE3 values, and token-only authentication passes without requesting provider product/archive bytes. The Burn Severity Portal lookup returns catalogue row `50890` rather than frozen `50884`, but every governing event/map field matches; that bounded drift is retained without claiming the service established replacement semantics.

Two cautions shape the rest of the milestone. The post scene reports 9.841206 percent tile-wide snow, which says nothing decisive about the local fire boundary until U03 opens SCL/SNW and rendered pixels. MTBS explicitly warns that wetland severity may be misrepresented, so U05 must measure the warning and U08 must keep unresolved wetland pixels out of both the candidate cores and unknown rings.

U01 passes only the entry gate. U02 is authorized to acquire the exact pair sequentially into ignored no-overwrite custody. No Petes Lake pixel, candidate, response, label, dataset, split, baseline, or model exists yet.

The owner also clarified the later interaction contract without interrupting U01-U08. U09 will be one Petes Lake batch containing only the exact burned and affirmative-background candidates. Decisions, notes, hashes, identities, and non-owner gates stay candidate-specific; there is no approve-all, prefill, missing-answer inference, or cross-milestone batch. U10 must lock the exact final export before reveal and treat multiple distinct valid finals as ambiguous until the owner explicitly designates one.

## 2026-07-21 - U02 code-only migration checkpoint

Before either provider archive was requested, BurnLens implemented and synthetically verified the exact U02 transaction boundary. The pre and post products remain separate singleton packages; pre must pass current metadata, download, checksum, SAFE/ZIP, registration, atomic no-replace promotion, private-state, and fresh reopen verification before post metadata or credentials may be exercised. Retry is bounded to transport, early EOF, HTTP 408, and 5xx failures with a fresh token; authentication, authorization, missing-object, quota, other 4xx, checksum, archive, path, and custody failures stop. Private state must remain ignored, untracked, single-link, no-overwrite, and exact-readback verified.

The code also retains revisioned full, post-only, and aggregate-only remediation routes so a later failure never requires deleting or reacquiring an already verified package. A tracked report cannot authorize U03 by itself: the credential-free verifier must freshly reopen both packages and match exact private/tracked aggregate bytes, hashes, trace, ancestry, and entry-gate bindings.

The owner then paused active custody for checkout migration. No CDSE product/archive request or download occurred. The code-only checkpoint is pushed for recovery, and this OneDrive checkout must remain frozen until the owner explicitly resumes after migration. U02, U03, every later unit, and every dataset/model gate remain unadvanced.

The first credential-free preflight in the fresh non-synchronized checkout then failed closed before provider access because the nested Petes Lake entry-gate JSON was outside the existing cross-event LF checkout rule. Windows checkout conversion changed its 18,270 exact bytes even though the index object remained correct. The branch now applies the cross-event JSON/HTML LF contract to one-level event subdirectories and tests the exact Petes bytes plus Git attribute. No provider request occurred, no custody byte changed, and the historical entry-gate object was not rewritten.

## 2026-07-21 - U02 cloud-metadata precision remediation

The retained post-migration probe exposed a second real pre-provider defect: live CDSE OData represented the two catalogue cloud percentages as `0.000358` and `0.008789`, while the frozen U01 STAC evidence represented the same records as `0.0` and `0.01`. Every UUID, SAFE name, size, online value, MD5, and BLAKE3 still matched. The transaction correctly stopped before credentials, archive access, or custody.

BurnLens now preserves both endpoint representations rather than replacing one with the other. The v0.2.0 optical intake contract requires the exact raw OData values and separately retains the immutable U01 STAC values. A deterministic decimal `ROUND_HALF_UP` comparison at two decimal places must agree, while any raw drift still fails. Current official CDSE documentation supports the endpoint-role distinction; catalogue cloud metadata still cannot establish local pixel quality.

Code checkpoint `c4987ce53a7606d828caf2db134a28f160bc867d` passes 64 focused Petes/provider tests and the full 395-test repository suite. From that exact pushed commit, the credential-free production preflight passed and live run `BL-2026-07-21-petes-lake-optical-metadata-reconciliation-r001` created one fixed no-overwrite report: 8,750 bytes, SHA-256 `b1062e8e8f298087852cd0199d83482a20873bcc90bd5d777c5c6e91c69aaa52`. Evidence commit `d690bf89da3e83271c571e96512d2c050399f4b6` preserves the report, registry, precheck, and human-readable decision on the remote milestone branch.

The run used public metadata only and created zero provider product/archive bytes, tokens, raw packages, quarantine entries, private states, or custody reports. It authorizes only a new clean U02 preflight after the evidence is committed, pushed, and recorded. U02 remains incomplete; the pre archive must pass independently before post access, and delivered local SCL/SNW plus rendered-pixel fitness remain U03 gates.

## 2026-07-21 - U02 exact optical custody

After the reconciliation evidence was pushed and recorded on issue #521, a new clean production preflight passed from remote-equal commit `31eedcddff0a712001181fd282f18f0e41ed5412`. Run `BL-2026-07-21-petes-lake-optical-intake-r001` then acquired the exact pre archive first. It passed live metadata, one-attempt download, size, MD5, BLAKE3, local SHA-256, ZIP/SAFE root, manifest, CRC, no-replace promotion, immutable registration, private-state, and fresh-reopen gates before the post transaction began. The post archive independently passed the same gates in one attempt.

The pre package is 1,185,284,273 bytes with local SHA-256 `c5b6ededc5a264fcc617f2712093b847351c5b8ed78f2ea9ce0ebc3a33e1bc34`. The post package is 1,243,068,088 bytes with local SHA-256 `636a3cafe66c16cb20c9fd490a7ad448c939e3476d9ac0a404576638c2cd3a25`. Each contains one expected SAFE root and 95 members with passing full CRC. All archives, registration manifests, and private states are ignored, untracked, no-overwrite, and single-link.

The transaction completed both packages, the private aggregate, and the public report, then the CLI exited nonzero because it looked for a top-level decision instead of the report's `semantic_record.decision`. The failure was after successful custody and is retained rather than hidden. Direct credential-free verification passed against the preserved outputs. Commit `62e920e272e83b67f096659f77a25eac129f707c` fixes only that result-display extraction and fails closed when a decision is absent; 64 focused tests and the full 395-test suite pass. A second credential-free verifier passed from that pushed fix without reacquisition.

Evidence commit `11580b28e5b8dd4be0b68420d0baf096c95b7e9b` preserves the 23,521-byte public custody report, SHA-256 `46f72f7fa7dc3b6fc3f96f6023b0d7b14f6990bcef8945a878ae04600d2e571d`, plus PRECHECK-2026-045, REGISTRY-2026-047, and the bounded decision. U02 passes custody and authorizes U03 only. No delivered pixel has yet passed CRS, band, grid, nodata, SCL/SNW, local cloud/smoke/shadow/snow, registration, or real-render fitness.

## 2026-07-21 - U09A/U09B verified handoff retained

The isolated owner-review utility work is verified on ordered commits `530ce51a01aa3da9446638073f218179592d3cbc`, `09e03192563a1cd0fa4206a1bfe7863d6f3a24be`, and `ab3efb71597b620bd74e1723527a03b9669069c7`. Its handoff reports 36 focused tests plus 410 passing and 17 skipped full isolated tests, successful desktop and 390 x 844 browser interaction/export, stopped servers, and zero provider or scientific advancement. The fixture and test URL are explicitly non-ingest.

U03 continued uninterrupted until the clean, pushed, issue-recorded selection checkpoint. The handoff is now integrated there with newer U02/U03 facts preserved. Production U09 remains dependency-blocked on U08 despite the preparatory utility being present.

The separately coordinated U09C geospatial-environment work is verified through source head `56980540a647211b31109e6303b3cca9ea1739ec` and issue comment 5038557387. It integrates only after the current selection evidence is cleanly committed, pushed, and issue-recorded, with newer U02/U03 facts preserved. The final label is `BurnLens geospatial research (Windows)`, and the canonical `.venv` is recreated from the lock rather than copied.

## 2026-07-21 - U03 first preview retained

Committed source-fitness code generated ignored preview run `BL-2026-07-21-petes-lake-source-fitness-preview-r001` without changing either provider archive. The 1,800 x 1,240 PNG renders the clear pre scene, visibly snow-dominated post scene, sparse valid continuous dNBR footprint, 21.8133% pair-eligible SCL quality, 100% post snow-probability p95, zero of eight passing registration windows, warning, and exact trace without clipping. The machine gate rejects the planned post scene and keeps U04 blocked.

Direct local-file navigation to the companion HTML was blocked by the in-app browser's security policy and was not bypassed. That exact ignored preview remains immutable as a superseded attempt. Tracked U03 evidence is therefore narrowed to the exact JSON, the visually inspected PNG, and a human-readable decision record; no unverified HTML is eligible for the tracked output roster. Preview r002 must be generated from committed code before the final r001 disposition.

Committed preview r002 reproduces every scientific metric with a narrowed JSON/PNG roster. Final run `BL-2026-07-21-petes-lake-source-fitness-r001` retains 34,103 native 20 m boundary pixels, pre SCL eligibility 99.0030%, post SCL snow/ice 78.0782%, pair eligibility 21.8133%, and zero of eight passing registration windows. Its 1,800 x 1,240 PNG passes original-resolution inspection and honestly renders the snow-dominated failure; continuous dNBR remains limited to 7,439 valid pixels and is never a label.

U03 r001 disposition is `remediate`. The planned post scene is rejected, U04 is not authorized, and all later units remain blocked. The next bounded work is current metadata-only research for a same-event, same-source-regime replacement post scene, followed by an exact contract revision before any provider request. Existing archives and every preview/final artifact remain immutable.

Evidence commit `24d3affa4160f049bf6c11ddc8fbe13f8f078c70` is pushed and issue #521 comment 5038371241 records the exact outputs, gates, disposition, verification, and next boundary.

## 2026-07-21 - U03 replacement-post metadata selection

A bounded current search preserves the failed planned-pair evidence and holds the source regime fixed: Sentinel-2 L2A, Sentinel-2A, MGRS-10TEP, relative orbit 13, processing baseline 05.10, and full frozen-boundary coverage. Six current STAC items match. Official regional incident updates support a conservative first eligible post-status date of September 23, so the otherwise clear August 30, September 9, and September 19 scenes are excluded before acquisition. September 29 and October 9 exceed the unchanged 20 percent catalogue-cloud gate.

The October 19 product is the only survivor. Current OData binds UUID `31fa8699-175b-4fd7-91c3-dd727a1576f5`, exact SAFE identity, 1,195,226,823 bytes, online state, MD5 `4cf05a073b4c67f5e92e052ed1eb32bc`, BLAKE3 `1b28f566aee5619ea9a48c8dd042f209194a40989ba4b54cfe4e14904a0ad878`, platform, tile, orbit, baseline, and product type. STAC reports 0.10 percent cloud and 0.564076 percent snow tile-wide. The cloud representations reconcile, but the nonzero snow value remains an explicit local SCL/SNW and real-render risk; no snow threshold is invented.

Run `BL-2026-07-21-petes-lake-replacement-post-selection-r001` is public metadata only: no credential and zero provider product/archive bytes. Its 10,127-byte report has SHA-256 `7fa82a61fa70d47364db29493700beedd60c9114a4b3a6d8ddbafdf77aecfc8c` and reconstructs byte-identically from source commit `cfa5d1923feb413859c3aa20ebc40df3e0ee2ee6`. The decision authorizes exact contract revision only. It is not acquisition, source acceptance, or an official fire-end claim.

Evidence commit `32c1b6761919ca9721edfe87b98d913f3a5dd23e` is pushed and remote-equal. Issue #521 comment 5038559167 records the exact selection, report/hash, tests, zero-provider-byte boundary, and next gate.

Before any provider transaction, BurnLens must commit, push, and record an exact singleton replacement contract and pass a clean live metadata/terms/custody preflight. The original post archive and U03 r001 failure remain immutable. U04 and every downstream candidate, owner-response, label, dataset, split, baseline, and model gate remain blocked.

## 2026-07-21 - U09B localhost review transport handoff

The owner-review delivery improvement was built away from the active milestone checkout so its ongoing U02 metadata reconciliation stayed untouched. The isolated branch first integrated the verified U09A batching contract, then added `burnlens-serve-review-surface`: one exact page, one hash-bound immutable asset allowlist, one random loopback capability URL, and no directory server, upload, public bind, or response interpretation.

The security boundary is deliberately narrower than manifest membership alone. The selected page must identify an owner-review response schema; it is the sole HTML route. Only safe sibling static assets may be referenced. Reveal, response, receipt, template, intake, raw, private, adjudication, quarantine, secondary HTML, JSON/text data, nested, ambiguous, external, traversal, and reparse paths fail before the listener opens. Exact Host/token routing, read-only methods, fixed headers, atomic port allocation, immutable startup bytes, and clean shutdown are covered by tests.

The combined U09A/U09B/lock suites pass 36 tests, the full isolated repository passes 410 tests with 17 expected skips and retained historical NumPy warnings, dependency health is clean, and an isolated installation exposes the console command. A real final-code browser session rendered the shipped Grandview fixture at desktop and 390 x 844 widths, exercised individual decisions and attestation, created and hash-verified the exact response download, and completed with no console warning/error or page-level mobile overflow. That 893-byte response was explicitly marked as a software fixture, removed from Downloads, and retained only in non-ingest staging; it is not owner evidence and cannot enter reconciliation.

The earlier broad `python -m http.server` listener was reverified and stopped, every QA listener was closed, and no constrained server process remained. This unit made zero provider request and created no Petes pixel, candidate, production response, label, dataset, split, baseline, model, version, tag, Release, or deployment. Commits `530ce51a01aa3da9446638073f218179592d3cbc` and `09e03192563a1cd0fa4206a1bfe7863d6f3a24be` are the ordered source handoff; their active milestone integration below preserves newer U02/U03 facts.

## 2026-07-21 - U09C locked geospatial environment handoff

The environment improvement was also built in a sibling worktree so the active Petes scientific/custody checkout remained untouched. BurnLens now pins CPython 3.12.10 and one complete uv graph, keeps runtime and development profiles lean, and makes GeoPandas, Pyogrio, PyProj, Shapely, PySTAC Client, Xarray, Rioxarray, and Boto3 an explicit `geo-research` profile. A Windows Codex worktree selector runs the same repository-owned setup path.

Setup refuses an incomplete or wrong-interpreter `.venv`, a stale lock, dependency incompatibility, a concurrent profile synchronization, or a failed offline functional check. The verifier opens real HDF5, image, GeoTIFF, GeoPackage, projected-vector, Rioxarray, STAC-client, and Boto3-client paths without provider access or credentials. A clean detached checkout created all 66 locked distributions and passed; a deliberately stale project failed closed; the disposable checkout and environment were removed afterward.

All three profiles pass, the focused environment suite passes 4 tests, and the full isolated repository passes 414 tests with 17 expected custody skips, 16 retained historical NumPy warnings, and 50 subtests. The final installed-distribution audit reports no known vulnerabilities after advancing the former Pillow and pytest pins, and a representative VIIRS HTML/PNG pair remained byte-identical. This tooling creates no provider request, archive, Petes pixel, candidate, owner response, label, dataset, split, baseline, model, version, tag, Release, or deployment. Implementation commit `0323307486e5b0493e2f4c60d743c98cf4d18023` is the source handoff; active milestone integration verification follows.

## 2026-07-21 - U09A/U09B/U09C active milestone integration

After U03 selection evidence reached clean remote-equal checkpoint `96af78f25b996093721c04ced23889d7f0edaed5`, BurnLens integrated the six ordered U09A/U09B/U09C source commits as milestone commits `addb88bd1cd6c5e1b184914f5c5015ada95f304e`, `b28ce065afc8a06c0b2c1607f05f37218bae4ce0`, `08de31b24dd54ab4b4f74f6bb33bf62540dc10bc`, `df9fa39d4df455830d91266a111e9e7cba432f9f`, `c1c5102ecff7f4df497b69236b01a2c9e528d49e`, and `3ae2f9e01fd369572596cf5b7606e4e012d65f33`. Conflict resolution retained every newer U02/U03 fact, the Petes selector entry point, and the exact metadata-only replacement state while adding the preparatory batching, loopback, and environment evidence.

The prior matching environment was explicitly moved aside. Repository setup created a new `.venv` from `uv.lock` with Python 3.12.10 and all 66 locked distributions, then passed dependency integrity and the offline runtime/geospatial checks. The prior environment was removed only after the new one passed. `uv lock --check` passes; the combined environment, owner-review, loopback, and Petes scientific/custody suite passes 112 tests plus 44 subtests; the full active repository passes 443 tests plus 50 subtests with the same 20 NumPy deprecation warnings.

No provider transaction, scientific artifact, Petes candidate, production owner response, label, dataset, split, baseline, model, software version, tag, Release, or deployment advanced. Record commit `a25836e7849719fc77a30023e24c0c7d20676892` is pushed and remote-equal; issue #521 comment 5038722068 records the mapping, verification, boundaries, and next gate. The next scientific gate remains the exact committed replacement-post contract and clean preflight before a singleton archive request. Production U09 remains blocked on U08.

## 2026-07-21 - U03 replacement contract and clean preflight

The original two-archive contract was deliberately left unchanged. New `TERMS-2026-026` and contract `petes-lake-optical-post-remediation-contract-v0.1.0` authorize only the exact October 19 replacement UUID, 1,195,226,823-byte ZIP, and provider MD5/BLAKE3. Native bytes remain ignored and undistributed; public derivatives require modified-Copernicus attribution and retain the no-warranty, no-endorsement, and official-source boundaries.

Contract checkpoint `ab6ecc04d847aba848f0fc1f384405b10f0d8f9f` is pushed and remote-equal. From that exact clean commit, the production CLI preflight passed at 2026-07-21T20:56:29.367477Z without loading credentials. It re-fetched the two official terms sources unchanged, matched live OData identity, rebound every tracked source/selection/failure/custody input, and freshly reopened the retained pre archive at local SHA-256 `c5b6ededc5a264fcc617f2712093b847351c5b8ed78f2ea9ce0ebc3a33e1bc34`. Every replacement output target remained absent.

The wrapper can now execute one exact r001 transaction. It repeats the preflight before reading DPAPI credentials, clears credentials before the final verifier, retains bounded failures, and supports r002+ finalization from an already promoted archive without reacquisition or overwrite. PRECHECK-2026-048 and REGISTRY-2026-050 record the contract and gate. Ninety-five focused tests and the full 455-test / 50-subtest repository pass; 20 existing NumPy deprecation warnings remain.

No provider archive, replacement pixel, source pass, reference, candidate, owner response, label, sixth accepted event, dataset, split, baseline, or model exists yet. The nonzero catalogue snow remains a mandatory delivered SCL/SNW and real-render risk. Record this checkpoint on issue #521 before the wrapper transaction; U04 remains blocked.

## 2026-07-21 - U03 replacement custody

After issue #521 recorded the exact contract gate, the production wrapper reran the clean preflight from remote-equal trace `9cd81518c8fd859a950eab263e82dc9c9e406c59`, then loaded the machine-bound credential and requested only the October 19 singleton. The 1,195,226,823-byte archive downloaded in one attempt from offset zero. Provider MD5/BLAKE3 and local SHA-256 `8bf6ffac0d46d17b6f3716250aed9dff49b9f89b62a310c296a5d36f41a0e1d9` pass. The ZIP has 95 members, one expected SAFE root, a manifest, 1,195,191,837 uncompressed bytes, and passing full CRC.

No-replace promotion, immutable registration, single-link custody, ignored/untracked state, credential clearing, private/public semantic binding, and the wrapper's credential-free final verifier all pass. Quarantine is absent. The 22,246-byte public report has SHA-256 `e23d601959d09fb54fc6409f5a073df4f1a3a3a8a0d040e04a9b46c2594537b1`; its semantic SHA-256 is `78c888b4a3b954c4038a3454773b1cd7c922bc5f71957524e803ee6534d71f75`. The original pre was freshly rehashed and never reacquired; the original post and failed U03 remain unchanged.

Two later credential-free verifier attempts failed closed on transient remote-query and official-terms transport errors. Direct checks proved the branch unchanged, and one bounded retry passed. The cleanup audit also found five stale broad loopback servers on ports 8765/8767, including processes launched from the retired OneDrive checkout; exact process/listener identity was verified and all five were stopped. No old-checkout file was used or changed.

PRECHECK-2026-049 and REGISTRY-2026-051 retain the exact transaction, output hashes, transient failures, server containment, privacy boundary, and next dependency. Custody authorizes replacement U03 source-fitness r002 only. Local SCL/SNW, usable fraction, registration windows, continuous change, and real render remain untested for this replacement, so U04 stays blocked.

The first replay after committing the custody report exposed one verifier design weakness: it required current HEAD to equal the immutable transaction trace, so a legitimate evidence commit made future verification impossible. Commit `7f6ea500efae5bfb44bf2c6ecc8258ee9d3aab6c` now requires the recorded trace to be an ancestor of the current clean remote-equal HEAD while keeping every repository, branch, issue, source, terms, file-binding, metadata, archive, and semantic gate exact. The real committed report and both archives replay successfully from the descendant commit; divergent ancestry fails closed. PRECHECK-2026-050 and REGISTRY-2026-052 record 96 focused passes and the full 456-test / 50-subtest pass with no custody or scientific byte changed.

## 2026-07-21 - U03 replacement source fitness passes with exclusions

BurnLens generated the replacement-pair preview and final only after evaluator commit `d42022b5bfd1eb58f487666745e1d8f1c33db45c` was clean, pushed, and remote-equal. The evaluator rebinds the immutable failed r001 report, replacement selection, replacement custody report, and frozen event plan, then freshly verifies the original pre and replacement post registrations and hashes. It makes no provider request and changes no custody byte.

The exact replacement yields 33,365 eligible pixels / 97.8360% of the 34,103-pixel boundary. Replacement-post SCL has no cloud, cloud shadow, cirrus, snow/ice, nodata, or saturation pixel; CLD and SNW are zero throughout. Five registration windows pass, three remain review-needed spatial exclusions, and none fails registration. The continuous 33,365-pixel dNBR footprint remains context only.

The original-resolution 1,800 x 1,240 preview and final PNGs show clear, aligned pre/post panels, coherent post-fire browning, and no visible cloud, smoke/haze, snow, clipping, or misleading display artifact. The final decision is `PASS_EXACT_PETES_LAKE_REPLACEMENT_OPTICAL_SOURCE_FITNESS_WITH_SPATIAL_EXCLUSIONS`. Exact output replay reproduces the 60,069-byte JSON and 588,891-byte PNG byte-for-byte.

U04 official-reference delivery inspection became the next dependency. The U03 pass did not create or accept a reference pixel, candidate, owner decision, label, sixth complete event, dataset, split, baseline, model, release, field-validation, official, endorsed, operational, or emergency-ready claim.

## 2026-07-21 - U04 exact MTBS native contract

BurnLens submitted exactly one Petes Lake MTBS request after a pushed code-only preflight. The Portal accepted one map ID and ten MTBS-native products; eight BAER/RAVG-only menu products remained explicitly excluded. The official completion message arrived minutes later. It was read without mailbox mutation, and its private USGS route, recipient, and message identity never entered tracked evidence.

One bounded GET produced a 5,963,437-byte ZIP with SHA-256 `f17db688309c72eb84460f67476c116525ae3727bddfd406a8616f1fe0fad6da`. No redirect or retry occurred. Safe structure, event/map/program identity, member limits, expansion, and CRC passed before no-replace promotion into ignored single-link custody.

The next code checkpoint enforced a strict 20-member Petes-only roster and opened the exact FGDC and ISO notices before any native raster or vector. Access `None`, acknowledgement, MTBS credit, no-warranty and revision language, and the wetland warning all pass. No conflicting archive-specific or unresolved third-party term was found.

The delivered burn boundary and mask are one-polygon EPSG:32610 vectors. Five rasters share one exact 434 x 374, 30 m grid without reprojection or resampling. Reflectance, dNBR, RdNBR, dNBR6, DBF thresholds, class domain, PDF, and exact 188-member KMZ all pass. Class 0 remains outside/nodata, class 1 remains ambiguous, classes 2-4 remain bounded reference evidence, and class 6 remains excluded. WFS raw threshold fields are not silently rewritten into the delivered DBF semantics.

The production report is 32,991 bytes with SHA-256 `b489bd30b467ab38f7320c9b313f904e0bbe9a33e2bed8b346230b9f48a6053c`; its ignored replay is byte-identical. The full repository passes 480 tests plus 50 subtests with 20 existing NumPy warnings. U04 passes with zero accepted reference pixels. U05 now owns wetland measurement, optical-grid coverage, temporal support, source precedence, and any bounded reference-fitness decision.

## 2026-07-22 - U05 pre-intake source and terms checkpoint

U05 begins with the unresolved MTBS warning, not with a candidate. Fresh official-source review identifies the exact public USFWS National Wetlands Inventory Wetlands and Data Source layer routes, current metadata identities, 2026 catalog/DOI identity, and five exact disclaimer, limitations, caution, web-service, and data-download pages. The live layer metadata arrived as `text/plain; charset=UTF-8`, so the planned intake accepts only JSON-compatible transport followed by strict JSON, schema, topology, count, and ID validation.

The source gate is fixed before any feature response is opened. A source project must have exact `Complete` status, an explicit `BW`, `CIR`, or `TC` source type, nonplaceholder source/emulsion/metadata, defensible pre-ignition date/year and scale metadata, and complete full-pixel eligible coverage of the frozen optical boundary after half-cell-diagonal erosion. Ineligible any-touch overlap loses. Mapped wetland/deepwater geometry remains exclusion context only and receives the greater of 20 m or 1 mm at source scale as a disclosed BurnLens uncertainty buffer, capped by the 100 m acquired halo. NWI absence can never establish dry land or affirmative background.

Specific raw-payload and verbatim-attribute republication permission remains unresolved, so those bytes and strings stay ignored and unpublished. Only neutral local hashes, normalized gate decisions/reasons, necessary normalized dates/scales, and aggregate measurements may enter tracked evidence. `SOURCE-2026-032` is 7,569 bytes / SHA-256 `2f063e80ba1e0d049cd6230de26aa46cb86014bfc1e33aafac81823949fc21fb`; `TERMS-2026-028` is 5,608 bytes / `6a40f423720e1eb276cc3c192fededea16a4cf8992af025ad34dc3094dbaa104`; and `PRECHECK-2026-053` is 6,566 bytes / `86a8d67bea2aedd22aa519237ec21a92d2e0ca36bc4cc616b70fdd8b18367b38`. Exact working-tree and index bytes match and are bound by source checkpoint `d87951284f68be8638556d1e4de44cd36d648493`.

Issue #521 [comment 5040755162](https://github.com/drwbkr1/burnlens-deschutes/issues/521#issuecomment-5040755162), persisted at 2026-07-22T01:18:25Z, authorizes only the U05 CLI/LF path-family additions required for this bounded transaction. Offline initialization may create only the immutable plan and mutable intake contract from a clean, committed, pushed, remote-equal checkpoint. Before authorization or any provider-data request, the code must fetch each of the five official terms pages once without retry, verify the required semantics, and retain one ignored current hash receipt. Only then may the twelve-request provider-data roster begin.

Source checkpoint `d87951284f68be8638556d1e4de44cd36d648493` is pushed and remote-equal. Issue #521 comment [5040976356](https://github.com/drwbkr1/burnlens-deschutes/issues/521#issuecomment-5040976356) records 103 Petes-focused passes with one expected pre-intake skip and five subtests; the full repository passes 512 tests, one expected skip, 20 existing warnings, and 55 subtests. Lock, dependency, compile, diff, privacy, LF/index-byte, Git, zero-custody-target, and independent adversarial checks pass with no material blocker.

The terms refresh has not run, the custody contract has not been initialized, and zero NWI provider feature bytes have been requested or acquired. U05 remains not executed, accepts zero reference pixels, and leaves U06 plus every candidate, owner response, label, dataset, split, baseline, and model gate blocked. The next action is the clean remote-equal record sync, offline contract initialization, external validation, one-shot terms refresh, validation again, then the exact ordered provider roster.

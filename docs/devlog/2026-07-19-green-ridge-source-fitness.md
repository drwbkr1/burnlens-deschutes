# Devlog - Green Ridge Source Fitness

The v0.32 planner put Green Ridge first because it adds a new Sentinel-era event and repeats all three official reference regimes. The new cycle reproduced that plan exactly, refreshed both exact OData records, and refused to turn a broad STAC-search failure into a reason to change scenes.

The protected CDSE wrapper downloaded the two frozen archives sequentially. The pre archive needed one bounded fresh-token retry; both final files then passed provider checksums, local hashes, SAFE structure, manifests, and full CRC inspection before atomic registration. Credentials, tokens, headers, and raw bytes remained outside Git.

Actual native pixels are strong optical evidence: 44,110 boundary pixels, five quality-flagged pixels, and nine of nine registration windows passing. The rendered pre/post views show large continuous change, but BurnLens does not promote appearance or dNBR into labels. Official Green Ridge reference pixels remain the binding weakness, especially for affirmative background evidence.

The in-app browser security policy blocked local `file://` navigation and was not bypassed. The canonical original-resolution PNG, semantic self-contained HTML, exact reconstruction, and privacy checks provide the rendered validation for this local repository surface.

Release verification also exercised the custody boundary from a fresh remote checkout. A directory junction to the registered package was rejected because that checkout had not performed the registration transaction. BurnLens did not relax the verifier. The clean source instead reconstructed all three outputs through the unchanged original custody path, while its test suite treated local custody as absent. Two fixed-epoch wheels are byte-identical at 489,828 bytes / SHA-256 `e11f2f073b7fb03f07fe95d4b48304b57bf78ac431a6bb942c8ab8b40d113a00`; the isolated 0.33.0 install exposes 50 commands and has no broken requirements.

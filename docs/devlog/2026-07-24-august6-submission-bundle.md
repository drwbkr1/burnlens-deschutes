# August 6 submission bundle

The verified page was strong. The handoff was not yet one-step.

BurnLens now builds one deterministic ZIP. A reviewer extracts it and opens
`START-HERE.html`. No development environment or network is required.

The package includes only exact verified public evidence. It rewrites the
reviewer page links into the extracted root, adds plain instructions, records
an internal manifest, and writes an external receipt.

Production and replay bytes match. Two extractions match. The real extracted
page passes desktop and narrow Chrome QA, all nine links, both images, keyboard
order, clean console, and zero external requests.

The first archive exposed missing LF checkout rules for five text members.
BurnLens retained it as failure evidence, added explicit contracts, and rebuilt
final r002 from checkout-stable bytes.

The first full suite exposed 29 stale package-version expectations. BurnLens
retained that run and updated only current-package assertions. The historical
v0.48 report identity remains fixed. All 187 affected tests now pass.

The bundle changes presentation custody only. It adds no data, model, metric,
scientific claim, deployment, or public-sharing change.

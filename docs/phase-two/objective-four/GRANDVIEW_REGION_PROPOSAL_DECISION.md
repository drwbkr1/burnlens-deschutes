# Grandview Region Proposal Decision

## Decision

`PROPOSE_TWO_GRANDVIEW_REGIONS_KEEP_OWNER_REVIEW_AND_PROMOTION_CLOSED`.

BurnLens 0.42.0 reruns the exact v0.41 source pipeline and applies the established region-candidate contract without threshold tuning. The burned route requires MTBS dNBR6 classes 2-4 on the verified context grid plus finite pre/post dNBR; RAVG modeled classes are not used. The background route retains the v0.41 three-scene stability, neighborhood, exact program-footprint, 60 m boundary-uncertainty, and registration-exclusion gates.

Both routes are partitioned by the established fixed 0.05 dNBR evidence-coherence bin. Selection keeps one intact eight-connected component per class, minimizes distance to the 25-pixel review target, and resolves exact ties by SHA-256 of class, bin, and ordered native-grid coordinates. It never clips or expands a component.

The result is one 25-pixel burned core and one 25-pixel background core, each 1.0 ha at the 20 m grid. Their one-pixel unknown rings contain 46 and 52 pixels. The burned proposal's observed pre/post dNBR is 0.015383 to 0.049850; the report exposes that low-effect range alongside its MTBS class evidence for owner judgment. Exact EPSG:32610 rasters encode 0 outside, 1 core, 2 unknown ring, and nodata 255; their tags state that no label was created.

The two regions are unreviewed proposals, not labels or truth. Issue #511 owns a separately versioned blank owner yes/no/uncertain surface. Response intake and any promotion remain later checkpoints. No dataset, split, baseline, model, metric, accuracy, field-validation, official, endorsed, or operational claim advances.

Public evidence is `GRANDVIEW-REGION-PROPOSAL-2026-001`, run `BL-2026-07-20-grandview-region-proposal-r001`, generated by source commit `78bb79eb04d76cff4ed183274de881557b176413`.

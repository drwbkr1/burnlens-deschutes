# Grandview Owner Response Intake Decision

**Issue / PR:** #517 / #522

**Run:** `BL-2026-07-21-grandview-owner-response-intake-r002`

**Decision:** `ACCEPT_GRANDVIEW_OWNER_APPROVED_PROTOTYPE_LABELS_DEFER_DATASET_SPLIT_BASELINE_MODEL`.

BurnLens preserved the exact 887-byte owner export and its separately hashed pre-reveal receipt in ignored repository-local no-overwrite custody before any decision value was read. The hardened intake pins both production hashes plus 22 exact source, terms, review, and prior-manifest records. Unit decisions, notes, private filenames, and private paths remain unpublished.

The aggregate response is two yes, zero no, and zero uncertain. Yes alone creates nothing. Both exact 25-pixel cores also re-pass proposal/raster identity, EPSG:32610 native grids, nodata/domain, eight-connectivity, exact one-pixel unknown rings, source and terms, quality/registration, uncertainty exclusion, and immutable event identity. BurnLens therefore adds one burned and one background region to `owner-approved-prototype-region-labels-v0.3.0`.

The cumulative prototype evidence is balanced at ten regions: five burned, five background, 236 core pixels / 9.44 ha, and 431 excluded unknown-ring pixels across five complete event groups. This is not independent ground truth, inter-rater validation, field validation, an official product, a dataset, split, baseline, model, metric, accuracy result, or operational system.

The sixth event count is necessary but not sufficient. Petes Lake must pass equivalent source, class, uncertainty, owner, and leakage gates under issue #521, after which the separate sufficiency evaluator must still prove class/unknown completeness, source-regime replication, never-tuned transfer evidence, and event dominance before dataset work can reopen.

PR #522 merges at verified checkpoint `5e1d5a05dbb09e8ac42be5928b2d042a0737336e`. Fresh merged main reconstructs the exact private reconciliation and all three public outputs, passes the full 328-test / 17-skip suite and repository checks, reproduces the canonical 630,232-byte wheel, and passes an isolated dependency-complete install. Annotated tag object `e4f834cd3c55d44895766695a40746fa224df9bd` remotely peels to that checkpoint. Lifecycle issue #523 records the release.

The owner directed BurnLens to stop after v0.44 synchronization and not begin issue #521 or a later checkpoint while a separate governance-only checkpoint remains pending. This records execution status only and does not amend the existing checkpoint policy.

> Experimental BurnLens owner-approved prototype region labels. Not ground truth, official wildfire information, emergency guidance, or a dataset. Official sources govern.

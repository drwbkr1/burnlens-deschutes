# Region Owner Review Surface Decision

**Issue / branch:** #457 / `codex/p2o4-t18-region-review-surface`

**Run:** `BL-2026-07-18-region-owner-review-surface-r005`

**Generator source / public artifacts:** `7fedd84c4ac9c732f698cf3b40b07e70bfe01cc6` / `f0412fd85092f00a5122b775c3e07891d2f156d7`

## Decision

`SURFACE_READY_FOR_OWNER_REGION_REVIEW_KEEP_RESPONSE_INTAKE_SEPARATE`.

BurnLens now presents the six exact v0.29 candidate regions as separately bound owner questions. Every card shows actual pre/post optical evidence, dNBR, permitted official reference context, the exact candidate core, its unknown ring, proposed class, pixel counts, and raster SHA-256. The generator reopens and verifies every candidate GeoTIFF before rendering and rejects drift in CRS, class domain, core/ring counts, no-promotion tags, report identity, run identity, or exact bytes.

The offline surface accepts only yes, no, or uncertain; supports hash-named draft and response export, exact bound-file reload, completion checks, and browser-session locking; and embeds no network resource. Completed export disables the loader with every response control so a locked session cannot be replaced. A yes remains necessary but insufficient. Later response intake must preserve exact bytes and then re-run reproducibility, source, quality, terms, uncertainty, and event-level leakage gates before any region-label decision.

The authoritative output and a second reconstruction match across all ten files. Real desktop and 390 by 844 browser inspection verifies semantic rendering, six loaded local evidence images, no horizontal overflow, no external resources or console warnings/errors, native labeled controls and live status, incomplete-state blocking, the six-decision path, owner attestation, and hashed draft generation. The in-app harness does not support native file upload, so bound reload remains covered by the deterministic controller and response-template tests rather than represented as owner evidence.

No owner response is collected, no region label is created, and dataset, split, baseline, and model versions remain absent. Nothing here establishes independent ground truth, inter-rater agreement, field validation, official or endorsed status, operational readiness, emergency readiness, or accuracy.

The candidate gate passes 246 tests, exact ten-file reconstruction, real rendering, compilation, dependency health, and reproducible packaging. The isolated 0.30.0 wheel exposes 44 commands and 96 clean entries. Merge, tag, and fresh-main verification remain separate release gates.

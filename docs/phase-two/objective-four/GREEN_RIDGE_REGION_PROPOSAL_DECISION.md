# Green Ridge Region Proposal Decision

## Decision

`PROPOSE_TWO_GREEN_RIDGE_REGIONS; KEEP_OWNER_REVIEW_AND_PROMOTION_CLOSED`.

BurnLens 0.36.0 reruns the exact v0.35 source pipeline and applies the established region-candidate contract without threshold tuning. The burned route requires MTBS dNBR6 and RAVG CBI4 classes 2-4 to agree with finite pre/post dNBR. The background route retains the v0.35 three-scene stability, neighborhood, both-program class-0, and 60 m boundary-uncertainty gates.

Both routes are partitioned by the already established fixed 0.05 dNBR evidence-coherence bin. Selection keeps one intact eight-connected component per class, minimizes distance to the 25-pixel review target, and resolves exact ties by SHA-256 of class, bin, and ordered native-grid coordinates. It never clips or expands a component.

The result is one 25-pixel burned core and one 25-pixel background core, each 1.0 ha at the 20 m analysis grid. Their one-pixel unknown rings contain 50 and 37 pixels. Exact EPSG:32610 rasters encode 0 outside, 1 core, 2 unknown ring, and nodata 255; their tags state that no label was created.

The two regions are unreviewed proposals, not labels or truth. Issue #487 owns a separately versioned blank owner yes/no/uncertain surface. Response intake and any promotion remain later checkpoints. No dataset, split, baseline, model, metric, accuracy, field-validation, official, endorsed, or operational claim advances.

Public evidence is `GREEN-RIDGE-REGION-PROPOSAL-2026-001`, run `BL-2026-07-20-green-ridge-region-proposal-r001`, generated by source commit `690137f1530a00ca79b8f5831f700541809b2a2b`.
